# Catalog of the removed `bugs/` directory

Generated by `bin/render` on 2026-08-06. Do not edit -- edit `catalog.tsv`.

The 857 files below were deleted by [`d2c8d27826`](https://github.com/Macaulay2/M2/commit/d2c8d278264348116ac4c1feeb95150699e3e11b) ("remove bugs directory"). Each was a bug report or feature request in Macaulay2's pre-GitHub catalog. This tracks whether each one still applies, whether an issue already covers it, and what fixed it.

Settling all of these is [#36](https://github.com/Macaulay2/M2/issues/36).

## Progress

**315 of 857 triaged (36.8%)**

| verdict | count | |
| --- | ---: | --- |
| `open` -- Still broken | 87 | `###.........................` |
| `duplicate` -- Already tracked by an open issue | 34 | `#...........................` |
| `fixed` -- Fixed | 132 | `####........................` |
| `wontfix` -- Won't fix | 26 | `#...........................` |
| `obsolete` -- Obsolete | 36 | `#...........................` |
| `todo` -- Not yet triaged | 542 | `##################..........` |

### Reproducer runs

`bin/run-repros` runs each `.m2` file under the installed M2 and records the exit status. **This is a hint, not a verdict.** These scripts are old enough that many now fail on API drift rather than on the bug they demonstrate (see `cache/stale-candidates.txt`), and `pass-partial` marks a script that exited 0 only because a bare `end` line stopped it before the interesting part.

| autorun | count | |
| --- | ---: | --- |
| `pass` | 44 | ran to the end without error |
| `pass-partial` | 41 | exited 0, but an `end` line cut it short -- weak signal |
| `fail` | 129 | uncaught error or failed assert |
| `timeout` | 15 | still running at the time limit |
| `n/a` | 628 | prose, not a runnable script |

### By original author

| directory | files | triaged |
| --- | ---: | ---: |
| `bugs/dan` | 582 | 291 |
| `bugs/mike` | 215 | 9 |
| `bugs/anton` | 49 | 15 |
| `bugs/LAcore` | 9 | 0 |
| `bugs/(root)` | 1 | 0 |
| `bugs/gfurnish` | 1 | 0 |

## Still broken -- `open` (87)

| prio | file | issue | fix | disposition | note |
| ---: | --- | --- | --- | --- | --- |
| 0 | `bugs/dan/0-M2-compute-process` | [#4526](https://github.com/Macaulay2/M2/issues/4526) | &nbsp; | issue | dumpdata went in 2019 (d560e8284a) and nothing replaced it: there is no fork server or daemon mode, so every invocation pays full startup, which is the cost the proposal was meant to amortize |
| 0 | `bugs/dan/0-SCSCP-checks` | [#4527](https://github.com/Macaulay2/M2/issues/4527) | &nbsp; | issue | not automated: startServer(String,String) exists so binding to 127.0.0.1 is available, but SCSCP.m2 has no TEST blocks, no workflow references it, and there is no insecure-check make target |
| 0 | `bugs/dan/0-Schubert2-projectiveBundle` | [#4538](https://github.com/Macaulay2/M2/issues/4538) | &nbsp; | issue | never attempted, and the tree is still on the near side of the diff the file carries: Schubert2/doc.m2:179 has projectiveBundle'(dual B, VariableNames => {,{z}}) and :1525 has map(B,A,{-h, h, h^2, h^3, h^4, h^5}), both its minus lines, and Schubert2.m2 has no elimination logic. Nothing broke because nothing changed. The file is worth filing for the patch it carries rather than the request: eliminating the unneeded variables in a projective bundle's intersection ring moves the generators, and the explicit abstract maps built by map break when it does, so the two doc adjustments are what anyone attempting it will need. No existing issue covers it; #2658 is Schubert2-adjacent but unrelated |
| 0 | `bugs/dan/0-add-mutex-for-factory` | [#4494](https://github.com/Macaulay2/M2/issues/4494) | &nbsp; | issue | still true: no C-level lock; ThreadedGB.m2:33 works around factory instead; a specific instance under #175, the umbrella thread-safety issue, alongside #3675, #3895 and #3927 |
| 0 | `bugs/dan/0-attribute-constructor` | [#4495](https://github.com/Macaulay2/M2/issues/4495) | &nbsp; | issue | still emitted: c/cprint.c:487 puts __attribute__ ((constructor)) in generated C |
| 0 | `bugs/dan/0-backtrace-from-debugger` | [#4507](https://github.com/Macaulay2/M2/issues/4507) | &nbsp; | issue | backtrace exists but is only a variable toggling whether one is shown after an error; there is still no way to produce one on demand from the debugger |
| 0 | `bugs/dan/0-bugs-ataylor.m2` | &nbsp; | &nbsp; | &nbsp; | the first ask is done -- prune N and minPres N both equal minimalPresentation N -- but the rest are unverified: synonyms in emacs highlighting and the doc index, and res landing on the resolution node |
| 0 | `bugs/dan/0-bugs-decker.m2` | &nbsp; | &nbsp; | &nbsp; | both asks stand: MinimalPrimes.m2:58 still carries '-- absolute case?', and no radical algorithm names Krick/Logar or Kemper |
| 0 | `bugs/dan/0-bugs-eisenbud.m2` | &nbsp; | &nbsp; | &nbsp; | at least two of its four asks stand: Tor errors with 'not implemented yet for noncommutative rings', and Linear is not among quotient's strategies |
| 0 | `bugs/dan/0-bugs-iswanson.m2` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files; individual asks unverified. Eight items separated by rules, each a transcript: ZZ/15 rejected as composite, two kernel computations over fraction fields failing as 'not implemented yet', RevLex behaving like GRevLex, a^(2^16) coming out 1 under LexSmall with proposed limits of 2^15-1 and 2^7-1, leadTerm(ZZ,RingElement) wanted, an unclear coefficient in gb output, and GroupRevLex not installed |
| 0 | `bugs/dan/0-bugs-kummini.m2` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files; individual asks unverified. Seven dated items headed ###### 1 through 7, one of which the reporter retracts -- 'AFTERTHOUGHT: I now think that the bug report is false, but I am leaving it here since Mike did say that there is some problem with DegreeLimit'. The others cover listUserSymbols and clearAll both failing with 'expected a list, hash table, or sequence', a [minors, First] reference generated into the documentation, and the Undocumented key |
| 0 | `bugs/dan/0-bugs-lgold.m2` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files; individual asks unverified. Eight items separated by rules, the first retracted in the file itself -- 'Never mind the earlier bug ... I figured out what was going on'. The rest are documentation-shaped: the poincare page, hilbertFunction and hilbertSeries on a CoherentSheaf with their doc pages commented out, examples wanted for reduceHilbert and PoincareN, saturation in hilbertSeries of a ProjectiveVariety, syntax colouring of dashes in a resolution, and making the emacs instructions prominent |
| 0 | `bugs/dan/0-bugs-popescu.m2` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files; individual asks unverified. Six numbered items, of which 6 is already marked FIXED in the file: a faceRing for SimplicialComplex returning the Stanley-Reisner ring, codim of an AffineVariety, why (codim,PolynomialRing) exists, a sheafExt computation returning zero, and genus of an ideal to match genera |
| 0 | `bugs/dan/0-bugs-stillman.m2` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files, and the least tractable of them; individual asks unverified. Not a bug list so much as Mike's working scratchpad: design musings on forceGB, bare TODO words under headings such as 'the following all need some efficiency work -- quotients, saturation, elimination', several items already annotated -- FIXED in place, and replies from Dan in the margin ('what's the problem? [drg]', 'leave those above for Mike (?) [drg]'). Counting its asks is itself a judgement, and much of it is actionable only by its author |
| 0 | `bugs/dan/0-check-for-integer-usage` | [#4517](https://github.com/Macaulay2/M2/issues/4517) | &nbsp; | issue | never adopted: fsanitize appears nowhere in the tree and no CI workflow builds with a sanitizer |
| 0 | `bugs/dan/0-debugging-loadPackage` | [#4533](https://github.com/Macaulay2/M2/issues/4533) | &nbsp; | issue | documented behavior and actual behavior disagree: code.m2:263 says 'break -- leave the debugger, returning to top level', but break leaves the debugger and resumes the file past the failing expression. Four lines reproduce it -- load a file whose function calls error, then break, and the rest of the file runs. Dan's case is the visible symptom: loading Macaulay2Doc twice, break carries on into the next error in the same file and re-enters the debugger, leaving the prompt at ii for the session. Distinct from #1928: continue re-runs the failing expression and loops, break skips it and continues, so the two commands differ and neither matches its description |
| 0 | `bugs/dan/0-dictionaryPath` | [#4496](https://github.com/Macaulay2/M2/issues/4496) | &nbsp; | issue | still reproduces: OutputDictionary is on dictionaryPath while a package loads; related to #1427, which proposes the same trim-and-restore fix |
| 0 | `bugs/dan/0-doc-Keywords` | [#4509](https://github.com/Macaulay2/M2/issues/4509) | &nbsp; | issue | the four examples are aliases from other systems, so the need is discovery by synonym; about "Normalform" returns nothing even with Body=>true, and about "solve" returns 220 hits swamped by resolve/resolution. newPackage's Keywords is per package and holds subject categories for browsing, not search aliases. #3689 is the diacritic special case of the same want -- about "Groebner" should find the node titled "Gröbner bases" -- which accent folding alone would fix, while author-declared aliases would subsume it |
| 0 | `bugs/dan/0-doc-option-names-in-packages` | [#4508](https://github.com/Macaulay2/M2/issues/4508) | &nbsp; | issue | verified: a package-defined option name must be exported or installPackage fails with "mutable unexported unset symbol(s) in package X: 'MyOpt'"; exporting it installs cleanly. Undocumented in Macaulay2Doc and in the wiki style guide. Note the file misplaces the error: it comes from package closing, not documentation processing, and fires with no doc node present |
| 0 | `bugs/dan/0-document-packages` | &nbsp; | &nbsp; | &nbsp; | two of four done: XML documents itself and [newPackage,Reload] is documented at package-doc.m2:274. --debug n exists but is undocumented, as are M2's command line options generally, and warningMessage is still unexported |
| 0 | `bugs/dan/0-engine-tower-rings` | [#4497](https://github.com/Macaulay2/M2/issues/4497) | &nbsp; | issue | raw hooks exist (rawTowerRing, e/rings/tower.cpp) but no top-level engineTowerRing |
| 0 | `bugs/dan/0-errorDepth` | [#4498](https://github.com/Macaulay2/M2/issues/4498) | &nbsp; | issue | unchanged: (loadDepth,errorDepth) is still (3,0) at startup |
| 0 | `bugs/dan/0-gb-strategies` | [#4518](https://github.com/Macaulay2/M2/issues/4518) | &nbsp; | issue | not done: gb.m2 keeps a fixed RawStrategyCodes table and errors on an unknown strategy, and BooleanGB exports gbBoolean as a separate entry point rather than registering one |
| 0 | `bugs/dan/0-indexed-variables` | [#4510](https://github.com/Macaulay2/M2/issues/4510) | &nbsp; | issue | still reproduces, one call earlier than reported: after a second ring reuses x_0..x_3, class\(R_0..R_3) gives IndexedVariable and stays that way; #2771 covers the warnings in the same area, not this |
| 0 | `bugs/dan/0-installing-methods-functions-on-function-closures` | [#4511](https://github.com/Macaulay2/M2/issues/4511) | &nbsp; | issue | first List := x->x is still accepted silently; note the file's own warning that erroring here would interfere with typicalvalues.m2; #1979 proposes typed function closures from the other direction, wanting the capability rather than an error |
| 0 | `bugs/dan/0-interrupts` | &nbsp; | &nbsp; | &nbsp; | the interpreter is still generated C from the d language, not C++; left unqueued because the file poses a research question rather than a request |
| 0 | `bugs/dan/0-inverse-matrix` | [#4506](https://github.com/Macaulay2/M2/issues/4506) | &nbsp; | issue | still reproduces: inverse matrix {{1,2}} returns matrix {{1},{0}} rather than erroring on a non-square matrix; related to #3738, which is a different path |
| 0 | `bugs/dan/0-isSurjective-RingMap` | [#4499](https://github.com/Macaulay2/M2/issues/4499) | &nbsp; | issue | no method is installed for (isSurjective,RingMap) |
| 0 | `bugs/dan/0-makefile-d` | [#4512](https://github.com/Macaulay2/M2/issues/4512) | &nbsp; | issue | still hand-maintained: d/Makefile.files.in lists the e/*.hpp dependencies literally and carries the comment 'should automate these dependencies' |
| 0 | `bugs/dan/0-methods-with-options` | [#4513](https://github.com/Macaulay2/M2/issues/4513) | &nbsp; | issue | still reproduces: g (ZZ,ZZ) := List => (opts)->(m,n)->... is accepted but installs nothing, leaving methods g empty; #2864 is a different confusion in the same area |
| 0 | `bugs/dan/0-monomialIdeal` | [#4500](https://github.com/Macaulay2/M2/issues/4500) | &nbsp; | issue | unchanged: monomialIdeal {} still errors 'expected a polynomial ring without quotient elements' |
| 0 | `bugs/dan/0-needsPackage` | [#4537](https://github.com/Macaulay2/M2/issues/4537) | &nbsp; | issue | 89 percent done and the remainder is a finite, checkable list: 233 of 300 distributed packages use PackageImports or PackageExports, and 34 still call needsPackage before beginDocumentation. Every other hit is inside a TEST block, where needsPackage is right -- Benchmark.m2 is the pattern, at line 13 in Dan's 2012 grep and now only at line 266 inside a TEST. Converting the 34 is not mechanical: PackageImports loads without importing symbols, so a package that wants the symbols has to use PackageExports or keep needsPackage, which is the gap #4502 describes |
| 0 | `bugs/dan/0-needsPackage-unadorned` | [#4502](https://github.com/Macaulay2/M2/issues/4502) | &nbsp; | issue | needsPackage still has no Using option; its options are LoadDocumentation, Configuration, FileName, Reload, DebuggingMode |
| 0 | `bugs/dan/0-newPackage` | [#4505](https://github.com/Macaulay2/M2/issues/4505) | &nbsp; | issue | reproduces exactly: (options Foo).Configuration is an OptionTable via loadPackage but a List via load |
| 0 | `bugs/dan/0-on` | [#4534](https://github.com/Macaulay2/M2/issues/4534) | &nbsp; | issue | still unmet, and the obvious workaround does not cover the useful case: f = on f only works for functions you defined, since basis = on basis is an error on a protected global, so Core method functions -- the ones worth tracing -- are out of reach. Dan's route works, installMethod(basis, ZZ, Module, on(lookup m, Name => ...)) traces real calls including ones made from library code. Three gaps stand: on labels by argument rather than by method, so foo(2,3) prints 'Sequence' and an inherited call prints the argument's class rather than the method that ran; there is no way to trace in place without rebinding; and for a MethodFunctionWithOptions the trace shows the OptionTable and FunctionClosure layer instead of the real arguments, which is the 'digging in one level deeper' the file asks for. His draft's own gap remains -- toString and toExternalString are in neither class, wider now that MethodFunctionSingle exists |
| 0 | `bugs/dan/0-package-garbage-collection` | [#4536](https://github.com/Macaulay2/M2/issues/4536) | &nbsp; | issue | the Attributes half reproduces and is measurable: 30 newPackage(...,Reload=>true) cycles take Attributes from 2682 entries to 2806, about 4 leaked per reload, never reclaimed. That is also the mechanism that would eventually make 0-slowness's complaint true again, since getAttributes scans the whole table. Not covered by #488 or #2562, which are engine and sequence allocation |
| 0 | `bugs/dan/0-precision-and-equality` | [#4503](https://github.com/Macaulay2/M2/issues/4503) | &nbsp; | issue | reproduces exactly: 1p10 == 1.0000000000000001 is true but 1p10 == 1.000000000000001 is false |
| 0 | `bugs/dan/0-quotient-for-non-free-modules` | [#4515](https://github.com/Macaulay2/M2/issues/4515) | &nbsp; | issue | the inconsistency stands: Matrix % Matrix still requires free modules while quotientRemainder accepts the same non-free source |
| 0 | `bugs/dan/0-reloading-packages` | [#4521](https://github.com/Macaulay2/M2/issues/4521) | &nbsp; | issue | still drops it: loadPackage Package forces Reload => true (packages.m2:193), and neither installPackage.m2:670 nor testing.m2:87 passes Configuration, so a package loaded with Configuration => {...} comes back with the defaults |
| 0 | `bugs/dan/0-rename-minimalPresentation` | [#4504](https://github.com/Macaulay2/M2/issues/4504) | &nbsp; | issue | minimizePresentation does not exist; the rename was never done |
| 0 | `bugs/dan/0-sprintf-buffer-overflow` | [#4522](https://github.com/Macaulay2/M2/issues/4522) | &nbsp; | issue | the audit never happened: 14 bare sprintf calls remain in c/ and d/, and two are sized by input rather than bounded -- uniquify formats an arbitrary identifier into char buf[1000] (c/dictionary.c:33) and system_errfmt leaves 10 bytes of slack for three ints (d/scclib.c:355) |
| 0 | `bugs/dan/0-symmetricPower` | [#4530](https://github.com/Macaulay2/M2/issues/4530) | &nbsp; | issue | half landed: the Module case now handles relations via coimage basis(p, symmetricAlgebra M) (multilin.m2:90), but the Matrix case is still the raw call the file calls probably-wrong, and they disagree -- symmetricPower(2,m) on a map of cokernels returns R^1 <-- R^1, forgetting the relations |
| 0 | `bugs/dan/0-typical-values` | [#4523](https://github.com/Macaulay2/M2/issues/4523) | &nbsp; | issue | undocumented: "specifying typical values" shows only the unary form prune Matrix := Matrix => f, and neither "binary methods" nor "installing methods" mentions X + X := X => (x,y) -> ...; the form works and does record typicalValues#(symbol +,X,X) |
| 0 | `bugs/dan/0-utf-8-in-doc-filenames` | [#4531](https://github.com/Macaulay2/M2/issues/4531) | &nbsp; | issue | still generated: toFilename maps ASCII specials through the tt table but passes multi-byte UTF-8 unchanged, and the installed docs hold 9 such names including the exact ___Gröbner_spbases.html this file names; #47 covered it but closed in 2014 on the broken links, not on the ask; #211 is a second closed symptom of the same cause, the file appearing under two utf-8 normalizations (303 266 vs 314 210), closed on 'just remember how I do it and check that one file' |
| 0 | `bugs/dan/0-vector-empty-list` | [#4524](https://github.com/Macaulay2/M2/issues/4524) | &nbsp; | issue | still no empty vector, and the message got worse: vector {} now dies in vector Matrix with "expected source to be free with rank 1" (modules.m2:70), and vector(R,{}) and vector(R^0,{}) fail the same way |
| 0 | `bugs/dan/0-when` | [#4525](https://github.com/Macaulay2/M2/issues/4525) | &nbsp; | issue | scc1 still miscompiles it: with every case covered, the else body is emitted inside the switch with no case label (chk.c:783-794 labels only uncovered types), so it is unreachable dead code and nothing diagnoses it |
| 0.1 | `bugs/dan/0.1-doc-remainder-links` | [#4539](https://github.com/Macaulay2/M2/issues/4539) | &nbsp; | issue | still unlinked: help "methods for normal forms and remainder" lists %, Gröbner bases, generators and Matrix % GroebnerBasis under See also, and nothing for //. There is no node actually titled 'methods for getting the coefficients', so the ask reads as a link to the // operator, which is where the quotient half lives |
| 0.1 | `bugs/dan/0.1-random-ring-elements` | [#4540](https://github.com/Macaulay2/M2/issues/4540) | &nbsp; | issue | not added: lookup(random, Sequence, Ring) is null, and random((4:1,3:2),R) errors. The file supplies the implementation it wants -- random(Sequence,Ring) := o -> (degs,R) -> for d in deepSplice degs list random(d,R) -- which is also what makes the mixed form random((4:{1},3:2),R) work, since deepSplice flattens the degree lists. No duplicate: #877 and #4161 both concern the one-argument random(List), where #4161 wants it to mean a random element with shuffle(List) taking over the permuting. Worth cross-referencing there, since this would add a two-argument form alongside it |
| 0.5 | `bugs/dan/0.5-Benchmark-package` | [#4541](https://github.com/Macaulay2/M2/issues/4541) | &nbsp; | issue | half done: Dan added the per-benchmark tests himself in 8753fd3ae7 (2010), scan(keys benchmarks, b -> TEST("runBenchmarks "\|format b)), so every benchmark now runs as a test. yang-gb1 still ends '(ti,re) := toSequence timing gb(J1); ti)' with no assertion on the result, which is the other half of the file |
| 0.5 | `bugs/dan/0.5-SafeC-bugs` | [#4542](https://github.com/Macaulay2/M2/issues/4542) | &nbsp; | issue | reproduces exactly under a current scc1: the file's lines give 'error: type mismatch' at line 8, the f():int := ( when ... ) whose every branch returns. Line 6, the direct expression form of the same when, compiles -- so it is specifically the statement form with return in all branches that scc1 does not recognise as returning. Adjacent to #4525, which is the other scc1 when defect |
| 0.5 | `bugs/dan/0.5-alternate-stack` | [#4543](https://github.com/Macaulay2/M2/issues/4543) | &nbsp; | issue | never done: sigaltstack, SA_ONSTACK and SIGSTKSZ appear nowhere in the tree, so a SIGSEGV raised by stack exhaustion still cannot be caught |
| 0.5 | `bugs/dan/0.5-compare-CC-CC` | [#4544](https://github.com/Macaulay2/M2/issues/4544) | &nbsp; | issue | reproduces exactly: 1+2*ii < 2.*ii is false and 1+2*ii ? 2.*ii is >, so an unordered pair of complex numbers still compares silently instead of erroring as the file asks |
| 0.5 | `bugs/dan/0.5-document-new-methods` | [#4545](https://github.com/Macaulay2/M2/issues/4545) | &nbsp; | issue | still unlinked: the node 'making new functions with optional arguments' (ov_language.m2:509) links only to >> and to 'symbols used as the name or value of an optional argument'. method-doc.m2:14 links the other way, which is the direction that already worked |
| 0.5 | `bugs/dan/0.5-hh-dispatch` | [#4546](https://github.com/Macaulay2/M2/issues/4546) | &nbsp; | issue | not done: Varieties.m2:777 still builds the lookup key as a := (pq,X) with X the entire argument, so hh^i(x,y) looks up (ZZ,Sequence) and an installed hh(ZZ,X,Y) is unreachable -- calling it errors 'no method available'. The file's premise that HH already allows this does not hold either: HH^2(x,y) fails identically |
| 0.5 | `bugs/dan/0.5-select-doc` | [#4547](https://github.com/Macaulay2/M2/issues/4547) | &nbsp; | issue | layout unchanged: help select still prints See also, with partition in it, before Ways to use, which is the order the file asks to reverse. The SeeAlso list has since grown to eight entries, making the placement more prominent rather than less |
| 0.5 | `bugs/dan/0.5-temporaryDirectory` | [#4548](https://github.com/Macaulay2/M2/issues/4548) | &nbsp; | issue | not provided: temporaryDirectory does not exist, though removeDirectory does -- so of the pair the file asks for, only the removal half is available |
| 0.8 | `bugs/dan/0.8-documentation-suggestion-link` | [#4549](https://github.com/Macaulay2/M2/issues/4549) | &nbsp; | issue | never added: no mailto, feedback or issue link is emitted onto doc pages -- the mailto hits in the tree are author addresses in ov_preface.m2 and package metadata. A link to the GitHub tracker would be the modern form of the ask; the mailto and google-group forms the file proposes are both dated |
| 0.9 | `bugs/dan/0.9-discrete-log` | [#4550](https://github.com/Macaulay2/M2/issues/4550) | &nbsp; | issue | not hooked up: rawDiscreteLog exists (d/interface.dd:996, e/interface/ringelement.cpp:286) but methods log covers only Number and InexactNumber, so both log(a^7) and log(a,a^7) over GF(2^5) fail with no method |
| 0.9 | `bugs/dan/0.9-toField` | [#4551](https://github.com/Macaulay2/M2/issues/4551) | &nbsp; | issue | none of the asks met: toField still returns a PolynomialRing rather than a Field class; gens is worse than unimplemented, giving {} where gens of the same quotient before toField gives {x}; and promotion across the tree of derived rings is unchanged. #1660 and #3973 are open toField issues, neither of them this |
| 1 | `bugs/dan/1-Ring_Sequence` | [#4557](https://github.com/Macaulay2/M2/issues/4557) | &nbsp; | issue | not installed: R_(1..3) still errors with 'no method for binary operator _' on (PolynomialRing, Sequence). Two spellings do the job today -- (R_*)_{1,2,3} and, literally the file's own body, apply(1..3, i -> R_i), both giving (b,c,d) -- so this is convenience rather than a gap in capability. One detail for whoever takes it: the adjacent slot is already occupied and differently, since R_{1,2,3} means the monomial with that exponent vector, a*b^2*c^3, so Ring _ List is not available but Ring _ Sequence is free. R_* postdates the file and covers most of what David liked about it. #3515 is a different notation ask, about partial application of functions |
| 1 | `bugs/dan/1-TAGS` | [#4561](https://github.com/Macaulay2/M2/issues/4561) | &nbsp; | issue | unmet, and worth filing after all -- the earlier note called the value marginal on the assumption that nobody uses tags, which the repository contradicts. Six directories build TAGS independently (c/, d/, e/, m2/, system/, html-check-links/), m2/ additionally hand-rolls TAGS.doc for the Macaulay2Doc sources by piping find into a shell loop (m2/Makefile.in:43-47), and there is no etags --include anywhere in the tree nor any umbrella target. So the specific mechanism the file proposes is unused and 'better tag files for everything' does not exist. That the m2 TAGS rule was repaired recently, in #4233 for an argument-list-too-long failure, is evidence the target is maintained and used rather than vestigial |
| 1 | `bugs/dan/1-ambient` | [#4553](https://github.com/Macaulay2/M2/issues/4553) | &nbsp; | issue | not adopted: ambient ZZ and ambient QQ still fail with 'no ambient ring present' (rings.m2:53), so Bart Snapp's suggestion that ambient R return R for a non-quotient ring is unmet. Dan's own objection in the file -- that internal code walks ambient until it errors -- is the thing to check before changing it |
| 1 | `bugs/dan/1-application-dir-info-dir` | [#4554](https://github.com/Macaulay2/M2/issues/4554) | &nbsp; | issue | still not done: installPackage writes an .info file into the user's Application Support tree but never creates or updates the info dir index beside it. install-info appears only in cmake/packaging.cmake, for the deb and rpm packaging, and in configure.ac as a build-time requirement -- nothing on the installPackage path |
| 1 | `bugs/dan/1-building-doc-databases` | [#4555](https://github.com/Macaulay2/M2/issues/4555) | &nbsp; | issue | the file settles its own first half -- it says (FIXED) for the .installed problem. The second is unmet: installPackage touches .installed when a run completes (installPackage.m2:846-849) but nothing detects a missing doc database and rebuilds it, writable directory or not. Nearby but not the same: #1643 wants one documentation database per prefix instead of per package, for speed, and #776 wants the open database files bounded by an LRU cache -- neither rebuilds a missing one |
| 1 | `bugs/dan/1-caching-idea` | [#4558](https://github.com/Macaulay2/M2/issues/4558) | &nbsp; | issue | two live asks, one of them a verified documentation defect. The thread opens with Mike reporting that 'our description of how to remove a cached GB is no longer correct', and that description is still in the manual: ov_groebner_bases.m2:439 is an EXAMPLE block that says 'we must erase the memory of the Groebner basis computed above', runs remove(f.cache,{false,0}), then says 'compute the Groebner basis anew' and times it. The key is a GroebnerBasisOptions hash today, not {false,0}, so that remove is a silent no-op -- verified: keys (generators I).cache is unchanged across it and the gb is still cached -- which means the timing the manual displays for the Hilbert-hint computation is a cache hit rather than a fresh run. The two gbRemove copies at ov_rings.m2:1535-1536 carry the same stale key but sit inside -* *- blocks, so they are dead. The design half is unadopted and undecided: gb and res have no UseCache, Cache or CleanCache option, clearCache exists nowhere in Core (only as a BasicDivisor method in WeilDivisors), and Mike's getGroebnerKey never appeared -- though the key is at least inspectable now via keys (generators I).cache. Filed as one issue covering both halves on the maintainer's call |
| 1 | `bugs/dan/1-d-translator-crash` | [#4562](https://github.com/Macaulay2/M2/issues/4562) | &nbsp; | issue | reproduced, after first being parked as unverifiable. Copy any .d file to string.d and run scc1 on it: it fails with 'chk.c:1421: assertion failed' and exit 1. Controls with identical content under other names -- notstring.d, foo.d, strings.d -- produce ordinary 'symbol never defined nor declared' diagnostics and no assertion. So the filename alone decides it, which is the file's claim; the crash mode is an assertion rather than the 2009 segfault because this build has assertions enabled. Presumably scc1 derives a package name from the filename and collides with the built-in string type. Minimal reproducer, no rebuild needed, and scc1 bugs are actively tracked (#1197, #4525, #4542 open; #1134, #1420 closed), none of them this |
| 1 | `bugs/dan/1-debugger-and-empty-frames` | [#4560](https://github.com/Macaulay2/M2/issues/4560) | &nbsp; | issue | reproduces, and it is not merely counter-intuitive as the file says -- it contradicts the documentation. ov_debugging.m2:102-107 promises that after typing end, 'the debugger will be entered again ... at the point inside the function g from which the function f was called', and that listLocalSymbols will then show g's local variables. Side by side on 1.26.06: with the file's functions, which declare no locals, end returns straight to the top-level prompt and g is never entered. With the same call structure but a local variable in each function, end re-enters the debugger inside the caller and listLocalSymbols shows it. So the skip is specifically the empty-frame case, and the documented walk up the stack silently does not happen for any function that happens to declare no locals. Minimal reproducer is four lines. Related but distinct: 1-debugger-and-tail-recursion, where frames are elided rather than empty |
| 1 | `bugs/dan/1-directSum` | [#4563](https://github.com/Macaulay2/M2/issues/4563) | &nbsp; | issue | not installed: directSum(List, Function) has no method, so David's directSum(x, f) equivalent to directSum apply(x, f) still has to be written the long way. Filed rather than parked -- it is small, the intended semantics are stated unambiguously in one line, and it is the kind of self-contained addition that suits a first-time contributor. Several open directSum issues, none about this signature: #606 MutableHashTable, #2891 components and direct sums of matrices, #1060 degrees ignored |
| 1 | `bugs/dan/1-doc-hilbertSeries` | [#4564](https://github.com/Macaulay2/M2/issues/4564) | &nbsp; | issue | the specific documentation gap the file names is still there. All three operations work -- on monomialCurveIdeal(QQ[a..d],{1,3,4}), hilbertSeries returns a Divide, numerator gives 1-T^2-3T^3+4T^4-T^5, denominator gives (1-T)^4, and value denominator expands it to 1-4T+6T^2-4T^3+T^4 -- but help hilbertSeries mentions none of them. Its See also lists degreesRing, reduceHilbert, poincare, poincareN, hilbertPolynomial and hilbertFunction; the file's request is 'Need to add references to numerator, value denominator, etc.', and that is exactly what is missing. The prose does say the result is 'a type of expression called a Divide', which is the hint but not the pointer. Neighbouring open issues, neither of them this: #974, value of a Hilbert series failing because the parts live in a Laurent ring, and #1701 on hilbertSeries MonomialIdeal |
| 1 | `bugs/dan/1-doc-rowMult` | [#4565](https://github.com/Macaulay2/M2/issues/4565) | &nbsp; | issue | true as reported, and the file's suspicion that 'the other col and row functions might also' is confirmed. rowMult(m,i,a) returns the MutableMatrix it just modified, and help rowMult documents Usage, Inputs and Consequences but has no Outputs section -- the return value shows up only incidentally, as o3 in the example. Checked the whole family: rowMult, columnMult, rowAdd, columnAdd, rowSwap and columnSwap all return the MutableMatrix rather than null, and none of those seven doc nodes, rowPermute included, has an Outputs section. Small and closable: either document the return value or say in Consequences that the matrix is returned for convenience |
| 1 | `bugs/dan/1-document-options-error-msgs` | [#4566](https://github.com/Macaulay2/M2/issues/4566) | &nbsp; | issue | the html half was fixed in direct response to this file and then silently undone. f1c1dd78f1 (2007-12-23) moved all [symmetricAlgebra,Opt] keys into the Key list, which rendered because the node still had a top-level Usage; 9be2d70841 (2009-01-23) restructured it into three SYNOPSIS blocks and deleted that Usage. processUsage returns early without one (document.m2:545-548) and SYNOPSIS has no Options slot at all (:758-768), so help symmetricAlgebra has no "Optional inputs" section while help newRing lists all 19 with links. All 18 keys resolve; the page just does not show them. The same early return exempts SYNOPSIS-only nodes from the warning, so ask (a) fails open -- (a) itself is met generically by 24478f749a (document.m2:564 appends every unmentioned option, validate.m2:76-81 warns). #2516 is the same machinery from the other side, warnings for pass-through options |
| 1 | `bugs/dan/1-document-run` | [#4567](https://github.com/Macaulay2/M2/issues/4567) | &nbsp; | issue | half met and half contradicted. The return code is documented at ov_system.m2:855-867 including "2 for interrupt", by 42ade68981 (2014-08-26). The interrupt-ignoring half is not, and ov_system.m2:851-854 points the other way -- "signals ... will go both to it and to Macaulay2". run is libc system() (scclib.c:850-855), which SIG_IGNs SIGINT and SIGQUIT in the parent: run("kill -INT " \| toString processID()) returns 0 and M2 keeps going, so Ctrl-C kills only the command and run returns 2. #1082 asks to redesign what run and wait return and to stop shelling through /bin/sh, which would change what needs documenting |
| 1 | `bugs/dan/1-documentation-installPackage` | [#4568](https://github.com/Macaulay2/M2/issues/4568) | &nbsp; | issue | the parenthetical was built and the titular ask was not. CacheExampleOutput arrived in aa8a540a3d (2008-12-14), plausibly before the file, and stashes per node -- installPackage.m2:522-541 keys each file examples/<node key>.out with a hash guard at :527-531; Dan endorses exactly that split in #1924. The Usage line is still installPackage PackageName (package-doc.m2:448-452), only the case having drifted from PACKAGENAME. Dan's literal wording would narrow it, since lookup(installPackage,Package) is non-null and installPackage FirstPackage works, but the need is live and in-style: of 260 Usage lines in Macaulay2Doc/functions 248 are bare metavariables and the 12 with quotes all quote a placeholder precisely where quoting matters, export {"symbol1", ...} being the model |
| 1 | `bugs/dan/1-documentation-of-optargs` | [#4569](https://github.com/Macaulay2/M2/issues/4569) | &nbsp; | issue | half met by 4f82b03a94 (2020-08-06), twelve years on: the [f,Opt] key syntax is documented (document-doc.m2:149-153, ov_documentation.m2:176) and there is an "optional argument documentation template" node (:513-535). What is missing is how the type, default and description reach a page -- [document,Inputs] (:233-263) lists seven item forms and never uses the word option, so nothing says an Option-shaped item is pulled into the separate "Optional inputs" section or that omitted options are auto-listed; that is documented only for SimpleDoc (doc.txt:161-165, 3cb2096b13). And document-doc.m2:636 claims "the options are used just as with document", which is false: SYNOPSIS has no Options slot and without BaseFunction => f processSignature falls to document.m2:516, a bare Name => ... with no type, default or link. Not on the wiki either -- the style guide's "Use of optional arguments" is about code. #1466 is the next layer up, documenting an option's admissible values; #1220 is the closed issue whose fix delivered the half that is met |
| 1 | `bugs/dan/1-dot-dot-2` | [#4570](https://github.com/Macaulay2/M2/issues/4570) | &nbsp; | issue | reproduces cold and the order matters: X11 .. a errors in a fresh session and only works once vars(-11) has memoized into varIndices, so running the file's two lines in reverse reads as fixed. varName (indeterminates.m2:8-13) defines the whole scheme -- X<n> for negative indices, x<n> for indices at least 52 -- but reverseVars (:24-33) inverts only single letters, match("^[a-zA-Z]$",s) at :28, plus whatever a prior vars ZZ call registered. So vars generates names it cannot invert; vars(52..54) is (x0,x1,x2) and x0 .. x2 fails the same way. Fix is the two missing inverses, which cannot collide with the single-letter case since x alone is 23 and x0 is 52. Distinct from #4510 and #2020, which are about .. returning the wrong type rather than failing to resolve a name |
| 1 | `bugs/dan/1-elliptic-integrals` | [#4571](https://github.com/Macaulay2/M2/issues/4571) | &nbsp; | issue | neither check was done and the source says so. EllipticIntegrals.m2:380-381 still asserts a negative imaginary part for D.Period', byte-identical to the file, under a live -* actually, the paper has the complex conjugate of this period ... *- comment against Grayson's 1989 Archiv der Mathematik period cited at :15-18; two neighbouring assertions dodge the sign with abs (:320,:322). The real part drifted from .0p200 to 2.37e-66 in 28a631f318 (2026-05-19), a test repinned to new sqrt(CC) output rather than an answer, and below the noise floor at 200 bits. Branch cuts were never looked at -- git grep "branch cut" is empty tree-wide and E.log (:226) still picks roots by magnitude, quadnorm eqn' > quadnorm eqn at :239. The file's {* *} clause is settled: f2c87ac678 converted this very file and bc59b7532b made the old syntax a lexer error (lex.d:308-309). #4416 is the neighbouring agm question, about infinite arguments |
| 1 | `bugs/dan/1-emacs-macro-needed` | [Macaulay2/M2-emacs#102](https://github.com/Macaulay2/M2-emacs/issues/102) | &nbsp; | issue | unmet, and filed on Macaulay2/M2-emacs rather than here because that is where the code is -- git ls-files '*.el' is empty, M2.el having left in 78186879eb (2020-06-24) for the submodule at M2/Macaulay2/editors/emacs. f11 is M2-send-to-program (M2.el:67) reaching a verbatim buffer-substring insert (:474-504, :512-521); the only semicolon logic in the mode inserts one (M2-electric-semi, :65, :680-685), and fifteen sibling send-commands arrived without this one. The region branch of the same key covers it -- "or the region, if the mark is active", :525-534 -- but as C-a C-SPC C-e C-b f11, five keystrokes for a one-keystroke ask, so a workaround rather than the need being met. For whoever implements it: C-u f11 is taken, the prefix argument picks the target buffer (:470-486). filed as Macaulay2/M2-emacs#102; deliberately given no row in issue-titles.tsv so bin/file-issues cannot file it against Macaulay2/M2 |
| 1 | `bugs/dan/1-error-methods` | [#4572](https://github.com/Macaulay2/M2/issues/4572) | &nbsp; | issue | still true and measurably worse: the file's transcript has 102 entries, methods symbol = now has 154, of which exactly 4 are real assignment methods (RawMutableMatrix _ Sequence, MutableMatrix _ Sequence, IndexedVariableTable _ Thing, Symbol _ Thing). robust.m2:64-130 installs a Thing OP Thing method per flexible operator whose whole body builds the "no method for" text. One distinction the file conflates: 74 of the 154 are ((op,=),Type,Type) and ((op,=),Type) inheritance dispatchers from methods.m2:567-578, which do real work, so "stop installing them" is not available for those. Measured 150 of 154 already satisfy isUndocumented, yet documentableMethods (help.m2:143) returns all 154 -- isDocumentableMethod asks whether each key component is an exported type, never whether the key itself was marked undocumented', and Thing and Type are exported. So adding that test is a one-line partial mitigation, at the cost of a doc-database lookup per key. #1331 asks for more output, #3108 was fixed by filtering unexported types (10529388c6) which provably leaves every stub, #1604 is the inverse complaint |
| 1 | `bugs/dan/1-exponent-formatting` | [#4573](https://github.com/Macaulay2/M2/issues/4573) | &nbsp; | issue | reproduces byte for byte and the cause is one line. net Superscript (expressions.m2:774-779) raises the exponent by 1 + depth n, computed from the exponent's own net, so the offset ignores how tall the base is; the parenthesized quotient ring measures (height, depth) = (3,3), six lines with its baseline in the middle, and the exponent lands on row 2 of 6 exactly as the file's arrow marks. Raising by max(1 + depth n, height b) against the base net puts it above the top line, which is what the file asks for, and is byte-identical to today for a one-line base and for a matrix base -- checked, since the tempting height b - 1 breaks the ordinary case by dropping the exponent onto the baseline. Not a deliberate choice: git log -S 'n^(1+depth n)' returns only 647f7532b3, Initial revision. #3198 is about the parser's precedence for _ and ^, not where a superscript is drawn |
| 1 | `bugs/dan/1-finalize-files` | [#4577](https://github.com/Macaulay2/M2/issues/4577) | &nbsp; | issue | unmet on all three counts and data is genuinely lost, measured rather than read: after zzf << "hello"; zzf = null and two collectGarbage() calls the file is empty and openFiles() still lists it. At normal exit it does reach disk, via closem (d/stdio.d:575-583) registered with atend -- which flushes but does not close. Two blockers, and the second is the interesting one. There is no finalizer on the file type (git grep -i finaliz over d/stdio0.d d/stdio.d d/stdiop.d exits 1; every GC_REGISTER_FINALIZER in the tree is for foreign pointers, mysql, pthreads or engine objects). And openfiles (d/stdio.d:154-158) is a strong-reference list that addfile pushes every file onto and only rmfile removes, from cleanUp, after close -- so an unclosed file is reachable from a GC root and can never be collected, which is what openFiles() still listing it demonstrates. The two halves of the file are in tension: the flush-at-exit net that makes the common case work depends on that same pinning. #1515 is a hard second blocker, registerFinalizer broken since <=1.12 with #2728 on the same GC warnings and #1852 closed as its duplicate |
| 1 | `bugs/dan/1-flattenRing` | [#4578](https://github.com/Macaulay2/M2/issues/4578) | &nbsp; | issue | three asks, three destinations. (1) the opening question, what to do when the requested coefficient ring appears as (k/I)[a], is unmet and errors: flattenRing(zzB, CoefficientRing => zzA) with zzB = (zzA/ideal(s^3))[t] gives 'unable to flatten ring over given coefficient ring' (newring.m2:184) while CoefficientRing => QQ gives QQ[t,s]/s^3. There is a smaller repro of the same mechanism -- flattenRing(zzA, CoefficientRing => zzA) also errors -- because flattenRing Ring tests k === R at newring.m2:198 but the overriding methods at :221-225 and :243 test only the coefficient ring, so the self-reference falls through to unable(). Filing that one; Schubert2 calls the CoefficientRing form at five sites. (2) is met, and Dan's own '(or does it?)' is answered: degrees and degreeLength are preserved variable by variable (a {{1,0}}/{{0,1}} tower flattens to degrees {{0,1,0,0},{0,0,1,0}}, degreeLength 4 to 4) and homogeneity survives the map, by the 2008 degree-joining series 0126f8f60b..3f8ebb6ab1 plus 1da8ef003c and bf728fe883; matrix2.m2:181 now relies on it. (3) is verbatim still true -- degreeLength is never reduced and the order is the product order (monoids.m2:681-682) -- and its live symptom is #3887, whose reporter works around it with newRing(..., MonomialOrder => GRevLex, Degrees => {1,1}); commented there. A doc gap found on the way: flattenRing-doc.m2 has zero mentions of degrees, homogeneity or monomial order, which is what #651 should absorb |
| 1 | `bugs/dan/1-flattenRing-can-save-time` | [#4579](https://github.com/Macaulay2/M2/issues/4579) | &nbsp; | issue | both halves unmet, and the second is measurable without the first. Applying a ring map never short-circuits: ringmap.m2 contains no raw-ring comparison and RingMap RingElement (:169-175) always calls rawRingMapEval, so map(zzB,zzB) -- which satisfies zzid == 1, source === target and zzid === id_zzB, since ringmap.m2:31 computes exactly that predicate for comparison -- still costs 2.55ms per application on a 210-term polynomial and returns its argument. The first half is real but small: for a tower flattenRing builds a fresh raw ring rather than reusing R.FlatMonoid (raw zzS =!= raw zzR, zzR.FlatMonoid =!= monoid zzS) even though describe of the two monoids is character-identical, but caching from b25a44b6d1 (2008-10-24) already amortizes it -- 4.0ms on the first call for a 16-variable tower, 4.1us after. Filed as one issue keeping Dan's sequencing, reuse then short-circuit. Unsettled and worth a guard: whether reusing R.FlatMonoid is sound for Weyl algebras, Inverses, SkewCommutative or Join => false, which is what 'when appropriate' is doing in the file. #2133 is the adjacent caching issue |
| 1 | `bugs/dan/1-for-loop-idea.m2` | [#4580](https://github.com/Macaulay2/M2/issues/4580) | &nbsp; | issue | filed as one issue covering the whole for-bounds gap, with 1-for-sequence-idea and 1-for-by as duplicates of it. Bart Snapp's example still fails: for i from (0,0) to (5,6) gives 'expected an integer' while the form the file compares it to, for j in (0,0)..(5,6), iterates all nine pairs -- verified. evalForCode (d/evaluate.d:314) is the whole story. The in clause accepts a Sequence, a List, or any iterable through getIterator and getNextFunction (:331-341); the from and to clauses accept only a ZZcell passing isInt (:348-358), because j and n are C ints. And there is no step at all: forCode (d/parse.d:252) has inClause, fromClause, toClause, whenClause, listClause and doClause and no field for one, and "by" appears nowhere in the d sources as a keyword -- which is why 1-for-by's first example fails as 'no method for adjacent objects: 0 (of class ZZ) SPACE by (of class Symbol)' rather than as a for-loop error. So the loop body already handles what the bounds refuse, and both needs are reachable today through the in clause: for i in reverse(0..4) descends, for i in select(0..10, zzk -> zzk%2==0) steps, both verified. Filed on the same basis as #4557 and #4563, convenience over a working spelling. The file's own parenthetical, 'what about installing a top level method to handle that?', is worth quoting in the issue because the answer is no -- from and to are parsed, not dispatched, so no method can reach them. #4271 is the live neighbour, a nested-for syntax whose author offers to implement it |
| 1 | `bugs/dan/1-front-end-tests` | &nbsp; | &nbsp; | issue | Mike's list is met; Dan's two exceptions are not. The June 2006 predicates for the Hilbert-aided GB are now canUseHilbertHint (gb.m2:260-271) item for item -- degreeLength R === 1, poly ring or quotient of one, over a field, commutative or skew, every variable's first degree positive, matrix homogeneous -- landed 6e72d40d14 (2025-07-29) and renamed ef88222289, nineteen years on. Exception (a), a Hilbert hint for res, is still literally as Dan describes: options gb has Hilbert and options res does not, comp-res.hpp and comp-res.cpp contain no hilbert at all, and set_hilbert_function exists only on GB classes. Demonstrated by installing a deliberately wrong hint, poincare J = 1: gens gb J becomes the zero map, so gb used it, while betti res J is unchanged from the correct resolution. res is now a synonym for Complexes' freeResolution, and the front-end store exists (poincare M = p, hilbert.m2:110) with no resolution consumer. Exception (b) is unmet too: isHomogeneous has 12 methods and none takes more than one argument, so there is no way to ask about the first degree component alone; Complexes/FreeResolution.m2:215-380 substitutes heft R =!= null, which is weaker, and full multigraded isHomogeneous, and does not enforce Mike's 'algorithm 3 additionally singly graded'. Filed as one issue covering both exceptions. #4487 is adjacent to (b), #3937 is why canUseHilbertHint was hardened |
| 1 | `bugs/dan/1-further-libraries-and-applications` | &nbsp; | &nbsp; | &nbsp; | parked with the other wishlist files: one row holding seventeen independent library suggestions, so filing it whole would produce an issue nobody can close. Seven are done in a real sense -- cddlib, lrslib and 4ti2 bundled and genuinely used (4ti2 has an interface package and six downstream packages), polymake as an interface and deliberately never bundled (configure.ac:612-616 calls its build procedure flawed), gap over SCSCP with a separate install, and linbox and palp present but inert. matplotlib is reachable through the Python package. The other nine -- iml, jmol, polybori, m4ri, genus2reduction, symmetrica, porta, coin/or -- are absent and none ever had an M2 package. The remainder is a grab bag rather than one ask, spanning dense-F2 linear algebra, boolean-ring Groebner bases, combinatorics tables, an arithmetic-geometry calculator, a molecular viewer used as a plotter and an LP stack that glpk and fflas-ffpack settled; several are near-dead upstream. Two findings recorded rather than filed: palp is in PROGLIST (configure.ac:593) and built, yet git grep palp over packages/ returns nothing and configure.ac:1119 still reads 'dnl TODO: uncomment whne we require palp'; and linbox defaults to ENABLE_LINBOX=no (configure.ac:1454) with no HAVE_LINBOX anywhere under Macaulay2/, though configure.ac:623 calls it an option for experimentation |
| 1 | `bugs/dan/1-galois-fields` | &nbsp; | &nbsp; | issue | unmet, and the answer is cheaper than the file imagined -- reframed onto FLINT, since PARI left the tree in e79bd82855 (2025-03-07). findGalois (galois.m2:95-121) asks a hook, the only hook is ConwayPolynomials, and its data is FLINT's Conway table (ConwayPolynomials.m2:13, 'the data comes libflint', via a2fd44e9f0). Outside that table it falls to galois.m2:113, a brute-force search over dense random monic polynomials, then findPrimitive. Measured: conwayPolynomial(3,100) is null, GF(3,100) takes 6.5s then 4.8s and yields a 67-term modulus that differs on every call, while rawConwayPolynomial(3,100,true) -- FLINT's fq_nmod_ctx_init -- returns a modulus with 3 nonzero coefficients in 2.2ms. ConwayPolynomials.m2:16 hard-codes false, so GF never sees it. GF(3,582), the pari thread's own example, did not finish in 540s. Two aggravating details: the modulus is non-deterministic, so GF(3,100) in two sessions gives incompatible fields, and findPrimitive (galois.m2:118) is computed then discarded on the FlintBig path (galois.m2:174-181), i.e. wasted precisely in the slow cases. Coupled cost for whoever takes it: a non-Conway modulus makes map(GaloisField,GaloisField) error by design (ConwayPolynomials.m2:42), which is #2143. #1917 is adjacent, same galois.m2 block |
| 1 | `bugs/dan/1-gcd-over-separable-extensions` | &nbsp; | &nbsp; | issue | unmet, and instead of declining it gcd returns a common divisor that is not greatest. Over QQ[a]/(a^2-1), which is QQ x QQ, with e = (1+a)/2 and f = (1-a)/2 verified idempotent and orthogonal: for F = e*(x-1)+f*(x-2) and G = e*(x-1)+f*(x-3), d = e*(x-1)+f has degree 1 and divides both, yet gcd(F,G) is 1. My first example, (x^2-1)*(x-a) against (x^2-1)*(x+a), gave the right answer x^2-1 and was a coincidence -- both components share a gcd there. Cause identified and the capability already exists: gcd calls the two-argument rawGCD (factor.m2:26-28) with no minimal polynomial, so an extension in the coefficient ring is invisible and only the base-field part comes back. Over K = QQ[a]/(a^6-a^3-1) with R = K[t], F = t^6-t^3-1 and G = (t-a)*(t+a), t-a divides both and the front end says 1, while rawGCD(raw F, raw G, raw (ideal K)_0) returns t-a. That is Mike's 2009 diagnosis verbatim, bugs/mike/0-gcd-doc.m2 line 3, 'rawGCDRingElement is not being called from the front end'. Also: the guard Dan proposed in #321 -- 'just give an error if the ring is a quotient ring' -- exists at factor.m2:24 but tests only the top ring, so gcd errors in QQ[a]/(a^2-1) and proceeds in (QQ[a]/(a^2-1))[x], and likewise for pzinn's own ZZ[y]/(y^2+3) example. Third guard-depth defect of this cohort after #4576 and #4578. bugs/mike/0-gcd-doc.m2 is left todo on purpose: it is a 120-line multi-ask file covering factor over toField ('NOT CORRECT!!'), bivariate gcd and missing documentation, so it must be read against this issue rather than settled as its duplicate. #1958 is the units-in-a-field case Mike called expected |

## Already tracked by an open issue -- `duplicate` (34)

| prio | file | issue | fix | disposition | note |
| ---: | --- | --- | --- | --- | --- |
| 0 | `bugs/dan/0-degrees-of-maps` | [#607](https://github.com/Macaulay2/M2/issues/607) [#1060](https://github.com/Macaulay2/M2/issues/1060) | &nbsp; | drop | duplicate, and it spans two open issues nobody has connected: #607 is \|\| and \| , #1060 is ++, and #736 is \| again. Still reproduces -- map(Q^1,Q^1,{{t_1}},Degree=>{1}) has degree {1} and is homogeneous, f*f correctly gets {2}, but f\|\|f, f\|f and f++f all come back degree {0} and inhomogeneous. The file's own transcript no longer exercises it, since degree(vars Q * id_(Q^2)) is {0} now rather than {1} |
| 0 | `bugs/dan/0-generateAssertions` | [#3413](https://github.com/Macaulay2/M2/issues/3413) | &nbsp; | drop | semicolon variant of #3413; still emits assert( (4;) === 4 ), which fails when run |
| 0 | `bugs/dan/0-getting-one-element-of-a-mutable-hashtable` | [#4231](https://github.com/Macaulay2/M2/issues/4231) | &nbsp; | drop | still reproduces; #4231 asks the same question and its author could not find this discussion |
| 0 | `bugs/dan/0-monoid-design-problem` | [#2905](https://github.com/Macaulay2/M2/issues/2905) | &nbsp; | drop | the design statement of #2905's defect: monoids.m2:684 takes Join from the first argument alone, as it takes DegreeRank and DegreeMap. So the associativity the file says was arranged does not hold -- tensor(M,N) with N carrying Join=>false gives Join=null and degreeLength 2, while tensor(N,M) gives false and 1. The file also names why it is hard: a DegreeMap on N is a map out of M's degree monoid, and M is not known when N is made |
| 0 | `bugs/dan/0-mutable-lists` | [#659](https://github.com/Macaulay2/M2/issues/659) | &nbsp; | drop | duplicate of #659, open since 2015: mutable lists grow one element at a time, which is why filling one by index is O(n squared). Still reproduces -- n=1000 takes .0013 s and n=10000 takes .3664 s, about 280 times the work for ten times the elements. Filed as #4501 before #659 was found, since searching 'mutablelist' as one word does not match a title reading 'growth of mutable lists'; #4501 closed as a duplicate and the timings went to #659 as a comment. #1608 is a larger redesign of the same type and stays separate |
| 0 | `bugs/dan/0-polymake` | [#457](https://github.com/Macaulay2/M2/issues/457) | &nbsp; | drop | #457 asks for exactly this -- polymake as a distribution prerequisite, with the same 'no way to build it' caveat |
| 0 | `bugs/dan/0-runLengthEncoding` | [#4393](https://github.com/Macaulay2/M2/issues/4393) | &nbsp; | drop | same greedy heuristic as #4393: runLengthEncode0 commits to duplicate-or-successor on the second element and never reconsiders (indeterminates.m2:82-91), with no minimum length before emitting a range, so {7,2,5,6} still gives {7, 2, 5..6} |
| 0 | `bugs/dan/0-settable-memory-limits` | [#519](https://github.com/Macaulay2/M2/issues/519) | &nbsp; | drop | the ask is #519's, closed in 2024 on argumentMode = defaultMode - SetUlimit; the reporter's own failure is separately moot for the main test suite, where 8ef9f80447 commented ulimit -v out. Worth noting the numbers have drifted far past the file: M2 now reserves about 2.2 GB of virtual address space at startup for 124 MB resident, so every -v value still written in the makefiles is below what it needs |
| 0 | `bugs/dan/0-sort-doc` | [#101](https://github.com/Macaulay2/M2/issues/101) | &nbsp; | drop | duplicate of #101, open since 2014: sort({3,1,2}, MonomialOrder => Descending) returns {1,2,3} because sort List := opts -> internalsort (lists.m2:216) binds opts and drops them. Filed as #4529 before #101 was found -- the capped issue search hid it -- so #4529 was closed as a duplicate and the documentation angle went to #101 as a comment |
| 0 | `bugs/dan/0-task-examples` | [#1147](https://github.com/Macaulay2/M2/issues/1147) | &nbsp; | drop | the examples Dan removed are still gone -- the (addCancelTask,Task,Task) node in ov_threads.m2:158 has no Example block -- which is what #1147 is reaching for when it says 'I believe we used to have something like this'. His guess at the cause was right: #2021 is the same failure on armhf ten years later, diagnosed as a timing race in example capture and closed by lengthening a sleep. The example runs correctly today but still prints an interleaved 'error: interrupted' backtrace from the cancelled task, which is the garbling. #1146 is the underlying instability |
| 0 | `bugs/dan/0-tensor-rings` | [#2905](https://github.com/Macaulay2/M2/issues/2905) | &nbsp; | drop | same line as #2905: with Join => false, monoids.m2:700 sets DegreeRank from the first argument's monoid alone, which collapses degreeLength tensor(R,R,Join=>false) to 1 for R = QQ[x][y] and produces #2905's 'length at most 1' error |
| 0 | `bugs/dan/0-tensor-rings-2` | [#2905](https://github.com/Macaulay2/M2/issues/2905) | &nbsp; | drop | third instance of #2905, and the one that shows it in the wild: tensor(RB, RA, Join => false) on flattened quotient rings over frac(QQ[t]) still dies at monoids.m2:655 with 'expected degree map to return a list of length at most 2'. Here both arguments have degreeLength 2, so it is not a rank mismatch like #2905's own example -- the DegreeMap itself returns too much, which is what 0-monoid-design-problem says goes wrong when a map declared on one monoid is applied to another. The three files interlock: symptom, cause, and a real failure |
| 0 | `bugs/dan/0-ulimit` | [#519](https://github.com/Macaulay2/M2/issues/519) | &nbsp; | drop | #519 asks for the same thing and names the same two places; closed in 2024 on argumentMode = defaultMode - SetUlimit, and the Makefile values are ?= overridable, though neither half comes from configure |
| 0 | `bugs/dan/0-value-symbol` | [#4520](https://github.com/Macaulay2/M2/issues/4520) | &nbsp; | drop | same defect as #4520 reached through value rather than parse: the quote keywords eat the implicit EOF token, so value "symbol" hands back that token as a Keyword; also value "local" and value "global" |
| 0 | `bugs/mike/0-frac-bug` | [#4576](https://github.com/Macaulay2/M2/issues/4576) | &nbsp; | drop | duplicate of #4576, which was filed for the cause while this row was still todo -- triaged out of turn because verifying bugs/dan/1-frac-tower turned it up. The literal transcript no longer reproduces: product l is now 1, not (10t^3-5t^2+t)/t, so the nilpotent denominator the file points at in o5 is gone. The cause is not. frac documents its argument as an integral domain and does not check, because factoryAlmostGood (enginering.m2:335-343) recurses to the bottom of the chain and tests only QQ, ZZ or a GaloisField, never whether a quotient's ideal is prime. In this file's own ring, frac(ZZ[t]/(t^4)): t =!= 0, t^4 == 0, t*(1/t) == 1, and yet (t^4)*(1/t) == 0 while t^3*(t*(1/t)) == t^3 -- the same product grouped two ways. Do not settle this fixed on the symptom |
| 0.1 | `bugs/dan/0.1-ulimits` | [#519](https://github.com/Macaulay2/M2/issues/519) | &nbsp; | drop | the same ask as 0-ulimit and 0-settable-memory-limits, and #519 names the same hardcoded line: it has moved from m2/html.m2 to m2/run.m2:13, still 'utest -t ... \| utest -m ...' with the values written in. #519 closed in 2024 on argumentMode = defaultMode - SetUlimit, which turns them off but does not make them settable from configure, make or installPackage as this file asks |
| 0.5 | `bugs/dan/0.5-network-check` | [#4527](https://github.com/Macaulay2/M2/issues/4527) | &nbsp; | drop | same ask as bugs/dan/0-SCSCP-checks, which was filed as #4527: automate the SCSCP round trip and give it a separate insecure make target. Two bug files, one issue |
| 0.9 | `bugs/dan/0.9-globalAssignment` | [#1056](https://github.com/Macaulay2/M2/issues/1056) | &nbsp; | drop | still true and already tracked: GlobalAssignHook is absent from both HashTable and MutableHashTable, and a named MutableHashTable prints as MutableHashTable{} rather than its name. #1056 is the same report and quotes the same code(net,HashTable) against code(net,MutableHashTable) comparison this file does |
| 1 | `bugs/dan/1-M2-symlinks` | [#715](https://github.com/Macaulay2/M2/issues/715) | &nbsp; | drop | settled by commenting on #715 rather than filing, because the two halves belong together. #715, open since 2017, reports the wrapper half: M2 execs `dirname "$0"`/M2-binary (bin/M2.in:9, and bin/CMakeLists.txt:106 for cmake), so any single symlink to the installed M2 dies with 'M2-binary: not found'. The file's own claim is the half underneath, and it is not in #715: initcurrentlayout follows exactly one link and never iterates (startup.m2:325), so reaching M2-binary through a chain of two leaves currentLayout null and startup.m2:351 dereferences it -- one link works and prints prefixDirectory /usr/, two fail to start. Dan wrote 'chain' and meant it. The reason to comment rather than file: the patch attached to #715 uses $(dirname $(readlink "$0")), a single-level resolve, so it would fix the wrapper and leave the chain broken; readlink -f settles both. That is worth more to #715 than a second issue beside it. Not covered elsewhere -- initcurrentlayout matches no issue, and Layout# matches only #54, a closed suffix bug in the same function |
| 1 | `bugs/dan/1-copyFile` | [#419](https://github.com/Macaulay2/M2/issues/419) | &nbsp; | drop | the same defect one level up is already open as #419, 'copyDirectory fails on read-only files', whose own suggestion is 'perhaps add an option to copyDirectory so it forcibly replaces read-only files' -- and that option would have to live in copyFile, since copyDirectory takes options copyFile ++ ... (files.m2:112) and delegates to copyFile for every file it copies (:131). Verified independently: copyFile onto a 0444 target fails and leaves the old contents, and copyFile's options are only UpdateOnly and Verbose. The consequence this file predicts is also already on record as #229, 'read-only files and installPackage', closed as stale rather than fixed. Settled by commenting on #419 rather than filing a third |
| 1 | `bugs/dan/1-debugger-and-tail-recursion` | [#3546](https://github.com/Macaulay2/M2/issues/3546) | &nbsp; | drop | #3546, 'Incorrect location when error occurs in the tail call', is the live manifestation of this and is labelled Interpreter. It reports that after tail call optimization the error position names the outer call rather than the expression that failed, and it records history the file could not: @pzinn fixed it in PR #3144, which had to be reverted because of #3417. This file's ask -- a way of preventing tail recursion when debugging -- is one candidate fix for exactly that, so it belongs on #3546 rather than beside it. The mechanism is unconditional today: evaluate.d does tail recursion at :519, :649 and :676 with the intent spelled out in comments, consulting no debugging flag, and :769 skips it only for 21-argument calls |
| 1 | `bugs/dan/1-dim` | [#3557](https://github.com/Macaulay2/M2/issues/3557) | &nbsp; | drop | same root cause as #3557, 'rank fails over ZZ because codim needs Generic => true', by way of a different entry point. Re-checked case by case rather than in a batch, and the four cases do not fail the same way: ideal(2) alone is an ideal of ZZ, so A/ideal(2) errors 'expected ideal of the same ring' at the quotient, which is arguable rather than wrong; but ideal(2,x), ideal(two) and ideal(two,x) are all ideals of A, those quotients build fine, and every dim on them fails with the identical message 'codim: expected an affine ring (consider Generic=>true to work over QQ)'. That is #3557's message verbatim -- codim refuses a non-affine ring and nothing can pass Generic => true through. So the answers Bart Snapp wanted (A/2 is (ZZ/2)[x], dimension 1) are blocked by the same plumbing that blocks rank. Neighbours over ZZ, none of them this: #4095 trim, #1518 kernels of ring maps. Settled by commenting on #3557 |
| 1 | `bugs/dan/1-dispatch` | [#1477](https://github.com/Macaulay2/M2/issues/1477) | &nbsp; | drop | the same puzzle as #1477, 'Dispatching of lift and promote', which asks why lift's method is installed the way it is, observes that Ring is not among RingElement's ancestors so the dispatch is hard to account for, and concludes 'the documentation is rather thin in this area'. This file is the user-facing half of that: three plausible lookup spellings and no way to tell which is right. Measured, the answer is worse than unclear -- help lookup describes lookup(M,A,B) as taking classes, but lookup(lift, Ideal, Ring) returns null, as does lookup(lift, ideal x^2, Ring), while lookup(lift, Ideal, R) with the actual ring returns the FunctionClosure at matrix2.m2:766. So the spelling that works is the one mixing a class with an object, and the documented convention does not describe it. Settled by commenting on #1477 rather than filing beside it. Distinct from #1531, which is lookup returning null for a Command |
| 1 | `bugs/dan/1-doc-headlines` | [#2372](https://github.com/Macaulay2/M2/issues/2372) | &nbsp; | drop | the same question as #2372, 'hypertext objects in documentation headlines', reached from the opposite side. The state is unchanged: document.m2:602 reads 'if instance(val, String) then fixup val else error("expected ", toString key, " option to be a string")', so a Headline must be a String, and the file's second clause is equally unmet -- SimpleDoc.m2:81 routes Headline through singleString. But #2372 is an open Documentation issue reporting a headline that did contain hypertext and rendered badly (Polyhedra's linSpace), and its proposal is the reverse of this file's: 'perhaps this should raise an error'. One of the two has to win, so this belongs on that thread rather than beside it as a competing request. Settled by commenting |
| 1 | `bugs/dan/1-document-function-value-assignment` | [#1980](https://github.com/Macaulay2/M2/issues/1980) | &nbsp; | drop | already tracked as #1980, 'fix, document, or remove storefuns', which is open, labelled Documentation, and links the exact methods.m2 code this file quotes along with its one real use, implanting a resolution in res.m2. #1980 states the three options in its title and adds a worked example of the feature misbehaving -- res M = 5 followed by res M returning 5 for a module -- which covers this file's closing remark that 'the code for resolution storage should check the classes of M and C'. The file's own framing, that a new documentation key type is needed to document assignment to functions, is the 'document' branch of that issue |
| 1 | `bugs/dan/1-dot-dot` | [#4510](https://github.com/Macaulay2/M2/issues/4510) | &nbsp; | drop | duplicate of #4510, which this catalog filed out of 0-indexed-variables -- same code path, X .. X at dotdot.m2:12-19, different spelling of the endpoints. The reported symptom is pre-commit: 92e9ca107e (2009-10-29, three months after the file) rewrote .. and added the guard that turns the wrong-ring answer into the IndexedVariable fallback, so ideal(x_1_R .. x_3_R) now errors at matrix1.m2:557 rather than silently building an ideal of S. Two things #4510 does not say: tests/normal/dot-dot.m2:128 asserts the fallback for a shadowed ring, and #2020 already settled the design question against fixing it -- pzinn implemented it (6116033703, 2340541e45), found it reinterpreted R=QQ[m_(1,1)..m_(2,2)] against the previous ring, and closed it on Dan's "let's be conservative here and not break code". Mahrud's 2024 promote-at-the-end suggestion there is unanswered. Comment posted rather than a new issue |
| 1 | `bugs/dan/1-double-semicolon` | [#4067](https://github.com/Macaulay2/M2/issues/4067) | &nbsp; | drop | close but not the same as #4067, which asks why ;; is a syntax error; this file asks what it should mean -- no output value recorded and no line-number increment. Both start from the same error, still live, but answering #4067 by making ;; a harmless empty statement would foreclose the proposal, so the design note goes on the issue as a comment. The need is already met and documented: lineNumber is assignable (ov_repl.m2:443-447), so 2+2; lineNumber = lineNumber - 2; makes the next prompt reuse i2. The other half is not -- ; suppresses printing but still records the value, and o1 is 22 after 11+11; |
| 1 | `bugs/dan/1-for-by` | [#4580](https://github.com/Macaulay2/M2/issues/4580) | &nbsp; | drop | merged into #4580, filed from 1-for-loop-idea.m2, which covers the whole for-bounds gap. Both of this file's examples reproduce verbatim: for i from 4 to 0 by -1 fails at the grammar level, since forCode (d/parse.d:252) has no field for a step and "by" is not a keyword in the d sources, and for i from x to x+10*y by y fails as 'expected an integer' from d/evaluate.d:348-358, which is the same half the other two files report. The need is reachable through the in clause -- for i in reverse(0..4) and for i in select(0..10, zzk -> zzk%2==0), both verified -- so this is syntax rather than capability. The third ask, 'parallel for', is too vague to file and is not in the issue |
| 1 | `bugs/dan/1-for-sequence-idea` | [#4580](https://github.com/Macaulay2/M2/issues/4580) | &nbsp; | drop | merged into #4580, filed from 1-for-loop-idea.m2, the same ask from a second reporter: for i from (1,1,1,1,1,1) to (1,2,3,4,5,6) gives 'expected an integer' because the from and to clauses take only a small integer (d/evaluate.d:348-358) while the in clause takes any iterable. Four lines each and they say the same thing, so one fix closes both |
| 1 | `bugs/dan/1-getWWW` | [#4179](https://github.com/Macaulay2/M2/issues/4179) | &nbsp; | drop | duplicate of #4179, which owns the behaviour that would explain it. The file is one dead 2009 URL, http://online.wsj.com/xml/rss/3_7041.xml, and the words 'This doesn't work', so the original cause cannot be verified -- but getWWW returns the raw response including the status line and never parses it, which is #4179's first point. Verified: getWWW 'http://macaulay2.com/' returns 1174 characters whose first line is 'HTTP/1.1 301 Moved Permanently', and a missing page returns 8566 characters beginning 'HTTP/1.1 404 Not Found', both with no error. So a URL that redirected -- which a 2009 http:// news feed almost certainly did -- yields a redirect response and no feed, indistinguishable from success. getWWW is hand-rolled HTTP/1.1 over a socket (http.m2:5-70) with Connection: close. #4179 also asks for https and file:// support; #663 was absorbed into it |
| &nbsp; | `bugs/anton/MISC/standardPairs.m2` | [#114](https://github.com/Macaulay2/M2/issues/114) | &nbsp; | drop | still reproduces: same non-disjoint cones the issue shows, 1 in three sets; #4492 is a draft converted in error, closed |
| &nbsp; | `bugs/mike/git-issue-568-569.m2` | [#568](https://github.com/Macaulay2/M2/issues/568) [#569](https://github.com/Macaulay2/M2/issues/569) | &nbsp; | drop | both issues are still open |
| &nbsp; | `bugs/mike/git-issue291.m2` | [#291](https://github.com/Macaulay2/M2/issues/291) | &nbsp; | drop | #291 is still open |
| &nbsp; | `bugs/mike/git-issue604.m2` | [#604](https://github.com/Macaulay2/M2/issues/604) | &nbsp; | drop | #604 is still open |

## Fixed -- `fixed` (132)

| prio | file | issue | fix | disposition | note |
| ---: | --- | --- | --- | --- | --- |
| 0 | `bugs/dan/0-bugs-caviglia.m2` | &nbsp; | &nbsp; | drop | topComponents on a module works now: topComponents(R^1/I) returns a real cokernel matching topComponents I, not the cokernel of the identity |
| 0 | `bugs/dan/0-cancelTask` | &nbsp; | &nbsp; | drop | cancelTask now takes effect: the task reads <<task, canceled>> within a second, not stuck at 'cancellation requested' |
| 0 | `bugs/dan/0-chi-doc` | &nbsp; | [`1b4bbe4494`](https://github.com/Macaulay2/M2/commit/1b4bbe4494) | drop | chi is documented in Macaulay2Doc/shared.m2 |
| 0 | `bugs/dan/0-decompose.m2` | &nbsp; | &nbsp; | drop | both the claim and the suspected mechanism are gone. decompose I on the 2-variable jacobian ideal returns 7 components and every one contains I, so the assertion at the end of the file passes; and the division identity j == h*(j//h) + (j%h) now holds with the saturate cache intact, where the file had to clear the cached GroebnerBasisOptions to make it work. Its autorun=fail is pure API drift, as the README warns -- running the assertion directly is what settles it. Those two assertions would make a good regression test in tests/normal if anyone wants one later; nothing has been added |
| 0 | `bugs/dan/0-dependent-packages` | [#4528](https://github.com/Macaulay2/M2/issues/4528) | [`0d2d059660`](https://github.com/Macaulay2/M2/commit/0d2d059660) | drop | closed as fixed in #4528: FILE_PREREQS (0d2d059660, 2020) did it more automatically than the file proposed, accumulating command -v paths for the external programs configure finds, and drove runtime dependencies for the .deb and .rpm builds; that use has since been scrapped for hardcoded dependencies per build system, leaving distributions/freebsd/Makefile.in:60 the only reader. The second ask, dropping hard-coded paths from packages, was met separately by findProgram (#1389) |
| 0 | `bugs/dan/0-disabling-threads` | &nbsp; | &nbsp; | drop | answered at runtime instead: M2 --no-threads skips initializeThreadSupervisor entirely (bin/main.cpp:101), and GC_NPROCS controls gc's thread count -- both stronger than the configure-time --disable-pthreads the file asks for, since neither needs a rebuild |
| 0 | `bugs/dan/0-doc-ideal-syntax` | &nbsp; | &nbsp; | drop | both forms documented: (ideal,Sequence) and (symbol /,Ring,Sequence) |
| 0 | `bugs/dan/0-doc-subquotient-module-maps` | &nbsp; | &nbsp; | drop | (map,Module,Module,Matrix) now documents the correspondence: M and N must have the same number of generators as target p and source p |
| 0 | `bugs/dan/0-doc-writing-code` | &nbsp; | &nbsp; | drop | documented in the Package Writing Style Guide wiki, under 'Order of arguments': "The argument upon which the function mainly acts should go last." https://github.com/Macaulay2/M2/wiki/Package-Writing-Style-Guide -- the wiki is not in git, so there is no commit to cite |
| 0 | `bugs/dan/0-document-Weyl-homogenizer` | &nbsp; | [#2600](https://github.com/Macaulay2/M2/issues/2600) | drop | the homogenizing variable is documented under monoid, with a worked example; dc6f993616 added that paragraph |
| 0 | `bugs/dan/0-document-local-scopes` | &nbsp; | [`fb2b4c6e3f`](https://github.com/Macaulay2/M2/commit/fb2b4c6e3f) | drop | both halves written down within a week of the request: fb2b4c6e3f added the Caveat on the for node -- 'new local variables defined inside the body of the loop will not be visible outside it' -- and value String documents that a string is parsed in its own scope, with an example showing a global a unaffected |
| 0 | `bugs/dan/0-final-check-interactive-input-behaviour` | [#813](https://github.com/Macaulay2/M2/issues/813) [#820](https://github.com/Macaulay2/M2/issues/820) | [`bcdebcded5`](https://github.com/Macaulay2/M2/commit/bcdebcded5) | drop | all four sections check out, tested under a pty with expect rather than piped stdin. Its named problem -- readline not discarding partial lines after SIGINT -- is #813, closed: ///asdf, "asdf, 1 + and -* each followed by CTRL-C all return to a usable prompt and evaluate the next expression. Blank lines inside multi-line input are tolerated, which is #820, closed, and #820 says it became possible once #813 landed. Multiple statements per line match the file's transcript and parsing recovers. The {* *} cases are moot since block comments are -* *-. Piped stdin does NOT recover after a syntax error, which looked like a defect until the same input under a pty recovered fine |
| 0 | `bugs/dan/0-gentoo-required-programs` | &nbsp; | &nbsp; | drop | configure takes --with-unbuilt-programs and M2 locates them at runtime via findProgram/programPaths |
| 0 | `bugs/dan/0-getWWW` | &nbsp; | [`c9185f564c`](https://github.com/Macaulay2/M2/commit/c9185f564c) | drop | splitWWW unchunks the body when the response is chunked; added in 1.9.1 |
| 0 | `bugs/dan/0-help-bug` | &nbsp; | [#3272](https://github.com/Macaulay2/M2/issues/3272) | drop | the command list no longer carries the leading asterisks that made it look pasteable; 75bb7260c6 replaced the UL with M2CODE |
| 0 | `bugs/dan/0-help-usage-multiple-lines` | &nbsp; | &nbsp; | drop | the synopsis now renders Usage on its own indented lines |
| 0 | `bugs/dan/0-highlighting` | &nbsp; | &nbsp; | drop | package names are in M2-symbols.el, in both the completion table and the highlighting regexp; #3330 is the adjacent open problem of highlighting symbols exported by packages |
| 0 | `bugs/dan/0-html-references` | &nbsp; | [`1cccd53054`](https://github.com/Macaulay2/M2/commit/1cccd53054) | drop | settled by the 1.12 layout rework: AbsoluteLinks was removed for logic that produces relative links only between files installed under the same prefix, the layout was simplified, and each prefixPath entry now carries a layout detected at runtime -- which is the currentLayout versus installationLayout question the file opens with. Installing with SeparateExec => false yields no ../common references and no file:// links |
| 0 | `bugs/dan/0-installPackage` | &nbsp; | [#1454](https://github.com/Macaulay2/M2/issues/1454) | drop | installPackage.m2:704 now refuses outright: "the package is already installed and loaded from ..." when the source directory is the install prefix's packages directory; the looser complaint in #1032, sources taken from some other installed prefix, is untouched and still open |
| 0 | `bugs/dan/0-isWellDefined` | &nbsp; | [`233422068a`](https://github.com/Macaulay2/M2/commit/233422068a) | drop | the patch in the file was applied: newring.m2 now calls flattenRing(R,Result=>3) |
| 0 | `bugs/dan/0-joint-package-documentation` | &nbsp; | [#1454](https://github.com/Macaulay2/M2/issues/1454) | drop | all of it landed via #1459: newPackage takes Keywords, defaulting to {"Uncategorized"} -- the Section option with an Other bucket that Mike described -- and it drives 'packages provided with Macaulay2', a page grouped by subject with refereed packages starred; every distributed package also has its own installed doc tree |
| 0 | `bugs/dan/0-k-basis` | &nbsp; | &nbsp; | drop | the work landed: rawBasis over a tower returns exactly what the file annotates as wanted -- 'y2 yz z2' and 'y2b yzb z2a z2b' -- and basis_2 S agrees |
| 0 | `bugs/dan/0-ker-RingMap` | &nbsp; | &nbsp; | drop | both cases now return the unit ideal: ker map(R/1_R,R) is ideal 1, as is ker map(T/ideal 1_T,S) |
| 0 | `bugs/dan/0-loading-documentation` | &nbsp; | [`de6a0e20e8`](https://github.com/Macaulay2/M2/commit/de6a0e20e8) | drop | the failure cannot recur: it was Macaulay2Doc aborting because NormalToricVarieties used PackageImports and PackageExports before they were exported, and both are now in exports.m2:318-319. html ofClass {ScriptedFunctor} returns its span with no package-loading cascade |
| 0 | `bugs/dan/0-more-run-length-encoding` | &nbsp; | &nbsp; | drop | describe now prints QQ[t_1..t_10, ...] instead of listing every variable |
| 0 | `bugs/dan/0-negative-dimension` | &nbsp; | &nbsp; | drop | dim I' is 1 and both asserts pass; worth promoting to tests/normal |
| 0 | `bugs/dan/0-options-PolynomialRing` | &nbsp; | &nbsp; | drop | the inconsistency Mike reported is gone: heft B is {1,1} for B = A[c,d], matching the degree length, so B^{1} erroring is now correct rather than surprising |
| 0 | `bugs/dan/0-overview-doc` | &nbsp; | [#3264](https://github.com/Macaulay2/M2/issues/3264) | drop | Macaulay2/m2/overview.m2 was removed as unused |
| 0 | `bugs/dan/0-parallel-assignment` | &nbsp; | &nbsp; | drop | (b) = 1:x now leaves b equal to x, instead of the one-element sequence |
| 0 | `bugs/dan/0-parallel-documentation` | &nbsp; | &nbsp; | drop | the package and documentation install runs under make -j; CI does exactly that in test_build.yml, 'make -j$(nproc)' |
| 0 | `bugs/dan/0-possible-memory-leak` | &nbsp; | &nbsp; | drop | no longer leaks: the dimension 6 loop that died with 'out of memory trying to allocate 16 bytes' now runs to completion, and VmPeak afterwards is 2285744 kB against 2277168 kB for bare startup, so the twenty-one varieties add about 8 MB of peak address space between them |
| 0 | `bugs/dan/0-pruningMap` | &nbsp; | [`d25d385134`](https://github.com/Macaulay2/M2/commit/d25d385134) | drop | the report was right and the doc was corrected: it now says the isomorphism goes from N to M |
| 0 | `bugs/dan/0-pushForward` | &nbsp; | [#2113](https://github.com/Macaulay2/M2/issues/2113) | drop | pushForward over an inhomogeneous map returns a subquotient; the error string was removed when pushforward moved to its own file |
| 0 | `bugs/dan/0-radical-crash` | &nbsp; | &nbsp; | drop | radical J returns instead of a SIGSEGV; worth promoting to tests/normal |
| 0 | `bugs/dan/0-ringmap-assertion-failure` | &nbsp; | [#2113](https://github.com/Macaulay2/M2/issues/2113) | drop | kernel returns instead of tripping the degreesRing assert, which the same commit deleted from matrix3.m2 |
| 0 | `bugs/dan/0-segfault` | &nbsp; | [#1190](https://github.com/Macaulay2/M2/issues/1190) | drop | the slow part is gone: the 2009 handler hand-unrolled 21 __builtin_return_address frames and provoked further SIGSEGVs caught by siglongjmp; ebc0aa3bcb replaced it with boost::stacktrace, and the whole crash now costs about a second, spent symbolizing |
| 0 | `bugs/dan/0-separate-tab` | &nbsp; | [#1425](https://github.com/Macaulay2/M2/issues/1425) | drop | tabs survive: ascii \ separate("a\tb\nc") gives {{97, 9, 98}, {99}}, not the eight 32s reported, and lines agrees; 99883f8b5e made separate String delegate to separate("\r?\n", str), which is the two-argument form the file identified as the workaround |
| 0 | `bugs/dan/0-simpleDoc` | &nbsp; | [`1c9ec4d961`](https://github.com/Macaulay2/M2/commit/1c9ec4d961) | drop | Subnodes has been a SimpleDoc node key since 2009 (NodeFunctions in SimpleDoc.m2:94); #1000 later extended it to Subnode within Description |
| 0 | `bugs/dan/0-sqrt-error-message` | &nbsp; | [#3318](https://github.com/Macaulay2/M2/issues/3318) | drop | sqrt ii works and agrees with sqrt(+ii); ii is still a Constant, but #3318 made Constant a subclass of Number, so the sqrt @@ numeric variant from typicalvalues.m2:155 reaches it |
| 0 | `bugs/dan/0-stdinc-frobby` | &nbsp; | [#3338](https://github.com/Macaulay2/M2/issues/3338) | drop | configure.ac:1173 falls back to -I/usr/include/frobby when frobby.h is not on the default include path, which is what version.dd:37 needs |
| 0 | `bugs/dan/0-synonyms-and-collisions` | &nbsp; | &nbsp; | drop | ask (2) is done and covers the need: loadPackage now prints 'symbol "Schubert2" in PackageDictionary is shadowed by a symbol in User#"private dictionary"' and names the Package$Schubert2 synonym, so the silent confusion is gone. Ask (1) was not done -- PackageDictionary is still last on dictionaryPath -- and reordering lookup globally is a bigger change than the warning that replaced it |
| 0 | `bugs/dan/0-temporaryFilename` | &nbsp; | [`b70dbb05ef`](https://github.com/Macaulay2/M2/commit/b70dbb05ef) | drop | b70dbb05ef answered every complaint: temporaryFileName now returns paths inside a per-session directory (/tmp/M2-PID-N/0, /1, ...) created by temporaryDirectory, which loops on 'try mkdir fn else continue' -- mkdir being atomic, that is the disk check whose absence the file reported, and it also groups the files as the file asked; the same mechanism covers #296, since temporaryDirectory re-creates when temporaryDirectoryProcessID stops matching processID(), so a fork gets its own directory |
| 0 | `bugs/dan/0-thread-safe-mutable-hash-tables` | &nbsp; | [#3186](https://github.com/Macaulay2/M2/issues/3186) | drop | the question is answered and implemented: reads do take a lock -- lookup1, the x#key path, calls lockRead(object.mutex) when the table is mutable (hashtables.dd:357), writes call lockWrite, per #3186 |
| 0 | `bugs/dan/0-toString-Vector` | &nbsp; | [#855](https://github.com/Macaulay2/M2/issues/855) | drop | toString M_1 returns "vector {2, 34}"; the #855 diff is the fix -- toString Vector applied super to the Vector itself, and now goes through expression Vector, which applies it to v#0, the underlying matrix |
| 0 | `bugs/dan/0-ubuntu32-inverse-matrix-RR` | &nbsp; | &nbsp; | drop | verified no longer crashing on Debian i386, and the fix is not attributed: the pre-2013 generic path, quotientRemainder(id_(target U), U), was run on i386 too and returns the same answer without crashing, which rules out e4d1989dea/665dab7cbe (the move to the engine's LAPACK LU for inexact fields) as the cause; either the fix is elsewhere or the 2009 crash was flaky |
| 0.1 | `bugs/dan/0.1-SmithNormalForm` | &nbsp; | [#3710](https://github.com/Macaulay2/M2/issues/3710) | drop | done by #3710, and enforced in code rather than merely documented: matrix2.m2:27 runs preSmithNormalForm and then loops on positions where D_(j+1,j+1) % D_(j,j) != 0, so the divisibility chain is guaranteed. Verified on cases that would expose a fake -- diag(2,3) comes back diag(1,6), diag(6,10,15) comes back diag(1,30,30). The second ask, speeding up the minors, is unverified; the implementation is no longer minor-driven |
| 0.1 | `bugs/dan/0.1-doc-symbols` | &nbsp; | &nbsp; | drop | not reproducible and not attributable. Doug writes unexported methods on exported classes routinely and has never hit this; a package doing exactly that installs clean under CheckDocumentation => true, and installPackage does error on documentation errors (installPackage.m2:633), so it is not passing silently. No fix commit: the gate that would exclude an unexported method function -- isDocumentableThing Symbol requiring isGlobalSymbol and getGlobalSymbol ... === key -- has been in place since 588bbdf34d in 2004, five years before this file, so whatever Dan hit came from elsewhere and there is nothing to point at |
| 0.1 | `bugs/dan/0.1-scc1-typecodes` | &nbsp; | [#1912](https://github.com/Macaulay2/M2/issues/1912) | drop | 16d9b213ff answered it the way the file hoped -- 'perhaps we can fix typecodes so types don't need to have names'. chk.c:316 now gives an unnamed type an autogenerated name, _N_unnamed, with a warning, instead of tripping over it, and the assert Dan wanted uncommented is live again at chk.c:324 |
| 0.4 | `bugs/dan/0.4-method-options` | &nbsp; | &nbsp; | drop | no longer discrepant: under the current 'true >>' idiom both forms agree, f X := true >> o -> x -> [o,x] with Dispatch=>Thing and the same without it each give [OptionTable{}, X{}]. Dan's unprefixed o -> x -> form now returns an unapplied FunctionClosure, which is obsolete spelling rather than the reported discrepancy; #1878 is the live issue in this area |
| 0.5 | `bugs/dan/0.5-XML` | &nbsp; | &nbsp; | drop | both asks met, the second by another route: XML.m2 carries a full multidoc block over its 742 lines, and the libxml2 version is exposed as version#"libxml2 version" from LIBXML_DOTTED_VERSION (d/version.dd:182), the exact macro the file names. It is not in copyright because that node stopped enumerating libraries altogether |
| 0.5 | `bugs/dan/0.5-documentation-missing-for-an-exported-ring` | &nbsp; | &nbsp; | drop | happens automatically now, as the file hoped it eventually would: a package exporting a ring and documenting only the ring installs under CheckDocumentation=>true with no 'missing documentation' warning for (promote,ZZ,R), (lift,...) or (symbol ?,R,R), so the undocumented{...} block the file supplies is no longer needed |
| 0.5 | `bugs/dan/0.5-dummy-line-numbers` | &nbsp; | &nbsp; | drop | reports something reasonable now: with errorDepth at 100 or 255 an error raised inside a Core function is still attributed to the user's own line, where errorDepth=0 gives monideal.m2:25:45. The 'is it possible??' the file wonders about is answered yes |
| 0.5 | `bugs/dan/0.5-homogenize-ideal` | &nbsp; | [#207](https://github.com/Macaulay2/M2/issues/207) | drop | fixed by Thomas Kahle in #207, whose message is the ask verbatim: homogenize(Module,RingElement) homogenizes the Groebner bases of generators and relations, so homogenize(ideal(x^2-y,x^3-z), h) gives {y^2-x*z, x*y-z*h, x^2-y*h}, the saturation, not the generator-wise {x^2-y*h, x^3-z*h^2}. The weighted overload was left behind: homogenize(M,h,{1,1,1,1}) still returns the generator-wise answer for the same module, and there is no homogenize(Ideal,RingElement,List) at all |
| 0.5 | `bugs/dan/0.5-select-submatrix` | &nbsp; | [`0a9c638129`](https://github.com/Macaulay2/M2/commit/0a9c638129) | drop | the matrix half landed: submatrixByDegrees is in Core (matrix.m2:481-515) as (Matrix,Sequence,Sequence), (Matrix,ZZ,ZZ) and (Matrix,List,List), taking a degree box rather than David's membership list. submoduleByDegrees was not added |
| 0.5 | `bugs/dan/0.5-source-rpm` | &nbsp; | [#3983](https://github.com/Macaulay2/M2/issues/3983) | drop | done in #3983: BUILD/rpm/Makefile:25 runs rpmbuild -ba, which builds the source rpm alongside the binary one, from a Source0 tarball staged into SOURCES |
| 0.5 | `bugs/dan/0.5-tallies` | &nbsp; | [#772](https://github.com/Macaulay2/M2/issues/772) | drop | both halves fixed by f39e27cba3 (fix #690): Tally is now a type of VirtualTally in d/sets.dd, exactly the reversal the file asks for, so VirtualTally + VirtualTally covers the mixed cases and they return VirtualTally rather than a bare HashTable |
| 0.5 | `bugs/dan/0.5-time-command` | &nbsp; | &nbsp; | drop | all three programs settled, one better than asked. install-info is checked at configure.ac:241 and errors if info documentation is requested without it. time is no longer a build dependency -- only RunExternalM2.m2 uses it, probing fileExecutable("/usr/bin/time") at runtime with a fallback. unzip appears only in a Docker image that apt-installs it |
| 0.9 | `bugs/dan/0.9-doc-package-source` | &nbsp; | &nbsp; | drop | works now: a package that installs trim String := ... on Core's trim and documents (trim,String) installs cleanly under CheckDocumentation=>true |
| 0.9 | `bugs/dan/0.9-hash-RR` | &nbsp; | [#4251](https://github.com/Macaulay2/M2/issues/4251) | drop | endianness-independent since #4251, 'Update hash for ZZ/RR to be platform-independent and use 64 bits': mpz_hash calls mpz_export with size 1 and endian 1, fixing byte order regardless of host, and mpfr_hash builds on that from mpfr_get_prec and mpfr_get_z_2exp (d/gmp_aux.c:7-45). The basictests/hashcodes.m2 the file points at no longer exists |
| 1 | `bugs/dan/1-2--int` | &nbsp; | &nbsp; | drop | no longer 512: interrupting M2 --int now leaves the shell reporting 130, i.e. terminated by SIGINT under the usual 128+signal convention. Dan asked for 2 and 130 is the conventional spelling of it; the pathological 512, a wait status leaking out where an exit status belongs, is gone and is not even representable in an exit code |
| 1 | `bugs/dan/1-3d-betti` | &nbsp; | [#906](https://github.com/Macaulay2/M2/issues/906) | drop | met by a better mechanism than the one Eisenbud proposed: rather than betti taking two weight vectors and drawing boxes, #906 added MultigradedBettiTally and multigraded(BettiTally), which puts the whole multidegree in each entry as a monomial -- on his own bigraded example multigraded betti gives rows 0,1,2,4 with entries 1, b, a, 2*a*b, a^3*b. That is strictly more information than two projections, and it does not degrade when the numbers reach two digits, which is the worry his message ends on |
| 1 | `bugs/dan/1-CC-inverse` | &nbsp; | [`e4d1989dea`](https://github.com/Macaulay2/M2/commit/e4d1989dea) | drop | the ask is met and by the mechanism it names: inverse Matrix routes every InexactField through the engine (matrix1.m2:626 hasEngineLinearAlgebra -> basicInverse -> rawLinAlgInverse -> DMatLinAlg::inverse), whose LU for ARingRR and ARingCC is LAPACK dgetrf_/zgetrf_ (dmat-lu-inplace.hpp:300,348). e4d1989dea routed inexact fields there; 665dab7cbe narrowed it to InexactField so polynomial rings over RR/CC would not use LU. Both predate the PR workflow. Verified: 5x5 random over CC and RR give norm(A*inverse A - I) of 5.6e-16 and 1.7e-15, CC_200 gives 3.9e-60, and the 'experimental computation over inexact field' warning the file quotes now fires only from groebner.cpp:46. But verifying it turned up a live defect in the same function: inverse of a SINGULAR matrix over an inexact field silently returns the zero matrix instead of erroring -- RR_53, CC_53 and RR_200 all do it, while rank, det and solve all report the same matrix singular -- rank is 1, det is -0, and solve(m, id_(RR^2)) returns null, that last being the call inverse makes internally -- so inverse is the only consumer of the LU that reports success. Cause is one line: DMatLinAlg::inverse (dmat-lu.hpp:529) discards solve's bool and returns true unconditionally, though solve returns false at dmat-lu.hpp:379. Inexact fields only: ZZ/5 and ZZ/32003 error correctly via their own ffpack/flint inverse specializations, and ZZ and QQ error on the non-engine quotientRemainder branch. Searched the tracker on ten term sets, ascending, and nothing covers it -- but #2208 is the same ask on ZZ/5, closed by #2241 (a032f77d13), which fixed element-level inversion in ZZp.cpp and aring-zzp*.hpp and never touched dmat-lu.hpp. Distinct from #4506 (non-square, ZZ, non-engine branch, no squareness check) and #3738. Filed as #4556, citing #2208 |
| 1 | `bugs/dan/1-CC-tostring` | &nbsp; | [`530ef987f8`](https://github.com/Macaulay2/M2/commit/530ef987f8) | drop | the ask is 'go the distance to the right of the point in both parts', i.e. carry both parts to a common accuracy, and 530ef987f8 (2008-02-03, 'print complex numbers to a combined precision for both parts') did exactly that on the 1.1 branch -- the release the file asks for it by. It is still the scalar rule: format(...,CC) at gmp1.d:246 derives one accuracy from exponent(z), the larger part, so at printingPrecision 6 a real part more than six decimal places below the imaginary one falls below the printed resolution and 1e-30+ii prints as ii. Matrix and vector entries reach expression CC (reals.m2:494) instead and format the parts independently, so they show \| 1e-30+ii \|. That difference is not a defect: showing a different number of digits for a CC and for the entry of a matrix over CC is the expected consequence of the two rules, and the scalar rule is the considered one -- a part thirty orders below the other is not part of a six-significant-digit rendering of the number. Note the file's own transcript shows the PRE-530ef987f8 independent formatting, which today's scalar path cannot produce at any printingAccuracy=-1 setting, so the file illustrates one convention while its sentence asks for the other; both halves now show both parts under their own rule |
| 1 | `bugs/dan/1-ConwayPolynomials` | &nbsp; | [`4ca4b0fc4f`](https://github.com/Macaulay2/M2/commit/4ca4b0fc4f) | drop | done in 1.3. ConwayPolynomials is in the preloaded list (Core.m2:191), so no needsPackage is required, and GF's minimal polynomials are the Conway ones: ideal ambient GF(3,4) is a^4-a^3-1 and GF(2,10) is a^10+a^6+a^5+a^3+a^2+a+1, both equal to conwayPolynomial of the same arguments. changes.m2:2532 records it under 'changes, 1.3' and explicitly corrects the 1.2 note at :2700, which had advertised the same thing while the user still had to load the package by hand |
| 1 | `bugs/dan/1-DegreeLength-DegreeRank` | &nbsp; | &nbsp; | drop | the inconsistency is gone and cannot recur: both spellings now resolve to DegreeRank -- it is in options monoid, QQ[x,y,DegreeRank=>2] and newRing(R,DegreeRank=>2) both work, and DegreeLength is rejected by both. newRing cannot diverge from monoid by construction, since modifyRing takes Options => applyValues(monoidDefaults, ...) (newring.m2:9). No fix commit: the March 2011 snapshot of newring.m2, contemporaneous with the reorg that filed this file, already read DegreeRank, so the premise was stale when the file was picked up; DegreeRank first appears there in c21ac71509 (2006-11-01). One trace survives -- the engine still prints the old spelling, Monoid::text_out (monoid.cpp:313) emitting 'DegreeLength => 1', visible from M2 as toString raw monoid R after debug Core. #2237 is a different DegreeRank complaint, that degreeLength of ZZ[DegreeRank=>0] is 0 |
| 1 | `bugs/dan/1-Dispatch-recovery` | &nbsp; | &nbsp; | drop | answered: methodOptions promote returns an OptionTable containing Dispatch => {Thing, Type, Type}, exactly the recovery the file asks for, and method-doc.m2:101 says so in prose -- 'The options given to method can be recovered with methodOptions'. No fix commit, and this one is not fixed-on-purpose in either direction: methodOptions dates to svn r4045 (41d4b2d2c9, 2003) and the doc sentence to before the 2008 Macaulay2Doc rename, so the file was already answered when it was written. #1878 and #4546 are live Dispatch bugs, neither about recovering it |
| 1 | `bugs/dan/1-Frobenius` | &nbsp; | &nbsp; | drop | Walther's functor exists, in the distributed package DirectSummands (=distributed-packages:246). F(M) = S tensor M is frobeniusPullback(ZZ,Module): over ZZ/5[x,y] with M = R^1/(x^2,y^3), frobeniusPullback(1,M) presents as \| x10 y15 \|, i.e. r^p tensor m = 1 tensor rm as the file specifies; frobeniusTwist and frobeniusPushforward are the other two directions, and the map he wanted is frobeniusMap(ZZ,Ring), a RingMap -- which also answers his aside, 'I don't know if M2 has Map as a class'. No fix commit pinned: this arrived as a whole package rather than a change. Core still has no frobenius, so methods frobenius is {} until DirectSummands or TestIdeals is loaded; #2779 tracks that for TestIdeals |
| 1 | `bugs/dan/1-Frobenius2` | &nbsp; | [#277](https://github.com/Macaulay2/M2/issues/277) | drop | Eisenbud's minimum -- 'I could live with (ideal)^[a]' -- is what landed. Ideal ^ Array is in Core (matrix1.m2:562) since d1179c34fa via #277, generalised beyond his code to take one exponent per variable, and MonomialIdeal ^ Array follows it. Dan's counter-proposal of a frobenius(ZZ,X) family never entered Core; frobeniusPower is in TestIdeals and SymbolicPowers, frobeniusMap and friends in DirectSummands (see 1-Frobenius). Matrix ^ Array is still the direct-sum selector, as Dan noted it would have to be. Two open issues sit on top of this, neither the file's ask: #662 says the bracket power is not well defined because equal ideals give unequal results, quoting the merged code verbatim, and #2779 is methods frobenius in TestIdeals |
| 1 | `bugs/dan/1-FromMethod` | &nbsp; | &nbsp; | drop | the syntax the file says is needed already existed when it was written: NewFromMethod dates to the 1997 initial revision (647f7532b3), and every conversion in the file's list installs and runs under it -- 'new List from Matrix := (List,m) -> entries m' then 'new List from matrix{{1,2},{3,4}}' gives {{1,2},{3,4}}, likewise from Set, from Tally, and 'new String from Number' gives a String. The file writes them without 'new', which does not parse, and should not: these expressions construct a new object of type X, so dropping 'new' would not mean anything. What is genuinely unadopted is the speculative block below -- 'Matrix of S from X', 'of' and 'from' as binary operators, 'Scheme over S' -- design sketches from the 2010 Colorado College workshop that nobody pursued, and Core ships none of the specific conversions either, which is a choice rather than a missing mechanism. The file's closing ask, that the 1.3.1.1 parsing precedences be preserved, has held: from/in/of/to/when are still 16 and every level it lists is unchanged. Its one live remnant is the failure it predicts itself: new Matrix from List still falls through to the built-in new HashTable from List and dies with 'key not found in hash table: RawMatrix' at matrix.m2:137 -- the same class of thing as the 'new HashTable from {asdf => foo}' example that opens #4506 |
| 1 | `bugs/dan/1-LU-over-QQ` | &nbsp; | [`46dffc024a`](https://github.com/Macaulay2/M2/commit/46dffc024a) | drop | Parrilo got both halves. LUdecomposition over QQ returns exact rationals -- on {{1,2,3},{4,5,6},{7,8,10}} it gives permutation {0,1,2} with L and U over QQ -- and his parenthetical 'this would probably also enable Solve to work in QQ' holds too: solve and inverse both work over QQ. 46dffc024a (2014-06-04, 'Added code to use flint for dense mutable matrices over QQ, part 1') is the dense QQ path, via dmat-lu-qq.hpp, dmat-qq-flint.hpp and the inverse(DMatQQFlint) specialization at mat-linalg.hpp:1075. He guessed the ZZ/p route would be easier to adapt than the lapack one; in the event it went through flint instead |
| 1 | `bugs/dan/1-MANPATH-on-MacOS` | &nbsp; | [`1d27f0e458`](https://github.com/Macaulay2/M2/commit/1d27f0e458) | drop | fixed by the mechanism the file implies is missing. setup()'s shellfixes carry a third 'rest' field, and MANPATH's is ':' (files.m2:442), which the bash template drops into the empty case as VAR="/PREFIX/DIR:REST" -> MANPATH="/prefix/man:" (files.m2:412). A trailing colon is precisely what BSD and macOS man read as 'and then the default path from man.conf', so the defaults the file worries about losing are preserved rather than replaced. INFOPATH gets the same treatment. Added in 1d27f0e458, the 1.4 release commit, which predates the 2011 reorg that filed this file. Not verified on macOS from here -- the reasoning is from the template and the man.conf convention the file itself quotes |
| 1 | `bugs/dan/1-MacOS-distribution` | &nbsp; | &nbsp; | drop | the info dir index is installed now, by rule rather than by luck: distributions/install/Makefile.in:24-25 installs files/info-dir-template as $(DESTDIR)@infodir@/dir, then :30 runs install-info for every .info file found. The current macOS builder (BUILD/dmg/Makefile) configures with --prefix=$(PKG) and runs make install, which goes through that path, so it picks the dir file up. Evidence it works: this Linux box has 24 Macaulay2 entries in /usr/share/info/dir alongside Macaulay2Doc.info.gz and BeginningMacaulay2.info.gz. Caveat stated plainly: I cannot run the dmg build here, so this is read from the makefiles plus one working platform, not observed on macOS |
| 1 | `bugs/dan/1-Schubert2-more` | &nbsp; | &nbsp; | drop | all three of Mike's numbered problems are settled, and the Riemann-Roch formula Sheldon asked for comes out exactly. (3), the one Mike called a bug -- chi L giving terms of degree above dim X -- is gone: on X = base(3, Bundle=>(L,1,D), Bundle=>(T,3,b)) with X.TangentBundle = T, part_3(todd X * ch L) contains only degree-3 monomials, mechanically checked, and chi L is integral(1/6 D_1^3 + 1/4 D_1^2 b_1 + 1/12 D_1 b_1^2 + 1/12 D_1 b_2 + 1/24 b_1 b_2). That is the Maple output in the file term for term under -K = b_1 and c2 = b_2. (1) and (2), which Mike called cosmetic -- naming the class D rather than D_1, and chern T = 1 - K + c_2 + c_3 -- work through the route he proposed himself in his third email: abstractSheaf(Y, Rank=>3, ChernClass=>1-K+c_2+c_3) prints chern TT as exactly 1 - K + c_2 + c_3, and part_3(todd Y * ch M) gives 1/6 D^3 - 1/4 D^2 K + 1/12 D*K^2 + 1/12 D*c_2 - 1/24 K*c_2. base itself still always subscripts, b_1..b_n (Schubert2.m2:399). And chi M, which his email says fails because integral is unknown there, now returns the integral. No fix commit pinned: this arrived as continued development of Schubert2, not as a response to the file |
| 1 | `bugs/dan/1-ScriptedFunctor` | &nbsp; | &nbsp; | drop | documented. There is a doc node keyed on ScriptedFunctor together with (symbol^, ScriptedFunctor, Thing), (symbol_, ...) and (symbol SPACE, ...) at Macaulay2Doc/doc12.m2:254, headline 'the class of all scripted functors', with subnodes for subscript, superscript and argument, each of which has its own node. help ScriptedFunctor renders it: description, the three-item menu, and the list of methods. The doc dates to the 1999 initial revision of that file (39ca5fabe6), so as with 1-Dispatch-recovery the ask was already met when the file was written; the exclamation mark suggests Dan wanted it fuller rather than existent, and it is still brief -- two sentences plus subnodes -- but ScriptedFunctor is documented |
| 1 | `bugs/dan/1-assert-arguments` | &nbsp; | [#1738](https://github.com/Macaulay2/M2/issues/1738) | drop | the need is met by another mechanism, and the trail is #1736 -> #1738: assert became a method and gained (assert,Expression), so assert Equation(2+2,5) now reports 'assertion failed: 4 == 5 is false' (expressions.m2:120-124). #1736 asked for the same thing from the same motivation as this file, diagnosing a failed assert from a build log, and #1738 closed it. Dan's literal ask is still unmet: assert(false,"D'Oh") gives 'no method found for applying assert to', so no custom string is accepted. The assertEqual family named in #1738's description never landed and appears nowhere in the tree or its history -- only the Expression form did. A derived message beats a hand-written one for reading build logs anyway, since it cannot go stale against the code it describes |
| 1 | `bugs/dan/1-associatePrimes` | &nbsp; | &nbsp; | drop | moved as the file suggested: [associatedPrimes,Strategy] is documented in PrimaryDecomposition/doc.m2:271, and Macaulay2Doc now only cross-references it as PrimaryDecomposition::associatedPrimes. The file's hedged second thought -- 'maybe the options should be method by method' -- was not taken: the node still keys the options globally on associatedPrimes rather than per (associatedPrimes,Ideal) and so on |
| 1 | `bugs/dan/1-cache-gb-MonomialIdeal` | &nbsp; | &nbsp; | drop | cached now, and by fixing the cause Dan suspected. He guessed the gb was not cached 'perhaps because the module is not', and his peek I.cache showed only generators; today it shows {module, generators}, so the module is cached on the ideal. The gb itself lands in (generators I).cache under a GroebnerBasisOptions{HardDegreeLimit => null, Syzygies => false, SyzygyRows => 0} key and is reused -- on a 7-generator monomial ideal in 8 variables the second gb call is about 4.6x faster than the first. gb MonomialIdeal still resolves to the generic gb Ideal := opts -> I -> gb(module I, opts) at gb.m2:311, exactly as in his transcript, so nothing needed a MonomialIdeal-specific method; caching the module was enough. No fix commit pinned |
| 1 | `bugs/dan/1-cartesian-power` | &nbsp; | &nbsp; | drop | Nat Stapleton's inconsistency is gone. set{a,b,c}^**1 now returns set {1 : (a), 1 : (b), 1 : (c)} and every element is a Sequence, so it matches s^**n for n > 1 rather than collapsing to the bare set -- which is what he expected and what he asked whether was intended. Checked at n = 1, 2 and 3; the n = 3 case has 27 elements as it should. No fix commit pinned |
| 1 | `bugs/dan/1-cddlib-makefile` | &nbsp; | &nbsp; | drop | addressed, and the comment left behind names this exact problem. libraries/gfan/Makefile.in:12-13 reads 'Inject LDFLAGS via CDD_LINKOPTIONS so the linker can find cddlib (homebrew on macOS, system or M2-built elsewhere)', and passes CDD_LINKOPTIONS="$(LDFLAGS) -lcddgmp" so the link step follows whatever cddlib configure selected; cddlib is in LIBLIST (configure.ac:590) so --with-system-libs covers it, and configure.ac:609-611 records that both gfan and topcom depend on it. The include line still adds -I$(BUILTLIBPATH)/include/cddlib unconditionally, but a -I on a directory that does not exist is ignored rather than preferred, so it does not make the build 'insist' on the build tree. Caveat stated plainly: this is read from the makefiles, not verified by building gfan against a system cddlib, which I cannot do here |
| 1 | `bugs/dan/1-check-package` | &nbsp; | &nbsp; | drop | check loads the documentation itself now, which is precisely the ask. After a plain loadPackage "Posets" with no LoadDocumentation, check(0, "Posets") prints ' -- warning: reloading Posets' and runs the test, and afterwards all 36 tests are visible; before the call, Posets#'test inputs' is empty. So the transcript's failure mode -- check silently doing nothing until the package is reloaded by hand with LoadDocumentation=>true -- no longer happens, and the option still exists for anyone who wants the documentation up front. The file's second question, 'Do we really like beginDocumentation()??', was rhetorical and beginDocumentation is still there. Testing note: inspecting Posets#'test inputs' before calling check shows 0 and reads as unfixed -- the loading happens inside check, so the function has to be called, not its precondition inspected |
| 1 | `bugs/dan/1-chrpath` | &nbsp; | &nbsp; | drop | the suggestion was taken: configure.ac:178 has AC_CHECK_PROGS([CHRPATH], [chrpath], [false]), distributions/install/Makefile.in:15 branches on it, and it is a BuildRequires in Macaulay2.spec and installed in the Fedora and RHEL Dockerfiles |
| 1 | `bugs/dan/1-configure-library-versions` | &nbsp; | &nbsp; | drop | done, by the autoconf idiom rather than by version arithmetic: where a particular version is needed, configure either checks the version or checks for the feature that version brought, and the second is the normal case. configure.ac carries 107 feature-test invocations -- 34 AC_SEARCH_LIBS, 34 AC_CHECK_HEADER, 14 AC_COMPILE_IFELSE, 11 AC_LINK_IFELSE, 10 AC_CHECK_HEADERS, 7 AC_RUN_IFELSE and so on -- including compile-and-link probes against flint, givaro, gmp and libnormaliz headers, which is exactly 'checking that recent versions appear' expressed as what the code needs rather than as a number. Explicit version gates exist for the two cases where no feature test would do: GNU make 3.81 is rejected at :209, and Arb errors 'found but too old' at :902. Separately, anything M2 builds itself is version-exact -- 27 of 37 libraries/*/Makefile.in pin a VERSION with a SHA256SUM. Recorded first as open on a count of version comparisons, which was the wrong measurement: a low count of those is what a well-written configure script looks like, not evidence of a gap |
| 1 | `bugs/dan/1-control-m-s` | &nbsp; | &nbsp; | drop | the symptom is gone, though the control-Ms are not. Decisive check: the installed Polyhedra.info.gz contains zero CR characters. The reason is that the package was restructured into auxiliary files -- every file under Polyhedra/documentation/ is LF-clean, and none of the eleven files that still carry CRs contains a document{ or doc /// block, so nothing CR-bearing reaches the info output. Polyhedra.m2 itself is now a 63-line newPackage stub with AuxiliaryFiles => true and is CRLF throughout, and CRs survive in about eleven code files under Polyhedra/ (loadFile.m2 324 lines, extended/standardConstructions.m2 261, extended/legacy.m2 249, core/cone/methods.m2 145 and so on). That is untidy but invisible to the reader the file was worried about, and it is not what was asked |
| 1 | `bugs/dan/1-decompose` | &nbsp; | &nbsp; | drop | decompose no longer overflows the stack in factory on this input |
| 1 | `bugs/dan/1-decompose-crash.m2` | &nbsp; | &nbsp; | drop | both halves settled, and the second one exactly. The reported segfault is gone: irreducibleCharacteristicSeries I runs to completion on the quartic and returns a Sequence, with no need for the trim the file suggests as a workaround. And the 'it would be nice if this worked' assertion now computes the wished-for answer -- decompose I returns three components, ideal(y-z,x-z), ideal(2y-z,x) and ideal(3y-2z+1,3x-z+2), which match the three expected ideals pairwise under ==, three for three in both directions. The assert as written still fails, but that is a defect in the assertion rather than in M2: it wraps both sides in set, and Set membership uses object identity, so ideal(y-z,...) and ideal(-y+z,...) count as different despite being equal ideals. Worth keeping as a regression test only if that line is rewritten to compare with ==; recorded here rather than acted on, since promoting reproducers is not what this branch does |
| 1 | `bugs/dan/1-degreeLift` | &nbsp; | &nbsp; | drop | done, and my first reading was wrong. I checked class degreeLift, found an unbound Symbol, and recorded the ask unmet -- but the feature is not a global function of that name, it is a derivation inside map. ringmap.m2:97-104 builds degmapmatrix from the DegreeMap and, when the user supplies no DegreeLift, derives one by solving quotientRemainder against it, raising 'degreeLift: degree not liftable' where no lift exists; identity is short-circuited. Verified: for map(A,B,gens A,DegreeMap => reverse) the cache holds a DegreeLift FunctionClosure at ringmap.m2:101 which sends {1,0} to {0,1}, correctly inverting reverse, and a map with no DegreeMap gets identity. So 'we can compute the degreeLift function for the user automatically' is exactly what map now does. #2580, DegreeLift not preserving homogeneity for multigraded maps, still reproduces and is a separate live bug in the same mechanism |
| 1 | `bugs/dan/1-demangle` | &nbsp; | &nbsp; | drop | the literal ask was 'fix configure.ac', and configure.ac is fixed: :1660-1664 checks whether nm accepts the option at all ('whether nm accepts the demangle option', testing nm --help for the string) and sets NM_DEMANGLES accordingly, and e/Makefile.in:86-88 adds --demangle to NMFLAGS only when it is yes. So the duplicate-symbol check no longer breaks on an nm that cannot demangle. Dan's suggested alternative, piping through c++filt, was not adopted -- on such a machine the dups target simply runs with mangled names rather than demangled ones, which is degraded output rather than a failure, and that is the outcome the ask was protecting |
| 1 | `bugs/dan/1-describe` | &nbsp; | &nbsp; | drop | done, and by exactly the mechanism asked for. describe no longer returns a net: class describe R is Describe, a dedicated type, and toString of it gives a single parseable line -- QQ[w_1..w_2, x..z, Degrees => {2:3, 3:1}, Heft => {1}, MonomialOrder => ...]. Compare the transcript in the file, where toString describe produced the net verbatim, subscripts stranded on their own line ('QQ [w , w , x, y, z, ...' with '1   2' beneath), which is what made it useless for round-tripping. No fix commit pinned |
| 1 | `bugs/dan/1-describe-FractionField` | &nbsp; | &nbsp; | drop | the parentheses the file marks as needed are there. describe frac(QQ[x]) now gives frac(QQ[x, Degrees => {1}, Heft => {1}]) rather than the ambiguous 'frac QQ [x]' in the transcript, so the argument of frac is unmistakably the whole polynomial ring. Same underlying change as 1-describe, describe returning a Describe object instead of a net |
| 1 | `bugs/dan/1-dismiss-Core` | &nbsp; | &nbsp; | drop | guarded now. dismiss Core errors 'Core package cannot be dismissed' and the session continues normally -- 1+1 still works afterwards. The transcript in the file is a collapsed session: dismissing Core took the language's own tokens with it, so startup.m2 line 669 came back with 'undefined token ===', 'undefined token and', 'undefined token <=' and then 'error while loading file', leaving the prompt reading '3 :' with no i. No fix commit pinned |
| 1 | `bugs/dan/1-doc-Weights` | &nbsp; | &nbsp; | drop | the need is documented, though not under the spelling the file uses. help "Weights" now works through a second weight vector explicitly -- 'The weight order may be combined with further ordering elements to break ties. In the following example, we use a second weight vector to break ties under first weight vector' -- with a worked example, QQ[a..d, MonomialOrder=>{Weights => {1,2,3,4}, Weights => {2,4,2,1}}], and leadTerm(1..3, ideal f) showing what each vector settles. So multiple weight vectors are documented, via repeated Weights keys rather than via the Weights=>{{1,2},{2,1}} list-of-lists in the file. The two are equivalent as orderings: both produce Weights {1,2}, Weights {2,1}, GRevLex, Position => Up, differing only in where the MonomialSize storage directive sits, which is not an ordering criterion. The literal ask -- document that Weights takes a list of lists -- is still unmet, but it is a spelling for something the manual already teaches |
| 1 | `bugs/dan/1-doc-disappears` | &nbsp; | &nbsp; | drop | no longer reproduces. After loadPackage("Macaulay2Doc", LoadDocumentation => true), help "changes, 1.3" renders the full node -- the Certification option, the OpenMath and SCSCP packages, the bundled 4ti2/gfan/normaliz -- with no 'warning: there is no documentation for' line and no empty stub. The file's symptom was that the node printed as a bare heading with a warning. No fix commit pinned; the documentation loading path has been reworked repeatedly since 2010 |
| 1 | `bugs/dan/1-doc-loads-packages` | &nbsp; | &nbsp; | drop | both banners are gone. Benchmark no longer announces itself at load time -- there is no stderr write in Benchmark.m2 at all now, and needsPackage "Benchmark" prints nothing -- so the 'type runBenchmarks' block and its eight-benchmark listing cannot appear during an install. The shadowing warnings the file also quotes are gone with the collisions themselves: loading SimplicialComplexes and Polyhedra together produces no warning about isPure, fVector or faces, and EdgeIdeals produces none about graph, checked with debuggingMode set since the warning is gated on it. No fix commit pinned |
| 1 | `bugs/dan/1-doc-random` | &nbsp; | &nbsp; | drop | the premise reversed, and by Dan's own second formula. The file asks to document that there is NO automatic randomization and to suggest setRandomSeed((currentTime() << 16) + processID()) as a substitute. M2 now does exactly that at startup -- startup.m2.in:32 reads 'if gotarg "--no-randomize" then 0 else (currentTime() << 16) + processID()', character for character the suggestion -- and it is documented: help setRandomSeed says 'Since version 1.2, when Macaulay2 starts the random number seed is initially set to a number that depends on the current date, the time (in seconds), and the process id, except for when running examples and tests in packages, where it is always initialized to 0 by passing the command line option --no-randomize'. Verified both branches: three plain M2 startups give three different values from random 2^40, and two with --no-randomize give the same value |
| 1 | `bugs/dan/1-document-Position-Up-Down` | &nbsp; | &nbsp; | drop | documented, and more fully than the three-word ask implies. ov_monomial_orderings.m2:236-275 gives both orders as formulas -- 'position up over term: m F_i > n F_j iff i>j or i==j and m>n' and the corresponding down version -- says which is the default ('The default order is: term over position up. This is called Position=>Up'), and works three examples: the default, Position => Down, and Position placed earlier in the MonomialOrder list so that position is taken into account before the term order. It also appears in the summary table of ordering keys at :681 and renders in help "Position" |
| 1 | `bugs/dan/1-documentation` | &nbsp; | [`9b00503756`](https://github.com/Macaulay2/M2/commit/9b00503756) | drop | the unary methods show now. help Parsing lists all ten entries of methods Parser under Exports > Methods, * Parser and + Parser included; the nodes existed since 2006 (Parsing.m2:420,434) and what was missing was the Methods section on a package page, added by 9b00503756 (help.m2:236,295) on top of 2bc8cd3401, which made code.m2:156-161 handle unary keys deliberately. fix takes the sha because gh api reports the carrying PR as #2289 "Truncations v1.0" -- correct, confirmed against that PR's 31 commits, and uninformative |
| 1 | `bugs/dan/1-emacs-startup` | &nbsp; | &nbsp; | drop | the mechanism is dead and the need is met, downstream. This tree cannot write to /etc: configure.ac:1968-1974 pins sysconfdir=/nowhere on purpose and emacs lisp goes under the prefix (:1956, LAYOUT:106). The postinstall/preremove scripts the ask depends on went with 6c833cbf0c (2025-09-08); reading that commit's parent copy of M2/distributions/deb/macaulay2-common/postinst.in, those scripts only ever ran install-info, so this was never implemented even while the hook existed. Debian answers it by the route that replaced site-start.d: elpa-macaulay2 ships /usr/lib/emacsen-common/packages/install/elpa-macaulay2 and byte-compiles into /usr/share/emacs/site-lisp/elpa/M2-1.26.6/, and emacs --batch with (package-activate-all) reports M2-mode autoloaded and .m2 in auto-mode-alist with no user configuration -- the third condition, not outside /usr, holding for free because the distro package does it. fix blank: nothing in this tree fixed it. Checked against the Ubuntu PPA build of the Debian source package, not Debian unstable itself. 1-site-start.el-file is the obsolete sibling |
| 1 | `bugs/dan/1-error-message` | &nbsp; | [`d76d89c350`](https://github.com/Macaulay2/M2/commit/d76d89c350) | drop | the opaque engine error is gone and the replacement names the cause. saturate(J, x_R, Strategy => Bayer) on the file's own input said "expected degrees all of the same rank"; it now says "assumptions for saturation strategy Bayer are not met" (Saturation.m2:438), and the old string is nowhere in the tree. Came in with d76d89c350 (2020-07-31, PR #1385), which moved saturate onto the hook framework so an inapplicable strategy is reported as such rather than surfacing whatever the engine complained about. Plain saturate(J, x_R) computes normally |
| 1 | `bugs/dan/1-example-files` | &nbsp; | &nbsp; | drop | the file's own preferred answer is the one that landed, and it is now the default. "Or better, get capture working!" -- capture is a d primitive (d/interp.dd:548) and installPackage runs examples in-process through it (installPackage.m2:573-577 -> examples.m2:245-262), with a fork fallback via runFile when isCapturable says no or capture errors; gated to two packages in 1.17, ungated by 29d49c01b6 (2025-08-19) and announced for 1.25.11. Of the first ask, the .m2 half is met -- inputs go to temporaryFileName in /tmp (installPackage.m2:523-524) and are removed on success, and dpkg -L finds no installed .m2 -- but the .out half rests on a wrong premise: getExampleOutput reads them at runtime to render help, so the 8472 installed .out files have to be there. The four-directory refactor was attempted and reverted (eae4bd1b1f, afda220fca, reverted 1a1a009cca and 9de2bbbe7a, June 2020) and the reproducible-path problem it was aimed at was solved textually instead by reproduciblePaths (92952d6389). Residue not worth filing: LAYOUT:89-91 still describes an examples/ directory holding *.m2 and *.out, where the installed one is example-output/ and holds only .out; LAYOUT:93 already says it may get obsolete. 2-check-packages-capture is the adjacent unread row |
| 1 | `bugs/dan/1-export` | &nbsp; | [`63271b9134`](https://github.com/Macaulay2/M2/commit/63271b9134) | drop | the literal sentence is still true and the consequence it turns on is gone. 63271b9134 (2011-03-30) put a new package's export dictionary on dictionaryPath during loading (packages.m2:403-407), so the file's "especially when the dictionary to be exported is not on the path" no longer applies: after export { "zzfoo" => "zzbar" } the private dictionary still holds only zzbar, but zzfoo resolves inside the package to the same symbol object -- zzbar = 42 then zzfoo gives 42, and assignment through either name is visible through the other. The file's own spelling no longer parses either, since de6a0e20e8 (2014-08-28) made both sides strings. Two side notes: packages.m2:436 now makes it an error for the public name to already exist privately, i.e. the file's premise is deliberate policy; and export-doc.m2:26-27 says the form "creates a symbol with the name name", which is imprecise -- no symbol is created, a key is bound to the existing one. Reproducing the transcript also turned up a separate defect, filed by hand as #4575 and with no row of its own: packages.m2:437 tests sym#1 and reports nam |
| 1 | `bugs/dan/1-factor` | &nbsp; | [`b007b6176d`](https://github.com/Macaulay2/M2/commit/b007b6176d) | drop | the experiment became the implementation. The file is a C++ program calling rootOf(x*x-2) then factorize(g,a1) to factor over a simple algebraic extension; e/interface/factory.cpp:661-749 now does exactly that pair of calls inside rawFactorBase, reached from the "NumberField" strategy at factor.m2:132-139, landed in b007b6176d (2012-07-03) and covered by tests/normal/factor.m2:51-56. Verified: toField(QQ[a]/(a^2-2))[x] factors x^2-2 as (x-a)(x+a), GF(9)[x] factors x^2+1, frac(QQ[a])[x] factors x^2-a^2. Towers do not work -- isSimpleNumberField requires one generator, so QQ[a,b]/(a^2-2,b^2-3) falls through and the engine rejects the coefficient ring. Checking this turned up a separate defect, filed by hand as #4574 and with no row of its own: without toField the same quotient silently reports x^2-2 as irreducible, which is the path #1996 left behind when it closed #677 |
| 1 | `bugs/dan/1-factoring-library` | &nbsp; | &nbsp; | drop | the suggestion was taken up, twice over. FLINT is a hard dependency -- configure.ac:590 lists it, :881 requires >= 2.6.3, and M2/libraries/flint/Makefile.in bundles 3.6.0 -- and it literally is M2's integer factoring library: rawZZfactor calls fmpz_factor at e/interface/flint.cpp:46-79. Polynomial factoring goes through factory rather than FLINT's own *_poly_factor, but factory is itself built on FLINT (configure.ac:600, and :942 forces building factory when FLINT is built), so the C library the file points at underlies that path too. fix left blank: adoption was incremental and no single commit answers the file. 5-factoring-by-lifting is a different unread ask, Decker's lifting algorithm |
| 1 | `bugs/dan/1-factory-conversion` | &nbsp; | [`2bc53f48f8`](https://github.com/Macaulay2/M2/commit/2bc53f48f8) | drop | no longer reproduces, and the fix is upstream. All four examples run: the file's own radical J on the ZZ/2 ring finishes in 1.8s, and Schwede's three ass ideal singularLocus cases -- including the ZZ/3 one that segfaulted -- return answers. The failing assertion was factory's own, fac_cantzass.cc:125 in distinctDegreeFactorFFGF; M2 only ever patched that file for an unrelated mpz_mdivmod_ui macro (libraries/factory/patch-3.1.1). M2 left factory 3.1.6 in 2bc53f48f8 (2014-03-02, a Singular snapshot), reached 4.0.0 in 9c051b92b8 (2014-05-24), and now requires >= 4.2.0 while bundling 4.4.1; libfac went separately in cf06c247bc. Same shape as 1-decompose, another factory crash that went away with a version. #4494 is the live factory issue and is about thread safety, not this |
| 1 | `bugs/dan/1-file-search-path.m2` | &nbsp; | [`a6aa79a1ee`](https://github.com/Macaulay2/M2/commit/a6aa79a1ee) | drop | the ask landed and the file the diff patched is gone. a6aa79a1ee (2020-12-21, 'Check package using tests from package test directory, if it exists') added loadTestDir (testing.m2:66-82), which scans PKG/tests/ and feeds it to tests, which check calls; documented at check-doc.m2:85-86. Verified: tests "JSON" prints 'loading tests from /usr/share/Macaulay2/JSON/tests/' and returns two TestInputs, with no path arithmetic in the package. Dmodules itself was renamed to WeylAlgebras in e3be0869c9 and the successor has plain load lines with TEST blocks inside the loaded files, so the workaround the diff introduced is gone too -- Dan fixed the root cause the same week he filed this, in c717fbb72e (2007-06-14). Residue not worth filing: only JSON, LanguageServer and RInterface use the tests/ convention, while 16 packages still hand-roll PKG#"source directory" \| ..., Schubert2.m2:1883-1890 being the purest surviving instance. #3420 is the successor ask, tests keyed to documentation nodes. Also worth knowing: LAYOUT's packagetests key has no counterpart in the live Layout built at startup.m2.in:277, so do not cite LAYOUT as evidence of it |
| 1 | `bugs/dan/1-flatmonoid` | &nbsp; | [`b16f33406a`](https://github.com/Macaulay2/M2/commit/b16f33406a) | drop | exposed, and by exactly the mechanism asked for. FlatMonoid is in the public export block at exports.m2:175 and has a full documentation node at doc_rings.m2:43-59 -- Usage R.FlatMonoid, Inputs, Outputs, a runnable example and SeeAlso flattenRing -- written by b16f33406a (2008-12-27) and relocated by 6d82610a3f. Verified reachable in plain user scope with no debug Core: class (QQ[x][y]).FlatMonoid is GeneralOrderedMonoid. It is load-bearing rather than incidental, being the definition of degreeLength, degreeGroup and heft for a PolynomialRing (polyrings.m2:95-96, hilbert.m2:71). And Schubert2, the package the file names, still uses it -- Schubert2.m2:266, weightRange(first \ degrees (ring f).FlatMonoid, f) -- so the need did not go away, it was met. The lowercase spelling in the file was informal; git grep flatmonoid finds nothing |
| 1 | `bugs/dan/1-font-lock-activity` | &nbsp; | &nbsp; | drop | done, and in the way the file asked for -- 'the way it's done in compile-mode'. The emacs mode is not in this tree (git ls-files '*.el' is empty; it is the Macaulay2/M2-emacs submodule at M2/Macaulay2/editors/emacs), and there M2.el:161,164 sets compilation-error-regexp-alist from M2-error-regexp-alist (:117-146, three patterns each commented with the d source that emits it) and turns on compilation-shell-minor-mode. RETURN and mouse-2 are therefore compile.el's own compilation-button-map, which binds both to compile-goto-error, rather than anything hand-rolled. The conversion was one commit, 5b68215 (2023-02-11, 'Use compilation-shell-minor-mode for jumping to source'), the only one touching either API. Both follow-on regressions in the converted mechanism are fixed: M2-emacs#48, filenames with spaces, by the quoted-filename alternation now in all three regexps, and M2-emacs#65, drifting line numbers, by M2-comint-forget-errors (M2.el:656-665) whose docstring describes that symptom. fix blank: the commit is in another repository |
| 1 | `bugs/dan/1-frac-tower` | &nbsp; | [`711507e071`](https://github.com/Macaulay2/M2/commit/711507e071) | drop | works now, and correctly for the case that can be correct. frac(QQ[x]/x^2[y]) returns a FractionField instead of 'not implemented yet: fraction fields of polynomial rings over rings other than ZZ, QQ, or a finite field', because 711507e071 (pzinn, 2024-02-27, PR #3177 'frac of iterated ring') made factoryAlmostGood recurse through the whole chain (enginering.m2:335-342) where it previously looked only one level down at coefficientRing and so refused iterated rings wholesale. The guard at enginering.m2:350 is still there; it was widened, not removed. For a domain base it is sound: over frac((QQ[a]/(a^2-2))[y]), a^2 == 2, y*(1/y) == 1, 1/a*a == 1 and (y+a)/(y+a) == 1. Found while checking this, and NOT filed here because it is the live core of bugs/mike/0-frac-bug, still todo: the same guard never asks whether any quotient in the chain is prime, so frac accepts non-domains and builds a ring that is not one. frac's own documentation says the input is an integral domain. In frac(ZZ[t]/(t^4)) -- 0-frac-bug's ring -- t =!= 0, t^4 == 0, t*(1/t) == 1 and yet (t^4)*(1/t) == 0 while t^3*(t*(1/t)) == t^3, so the same product grouped two ways gives 0 and t^3. That file's own transcript is fixed, its product l now being 1 rather than a nilpotent denominator, so it could have been settled fixed on the symptom Filed as #4576 and bugs/mike/0-frac-bug settled as its duplicate. |
| 1 | `bugs/dan/1-gcd-GF` | &nbsp; | [`318dccabbd`](https://github.com/Macaulay2/M2/commit/318dccabbd) | drop | Dan asked to get it to work and then made it work, within weeks. 318dccabbd (2008-10-22, 'GF gcd now working'), after eeb878da6d ('GF gcd progress') and 89ed026e2e ('start interface with Singular Libfac for factorization over finite non-prime fields'), added the modeGF branch that calls set_GF_minimal_poly, now at e/interface/factory.cpp:515-519 inside rawGCDRingElement. Verified on the file's own input: A = GF(3,20, Variable => a), R = A[x], gcd(F,G) returns a cubic that divides both F and G and is an associate of the expected common factor a*x^3-2*a*x^2-x-1 -- degree 3, remainder zero both ways, leadCoefficient ratio exact. The file's second half, the toField comparison it says should work, is the part that is still broken; that belongs to bugs/mike/0-gcd-doc.m2, whose last block is this file's example verbatim |
| 1 | `bugs/dan/1-gcd-lcm` | &nbsp; | &nbsp; | drop | no longer recursive, so the limit cannot be reached. gcd(500:1) and lcm(500:1) both return 1, and gcd(5000:1) does too. Binary associative methods now fold iteratively: foldL (methods.m2:72) is (f, x, L) -> (scan(L, y -> x = f(x, y)); x), used by the Sequence and List dispatch installed at methods.m2:87-90, so argument count costs no stack depth. That shape reached its current form in PR #1939 (43cc816c41 and 6e85628c9e, February 2021), but the pre-#1939 code was already a for loop rather than a recursion, so the recursive version the file hit is older; the loop first appears in 5598a53088, a February 2009 merge of r8149-r8463 from the 1.2 release branch, which is the era of this file. fix left blank because that is a branch merge rather than a targeted commit, and pinning the original would need a bisect |
| 1 | `bugs/dan/1-getSymbol` | &nbsp; | [`6fd0832dd5`](https://github.com/Macaulay2/M2/commit/6fd0832dd5) | drop | the invalid name is rejected now. getSymbol("d10--") errors 'invalid symbol' from d/actors5.d:1598, where isvalidsymbol guards symbol creation. The file's odd transcript -- first call silent, second returning the symbol -- is exactly the pattern in #2713, whose body shows getSymbol "x_1" silent at i2 and returning at i3, so this row is that issue seen two years earlier. Closed 2023-03-14 by 6fd0832dd5 (PR #2794, 'fix #2713 and assorted alphanum clarifications'), with tests/normal/unicode.m2:8 keeping it honest |
| 1 | `bugs/dan/1-gmp-random` | &nbsp; | &nbsp; | drop | met, in a better place than the file asked for. There is no such check in configure.ac -- every gmp test there is presence or version, including the only AC_RUN_IFELSE at :918-940, which is a compile-time __GNU_MP_VERSION predicate. But tests/normal/random.m2 is exactly this test: its first line is '-- let's make sure the random number generator doesn't change', and it pins the output of random 100 and of five seed settings against hard-coded vectors, with the superseded pre-1.1 vectors left in comments as the record of the last change. It runs in both build systems and therefore in CI, and it passes today against gmp 6.3.0 -- verified by loading it under --no-randomize, which is how the harness runs tests and which the file's own lines 3-5 explain. M2 does depend on gmp here: random ZZ reaches mpz_urandomm on a gmp_randinit_default state (e/interface/random.cpp:25,63), and gmp documents that default as free to change between releases. A configure probe would be strictly worse -- it cannot see M2's own seeding layer and would fire on machines that only consume prebuilt packages. Residue not filed: random QQ and random RR (random.cpp:182-183, :217, :245, the latter via mpfr_urandomb) have no pinned vectors |
| 1 | `bugs/dan/1-singularLocus` | &nbsp; | &nbsp; | drop | singularLocus(ZZ[x,y]/(11,x)) no longer returns the spurious (11,x,1) |
| 2 | `bugs/dan/2-signed-debian-packages` | &nbsp; | &nbsp; | drop | settled both ways it could be: M2 is in the Debian and Ubuntu archives, so those packages carry the archive signature, and Doug signs the Debian backports on the M2 website with his own key. Nothing is left for a user to click through |
| &nbsp; | `bugs/anton/LINEAR-ALGEBRA/RESOLVED/gCorners.m2` | &nbsp; | [#1651](https://github.com/Macaulay2/M2/issues/1651) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MEMORY-LEAKS-old/RESOLVED/mem-leak-1.m2` | &nbsp; | [#1096](https://github.com/Macaulay2/M2/issues/1096) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MEMORY-LEAKS-old/RESOLVED/mem-leak-det.m2` | &nbsp; | [#1096](https://github.com/Macaulay2/M2/issues/1096) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/RESOLVED/SLEvaluator-checking-for-leaks.m2` | &nbsp; | [#1096](https://github.com/Macaulay2/M2/issues/1096) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/RESOLVED/mutableMatrix-leak.m2` | &nbsp; | &nbsp; | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/RESOLVED/roots-leak.m2` | &nbsp; | [#2359](https://github.com/Macaulay2/M2/issues/2359) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MISC/RESOLVED/complex-matrix-times-polynomial-matrix.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MISC/RESOLVED/n-decompose.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/MISC/RESOLVED/radical.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/NumericalAG/Bertini/RESOLVED/mike-decomposition-problem.m2` | &nbsp; | [#1041](https://github.com/Macaulay2/M2/issues/1041) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/NumericalAG/Bertini/RESOLVED/missing-solutions.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/NumericalAG/Bertini/RESOLVED/refine.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/NumericalAG/Bertini/RESOLVED/witness_set_bug.m2` | &nbsp; | [#955](https://github.com/Macaulay2/M2/issues/955) | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/anton/NumericalAG/RESOLVED/pointArray-SIGSEGV.m2` | &nbsp; | &nbsp; | drop | author filed it under RESOLVED/; fix points at that filing, not necessarily the code change |
| &nbsp; | `bugs/mike/git-issue203-quotient-over-integers.m2` | [#203](https://github.com/Macaulay2/M2/issues/203) | [`172cdeff22`](https://github.com/Macaulay2/M2/commit/172cdeff22) | drop | the quotient assert passes; autorun=fail comes from later API drift |
| &nbsp; | `bugs/mike/git-issue359.m2` | [#359](https://github.com/Macaulay2/M2/issues/359) | &nbsp; | drop | rank assert passes; worth promoting to tests/normal |
| &nbsp; | `bugs/mike/git-issue370.m2` | [#370](https://github.com/Macaulay2/M2/issues/370) | [`0ae5b6eb19`](https://github.com/Macaulay2/M2/commit/0ae5b6eb19) | drop | substitute no longer crashes, and lift(v_1,RR_53) now works |
| &nbsp; | `bugs/mike/git-issue473.m2` | [#473](https://github.com/Macaulay2/M2/issues/473) | [`0ae5b6eb19`](https://github.com/Macaulay2/M2/commit/0ae5b6eb19) | drop | sub(C,QQ) raises a clean error instead of a SIGSEGV |
| &nbsp; | `bugs/mike/git-issue56.m2` | [#56](https://github.com/Macaulay2/M2/issues/56) | [`ff7473fb87`](https://github.com/Macaulay2/M2/commit/ff7473fb87) | drop | 'unknown engine error' is now a specific not-implemented message |

## Won't fix -- `wontfix` (26)

| prio | file | issue | fix | disposition | note |
| ---: | --- | --- | --- | --- | --- |
| 0 | `bugs/dan/0-Macaulay2-deps` | &nbsp; | &nbsp; | drop | dependencies come from a package manager on most modern systems, and an offline build can pre-populate BUILD/tarfiles, which exists for exactly that -- so a published deps tarball is not worth maintaining |
| 0 | `bugs/dan/0-SourceCode` | &nbsp; | &nbsp; | drop | settled the other way Dan floated in the same email -- 'perhaps by ensuring the file is present in the install tree' -- rather than by removing the lines: SourceCode => applicationDirectory is still there, moved to ov_system.m2:1828, and help applicationDirectory works because the layout ships the .m2 sources. The hard failure remains for anyone who strips them, code.m2:71 erroring 'couldn't find file' rather than degrading, and the test he wanted first was never added |
| 0 | `bugs/dan/0-library-source-URLs` | &nbsp; | &nbsp; | drop | not worth automating: each library still has one hardcoded URL (Makefile.library.in:265, and the same shape in cmake/build-libraries.cmake, where M2_SOURCE_URL is shorthand for the mirror rather than a fallback), and #2963 is the failure that causes -- cohomCalg unreachable, five retries against the same dead host, build dead. But the fix would have to be written twice, once per build system, and the standing practice of mirroring a tarball to macaulay2.com and changing one line has handled it about nine times. The mirror-only configure option is release tooling CI has displaced, and the math.uiuc.edu ask is obsolete. #313, sha hashes on every download, was the neighbouring request and is done |
| 0 | `bugs/dan/0-package-doc` | [#4514](https://github.com/Macaulay2/M2/issues/4514) | &nbsp; | drop | closed as wontfix in #4514: a link to the table of contents is already there, and the export list is considered useful |
| 0 | `bugs/dan/0-utf8-and-column-number` | [#4535](https://github.com/Macaulay2/M2/issues/4535) | &nbsp; | drop | closed as wontfix in #4535: byte columns are the convention, not an oversight, and Macaulay2Web relies on locations being bytes, so changing it would be a breaking change across repositories. The tab-stop argument -- that getc(o:PosFile) at d/stdiop.d:198 already advances to the next multiple of 8, so the counter is a display column -- did not carry, because the objection was never that strings are bytes but that downstream consumers index by bytes. Worth recording that grepping the M2 tree for consumers found none; they live in M2-emacs and Macaulay2Web |
| 0.5 | `bugs/dan/0.5-extensions` | &nbsp; | &nbsp; | drop | deliberate: installing methods is how a package extends M2, and the file proposes no rule that would separate a legitimate extension from a conflicting one -- any such heuristic would have to know which of two packages is entitled to the operator. The file's own example settles it: List ! is now installed in Core, so the extension it worried about was adopted rather than disallowed |
| 0.5 | `bugs/dan/0.5-newPackage-Headline` | &nbsp; | &nbsp; | drop | the tree went the other way: Headline is not merely still an option (packages.m2:258) but required -- installPackage.m2:694 errors 'expected non-empty Headline in package X' -- so deriving it from the package doc node is not a direction anyone is taking |
| 0.5 | `bugs/dan/0.5-overflow-auto` | &nbsp; | &nbsp; | drop | deliberate: the html documentation is being rewritten in the second half of 2026, so a rule added to doc.css now would be discarded with it -- and doc.css today has no overflow, white-space or scroll rule at all, table.examples pre setting only margin and padding. The need, that wide example output be scrollable rather than wrapped, belongs to whatever replaces it |
| 1 | `bugs/dan/1-GF-gens` | &nbsp; | &nbsp; | drop | the comment is still there verbatim at galois.m2:214, but the hazard it warns about is unreachable: GF(Ring) starts by calling unpack (galois.m2:62), which errors 'expected k[y] to be a polynomial ring over a finite prime field' on anything that is not a quotient of a one-variable polynomial ring over ZZ/p -- GF(GF(4)[y]/(y^2+y+a)) is rejected there, never reaching line 214. The line is also dead in the ordinary case, superseded six lines later by 'if S.?generators then F.generators = ...' (galois.m2:220), which a quotient ring always satisfies. Nothing to fix; deleting the line or turning it into an assertion would be tidying. #4551, filed from 0.9-toField in this cohort, is a real gens-of-a-field bug, but on toField rather than GF |
| 1 | `bugs/dan/1-Package-dictionary` | &nbsp; | &nbsp; | drop | the same ask as bugs/dan/0-synonyms-and-collisions, which was settled fixed in this catalog, and settling this one as open would contradict that. That file states it as two asks -- '(1) put the package symbols at the head of the list; (2) alert the user to the shadowing' -- and (2) is what got built: loadPackage warns 'symbol "Normaliz" in PackageDictionary is shadowed by a symbol in User#"private dictionary"' and names the Package$Normaliz synonym, so the silent wrong answer David hit in 2010 is gone. (1) was deliberately not done in favour of it, and reordering global symbol lookup is a much larger change than the warning that replaced it. So the symptom reproduces exactly -- verbatim transcript on 1.26.06, interactively; PackageDictionary is still last on dictionaryPath, index 24 of 25; class Normaliz comes back Symbol -- but the need is met and the mechanism was declined, which is wontfix rather than open. One testing note worth keeping: the warning is gated on debuggingMode (packages.m2:88), which M2 --script turns off, so a scripted check shows a clean load and reads as fixed for the wrong reason. Ten open issues cover the surrounding dictionary hygiene, none of them this ordering ask: #666, #799, #1184, #1203, #1427, #1545, #1627, #1649, #4292, and #4496 and #4502 from this cohort |
| 1 | `bugs/dan/1-Parsing` | &nbsp; | &nbsp; | drop | still true and deliberately so. orP(constParser "&&", constParser "&") on input "&" throws: it commits to && and cannot back off. But the absence of backtracking is a design decision stated in the source -- andP carries the comment 'we don't do backtracking: the first parser absorbs as much as it can, and then the second one takes over' (Parsing.m2:97) -- and the package's own tests assert the resulting failures, expecting parser(orP(NNParser, constParser "ab"), "a") and the "abc" case to throw (Parsing.m2:547-550). Changing it would break the package's tests by design. The file's other half, 'doesn't work as advertised', is nearly met: the orP doc now states the requirement, 'In an efficient grammar, the first token presented to r will be acceptable to at most one of the input parsers'. Residual nit only, not worth an issue: the Outputs line above it still promises a parser accepting anything acceptable to one of them, without the caveat. Also worth noting the file's own reproducer is unusable as written -- orP("&amp;", "&", "a") is HTML-escaped, so what was tested cannot be recovered from the text |
| 1 | `bugs/dan/1-RR-QQ-comparison` | &nbsp; | &nbsp; | drop | 1/3 == 1./3 is still false, and it is the right answer. The comparison is exact, not a promotion: compare(x:RR, y:QQ) is mpfr_cmp_q (gmp.d:1475-1479), which compares the double against the rational without rounding either, and since the nearest double to 1/3 is below 1/3 the result is 1/3 > 1./3, which M2 reports. Dan's proposed fix, making them compare equal, would require rounding the rational to the float's precision first -- that is what would make == inexact, and it would break transitivity against nearby rationals. Where the float is exact the two do agree: 1/2 == 0.5 and 1/4 == .25 are both true, which is the tell that promotion is not what is happening. mpfr_cmp_q predates the file (510344c38c, 2008-01), so nothing changed here and the answer to the file's closing question, 'Is it okay?', was settled by leaving it alone |
| 1 | `bugs/dan/1-architecture-dependent-only-build` | &nbsp; | &nbsp; | drop | deliberate: there is no split to expose. Building the architecture-independent half means generating documentation and example output, and that is done by shelling out to the compiled binary -- run.m2:119 builds a command around format(bindir \| "M2-binary") -- so the arch-independent files sit downstream of the arch-dependent ones rather than parallel to them, and a target for one half alone would still have to build the other. What convenience the ask has, insofar as it is coherent, already exists: Makefile.in:117-118 give relink and relink-nostrip, which rebuild the binary without touching anything else |
| 1 | `bugs/dan/1-classic-parsing` | &nbsp; | &nbsp; | drop | unmet, and unimplementable as written. Classic's number parser is one line, Classic.m2:25, numberP = ZZParser \| QQParser -- integers and rationals only, no float parser, so 1.23p123e345 does not parse. But the ask does not say what to do: it proposes replacing e by @ because e is a variable name in classic Macaulay syntax, and then 'p by ???', which is Dan not having decided. There is nothing to implement until someone chooses the spelling, and Classic exists to read Macaulay-1 input, which had no floating point. Note for anyone testing this: the entry point is poly "..." (Classic.m2:36), not Classic"..." |
| 1 | `bugs/dan/1-combine-doc` | &nbsp; | &nbsp; | drop | the mechanism cannot work and the need is met anyway -- the same shape as 1-constant-to-RR, where toRR being compiled made the ask wontfix. combine is a CompiledFunction, so combine(HashTable,HashTable,Function,Function,Function) := HashTable => combine still fails; you cannot install a method key on a compiled function, which is why 'this doesn't work'. But the file's actual want is the second clause, 'it should make its way into the documentation somehow', and it has: combine-doc.m2:4 gives Usage => "z = combine(x,y,f,g,h) or z=combine(x,y,f,t,g,h)", documenting both arities under Key => combine, with worked examples for each (a polynomial product at :53-59 and a divided-power product at :63-69). The five-argument call runs: combine(h,h,plus,times,plus) on hashTable{1=>2,3=>4} returns a HashTable |
| 1 | `bugs/dan/1-configure-build-target` | &nbsp; | &nbsp; | drop | the same ask as bugs/dan/1-architecture-dependent-only-build, which is already settled wontfix in this catalog, and settling it differently here would contradict that. The reason recorded there: there is no split to expose, because building the architecture-independent half means generating documentation and example output, and that is done by shelling out to the compiled binary (run.m2:119 builds a command around format(bindir \| "M2-binary")), so the arch-independent files sit downstream of the arch-dependent ones rather than parallel to them. A target for one half alone would still have to build the other. What convenience the ask has already exists as relink and relink-nostrip, Makefile.in:117-118 |
| 1 | `bugs/dan/1-containment` | &nbsp; | &nbsp; | drop | neither half wants a tracker entry. (a) 'an easy way to tell the maximum degree of the entries of a matrix': there is still no maxDegree, but max apply(flatten entries m, f -> first degree f) is a one-line idiom and degrees source/target expose the graded data directly, so this is a convenience with a working spelling rather than a gap. (b) the comment Dan quotes, 'we can do better in the homogeneous case!', is still in the source, reworded as '-- TODO: how can do better in the homogeneous case?' at modules.m2:311 -- and it now sits directly above the extension point that would implement it, since issub dispatches through tryHooks(ContainmentHooks, ...) so a specialized strategy can be registered as a hook. An in-source TODO next to its own hook is better placed than a tracker entry, and moving it would lose that adjacency |
| 1 | `bugs/dan/1-cookies` | &nbsp; | &nbsp; | drop | a non-goal rather than a gap, and the ecosystem settled it the other way. getWWW is a hand-rolled HTTP/1.1 client from 1996 (http.m2, Copyright 1996) that writes request lines onto a socket with four headers, and delegates HTTPS to a subprocess -- 'openssl s_client -quiet -verify 1' at http.m2:45-48. It is a plain method() with no options, so there is nowhere to pass a cookie, and nothing reads Set-Cookie. But no consumer wants one: OnlineLookup does stateless query-string GETs against oeis.org/search and wayback.cecm.sfu.ca/cgi-bin/isc/lookup, and ReflexivePolytopesDB the same against quark.itp.tuwien.ac.at/cgi-bin/cy/cydata.cgi. That package also shows what happened instead: it carries a documented Access option taking 'm2', 'curl' or 'wget' (ReflexivePolytopesDB.m2:93-98), so an author who hit getWWW's limits shelled out rather than asking for the feature -- and curl brings cookies, redirects, TLS, proxies and auth together, each of which getWWW would otherwise reimplement, cookies being the first of the sequence rather than the last. The second ask, 'also the email address option!', is two words that cannot be reconstructed from the file; the likeliest reading is a From: header, but nothing in the tree corroborates it and guessing was not worth publishing. Would be worth revisiting only for a concrete endpoint someone wants that requires a cookie |
| 1 | `bugs/dan/1-degree-of-zero` | &nbsp; | &nbsp; | drop | a design musing that was never taken up, and the file knows it -- it ends 'Feedback on that idea would be welcome'. The description still holds: degree of the zero ring element is -infinity, while a zero vector keeps the degree of whatever it came from, so degree(0*F_1) is {0}. The proposal was to make R seriously graded with distinct zero elements of each degree, and the file itself names the reason nobody did: you would then have to decide whether r == s and r === s agree for zeros of different degrees, which is a change to equality semantics across the whole system for a cosmetic gain. Fifteen years on there is one zero per ring and no sign anyone wants otherwise |
| 1 | `bugs/dan/1-dismiss-rings` | &nbsp; | &nbsp; | drop | the need has a one-line answer already. dismiss on a PolynomialRing still reports no method, as in the transcript, but the file's stated purpose is the tentative sentence after it -- 'This could unassign x, releasing it for other uses...' -- and that is what 'x = symbol x' does: verified, after QQ[y] the symbol y has class the ring, after y = symbol y it is a Symbol again, and QQ[y] can be built afresh. Extending dismiss to rings would have to decide what becomes of elements still referring to the ring, of its caches, and of every other symbol it bound, for no gain over the idiom. 1-dismiss-Core, the other dismiss file, was a real defect and is fixed |
| 1 | `bugs/dan/1-doc-startup` | &nbsp; | &nbsp; | drop | the half that serves users is done and the other half should not be written yet. 'Indicate that in the error message if they aren't found' is met: loadCore calls describePath before failing, printerring every directory it searched, and only then raises 'Core.m2 was not found on path' (startup.m2.in:622-626) -- which is what someone whose installation is broken actually needs. The remaining ask, documenting how M2 detects where the shared files are, is the exe/readlink/Layout/prefixDirectory logic in initcurrentlayout, i.e. internals; and that same function is the subject of the untracked defect in 1-M2-symlinks, where it follows only one symlink. Documenting it now would be documenting behavior that ought to change |
| 1 | `bugs/dan/1-doc-top` | &nbsp; | &nbsp; | drop | not worth acting on against the current generator, which is a judgment rather than a finding and is flagged as such. The code now builds the button from the package's own directory -- topNodeButton(htmlDirectory, topFileName) at installPackage.m2:253, htmlDirectory being set per package at :459 from installLayout and the package name -- so the defect may already be gone. Confirming it needs the same package installed twice, once bundled and once by the user, which is Mike's exact scenario and which I did not build. Weighed against that: the HTML documentation generator is being rewritten during Aug-Oct 2026, so a verdict on today's output would not survive, and a rendering-only report against code being replaced costs a reader more than it gives. If the top link still misresolves once the new generator lands, that is a fresh report with a current reproducer |
| 1 | `bugs/dan/1-erase-symbol` | &nbsp; | &nbsp; | drop | the need has a supported one-word answer and the proposal would be worse. erase still refuses outright (actors5.d:697, "symbol is in a protected dictionary"), and the 1-dismiss-rings answer does not carry over -- firstFunction = symbol firstFunction fails too, with "assignment to protected global variable". But dismiss FirstPackage takes the package's dictionary off dictionaryPath and the name then resolves afresh in User: class firstFunction goes FunctionClosure -> Symbol and firstFunction = 7 works. Doing it inside erase would have to defeat symbol protection and would leave the package loaded with its exported function no longer a function, silently, for the rest of the session. A judgment rather than a finding, so overrule it cheaply if you disagree. #666 is the opposite direction, asking for a way to create protected globals outside a package dictionary |
| 1 | `bugs/dan/1-error-file-paths` | &nbsp; | &nbsp; | drop | declined for reproducibility of error messages in the documentation, which is the maintainer's call and outranks the argument I first made. The ask is unmet and the mechanism is deliberate: tostring(Position) (d/stdiop.d:113-122) runs the filename through minimizeFilename (:83-90), which takes whichever of as-given, relativized or absolutized is shortest, so an absolute path handed to M2 is discarded. Doug's f75c83343a (2020-06-03) made positions always relative for exactly this reproducibility reason and was reverted 13 days later in 9de2bbbe7a as collateral of the build-directory revert; I argued the reason was obsolete because reproduciblePaths exists, but that is a textual scrub over example output keyed to known prefixes, whereas the relativizing is what keeps builder paths out of the error text at all. One consequence stays observable: with cwd an ancestor the leading slash is dropped, so from / even Core prints usr/share/Macaulay2/Core/startup.m2. That is #1779's symptom, closed 2021-01-04 on not-reproducing-from-a-different-cwd rather than on a change |
| 1 | `bugs/dan/1-example-rerunning` | &nbsp; | &nbsp; | drop | RerunExamples => true is the supported answer, and keying the cache on source mtime would rerun every example in a 300-package distribution on every commit and invalidate all 8472 installed .out files once. The ask is genuinely unmet, verified with a throwaway package rather than by reading: installPackage.m2:561 sets inputhash := hash inputs over the example text alone and :562-564 reuses the .out when that matches, so changing zzAnswer from 111 to 999 with the doc untouched left the output at o1 = 111 under the same hash 1332353094583, and the 999 surfaced only on a third install after an unrelated line was added to the example. Editing an example does rerun it, which is the everyday case. Nothing ever attempted the source-stamp version -- no hit from git log --grep=rerun or -S fileTime on installPackage.m2. A judgment rather than a finding. #4311 is the live neighbour and a different failure, the input changing and the rebuild still going wrong |
| 1 | `bugs/dan/1-fork-sandbox` | &nbsp; | &nbsp; | drop | the two resource-limit steps shipped and the security content is better done from outside the process. Of the seven-step recipe, step 1 is limitFiles and step 6 is limitProcesses (d/system.d:204-221, documented ov_system.m2:2044-2062), both from 778e7f0cad (2012-03-13) -- the same commit as the --read-only-files the file itself credits, which still works: openOut under it is blocked. Steps 2-5, the privilege separation, are absent; there is no chroot or setuid anywhere in d/, c/ or e/, and class sandbox is Symbol. Two reasons not to pursue it. The recipe is fork-then-drop-privileges and fork's own documentation now says very few operations can be safely done in the child of a multithreaded program, even allocating memory -- M2 became multithreaded after this was written. And sandboxing the whole M2 process externally is strictly better than building privilege separation in: a container runtime, systemd unit confinement, a seccomp or firejail-style wrapper or a VM all cover the engine and every library M2 links, do not depend on M2 being correct about it, and need no fork inside a threaded process. Macaulay2Web is one consumer that does exactly that, out of tree. A judgment, so overrule it cheaply. One live gap that is not this file's ask: --read-only-files is documented nowhere in Macaulay2Doc or the man page, unlike limitFiles and limitProcesses. #519 is the adjacent resource-limits issue |

## Obsolete -- `obsolete` (36)

| prio | file | issue | fix | disposition | note |
| ---: | --- | --- | --- | --- | --- |
| 0 | `bugs/dan/0-gc-ucontext` | &nbsp; | &nbsp; | drop | Snow Leopard is long dead and the bundled gc is 7.4.0, not the 7.1 this patches |
| 0 | `bugs/dan/0-gcc-bug-under-debian64` | &nbsp; | &nbsp; | drop | cddplus is no longer built -- only cddlib remains, and configure.ac mentions cddplus solely in a historical comment; g++ 4.3 is from 2008 |
| 0 | `bugs/dan/0-getBlock-alignment` | &nbsp; | &nbsp; | drop | d/factory_allocator.c is gone from the tree; the copied allocator it describes no longer exists |
| 0 | `bugs/dan/0-library-dependencies` | &nbsp; | &nbsp; | drop | libraries/final/bin no longer exists, and the file already records this as done for deb |
| 0 | `bugs/dan/0-non-blocking-hashtables` | &nbsp; | &nbsp; | drop | cites Google Video and Google Code, both shut down; nbds and high-scale-lib are abandoned |
| 0 | `bugs/dan/0-ports` | &nbsp; | &nbsp; | drop | a 2009 release-engineering checklist whose every entry is long EOL: Mac OS 10.4 and 10.5 including PowerPC, cygwin 1.5, ubuntu 8.10, debian 4.0, Fedora 10, Scientific Linux 5.2, freebsd 7.1. PowerPC appears nowhere in configure.ac or CI; the distributions built now are deb, rpm, dmg, freebsd and tar |
| 0 | `bugs/dan/0-resolution-premature-display` | &nbsp; | &nbsp; | drop | cannot be staged any more: inThread is gone from the tree, it came from Bill's experimental threading branches and the API is schedule now, and there is no M.cache.res to catch mid-computation -- the cache holds ResolutionObject and Resolution, and res returns a Complex. The final betti still matches the transcript |
| 0 | `bugs/dan/0-slowness` | &nbsp; | &nbsp; | drop | the premise no longer bites: Attributes holds 2679 entries, well past the 1600 complained of, and getAttributes ReverseDictionary scans them in 2.3 ms; its one caller is allValues, behind the interactive showStructure |
| 0.1 | `bugs/dan/0.1-readline-configure` | &nbsp; | &nbsp; | drop | a 2009 Mac toolchain: gcc 4.0.1 build 5493 targeting i686-apple-darwin9, which is Mac OS 10.5, failing on '-compatibility_version only allowed with -dynamiclib' while linking libreadline.6.0.dylib. M2 can still build readline from source, but nothing about that compiler or that OS survives |
| 0.5 | `bugs/dan/0.5-bison-version` | &nbsp; | &nbsp; | drop | premise gone: bison++ was abandoned around 2000, and configure.ac:281 already rejects anything that is not bison outright, AS_IF([test "$YACC" != "bison -y"], [AC_MSG_ERROR([bison is required])]) -- a stricter test than the version floor the file asks for. No version check exists |
| 0.5 | `bugs/dan/0.5-cygwin-port-too-slow` | &nbsp; | &nbsp; | drop | cygwin is gone: M2/distributions/cygwin was removed and 51d82cd738 cleaned up the remnants in 2021, __CYGWIN32__ went with dumpdata in d560e8284a in 2019, and no CI job builds for Windows. The benchmarks here are from 2007 on gcc 4.1.3 and Cygwin 1.5.25 |
| 0.5 | `bugs/dan/0.5-share-precomputed-files` | &nbsp; | &nbsp; | drop | the experiment was never run and nothing in the tree carries the notion: no 'precomputed' machinery in the build at all, and distributions/tar splits pre_prefix from pre_exec_prefix for binary tarballs only, not for the source distribution the file is about |
| 1 | `bugs/dan/1-INSTALL` | &nbsp; | [#3334](https://github.com/Macaulay2/M2/issues/3334) | drop | both halves of the premise are gone. The rpm is no longer built from distributions/: it comes from BUILD/rpm/Macaulay2.spec driven by .github/workflows/build-rpm.yml, and that spec mentions neither INSTALL nor distributions nor a %doc section. And the file it wants shipped no longer exists -- #3334 deleted M2/INSTALL in favour of the wiki page 'Building M2 from source using Autotools', which is where an installer would now be pointed. See 1-INSTALL-file for the one INSTALL that survives |
| 1 | `bugs/dan/1-INSTALL-file` | &nbsp; | [#3334](https://github.com/Macaulay2/M2/issues/3334) | drop | literally still true and no longer worth acting on. distributions/top/INSTALL is still generated from INSTALL.in, distributions/tar still archives only @pre_prefix@ and @pre_exec_prefix@, and nothing installs INSTALL into either, so it is still absent from the tarball. But --enable-tarfile defaults to no (configure.ac:446) and releases ship as deb, rpm and dmg from CI instead, and the INSTALL.in it would carry is a period piece: it tells Windows users to install cygwin, recommends epkg for administrators, and devotes a section to M2 --dumpdata, which has zero occurrences left anywhere in d/ or m2/. The top-level INSTALL went to the wiki in #3334 for the same reason |
| 1 | `bugs/dan/1-README-MacOSX-fink` | &nbsp; | [#3334](https://github.com/Macaulay2/M2/issues/3334) | drop | every noun in it is gone. There is no Macaulay2/Downloads/MacOSX/README-MacOSX-fink -- the whole hand-maintained Downloads tree went, releases come from CI as dmg, deb and rpm. The INSTALL it would be kept in step with was deleted by #3334 in favour of the wiki, so there is nothing left to automate the production of, and m2/install.m2.in is likewise gone. fink survives only as four historical comments: two describing the fink-vs-fhs directory layout choice (Macaulay2/LAYOUT:8-10, include/config.Makefile.in:47), one in configure.ac:613 about a readline library, and one suggesting fink as a way to install polymake yourself |
| 1 | `bugs/dan/1-abs-paths` | &nbsp; | &nbsp; | drop | premise no longer holds in the form written: an error in generated example output is recorded as stdio:1:4:(3):[1]: error: ..., a stdio position with no filesystem path in it at all, so there is no relative path to make absolute. The tree also moved deliberately the other way -- runFile left html.m2 for run.m2 and passes --srcdir through relativizeFilename(rundir, d), relativizing on purpose. No absolute-path switch exists and none is wanted |
| 1 | `bugs/dan/1-benchmarks` | &nbsp; | &nbsp; | drop | a 2008 SVN patch adding benchmark timings from gcc 4.0/4.1 machines |
| 1 | `bugs/dan/1-book-code-updates` | &nbsp; | &nbsp; | drop | never built, and the venue moved: no mechanism ships errata for the 2001 book with a release, and nothing in the tree tracks its code -- Book3264Examples.m2 is a different book, and the only references to Computations in Algebraic Geometry with Macaulay2 are two comments in LocalRings. Twenty-five years on, per-release errata is not the form this would take; the wiki is where such advice now lives |
| 1 | `bugs/dan/1-carbon-emacs` | &nbsp; | &nbsp; | drop | Carbon Emacs is long dead; this was about borrowing its icon for .dmg files |
| 1 | `bugs/dan/1-checkpointing` | &nbsp; | &nbsp; | drop | a 2007 'try it out!' for Gene Cooperman's mtcp/dmtcp, pointing at internap.dl.sourceforge.net for dmtcp_0.99-r80. Nothing was tried: there is no mtcp_init, libmtcp or checkpoint machinery anywhere in the tree. The feature it would have served is dumpdata, saving interpreter state to start faster, and that is gone outright -- zero occurrences of dumpdata in d/ or m2/. So the premise is retired on both sides, the mirror URL is long dead, and process-level checkpointing of an interpreter is not something M2 has pursued since |
| 1 | `bugs/dan/1-clustrmaps` | &nbsp; | &nbsp; | drop | a 2008 note about adding a clustrmaps.com widget to the web site |
| 1 | `bugs/dan/1-configure-64-bit` | &nbsp; | &nbsp; | drop | the premise is a 32-bit-by-default toolchain on 64-bit-capable Intel hardware, which is how Mac OS X 10.5 and 10.6 shipped. configure.ac has no sysctl, no hw.optional and no -m64 anywhere, and it needs none: every supported Mac is 64-bit, Apple Silicon has no 32-bit userland at all, and the compilers default to 64-bit, so the automatic --build=x86_64-apple-darwin CC="gcc -m64" the file asks for would now be either a no-op or wrong. Same era and same fate as 1-configure-arch, already settled obsolete in this catalog |
| 1 | `bugs/dan/1-configure-arch` | &nbsp; | &nbsp; | drop | all three asks are dead: 32-bit Mac (the warnings quote i686-apple-darwin8, which is 10.4, and gcc 4.2.3), an --enable-*bit selection that configure.ac has no trace of, and disable-fc-lib-ldflags, which appears nowhere in the tree |
| 1 | `bugs/dan/1-cygpath` | &nbsp; | &nbsp; | drop | use cygpath rather than mount, in a build M2 no longer has: the cygwin distribution was removed, remnants cleaned in 51d82cd738 |
| 1 | `bugs/dan/1-cygwin-info` | &nbsp; | &nbsp; | drop | /usr/share/info/dir not updating under cygwin, in a distribution that no longer exists. The install-info half is separately settled -- configure.ac:241 checks for it and errors when info documentation is requested without it |
| 1 | `bugs/dan/1-debian-old-compiler` | &nbsp; | &nbsp; | drop | both halves are gone. cddplus is no longer in the tree at all -- M2/libraries/ carries cddlib and nothing else matching cdd -- so there is no longer anything to fail to compile. And the compiler in question, g++ 4.3.2, is from 2008; the friend-function-in-template lookup the file demonstrates was a real g++ bug of that era, fixed by 4.3.4 as the file itself notes, and no supported build uses anything remotely that old |
| 1 | `bugs/dan/1-emacs-app` | &nbsp; | &nbsp; | drop | fetch Emacs.app from the Emacs CVS repository at tag EMACS_22_3 for the .dmg icon. Emacs 22 is from 2007, Carbon is gone in favour of the NS port, and Emacs left CVS for bzr and then git |
| 1 | `bugs/dan/1-freebsd` | &nbsp; | &nbsp; | drop | the specific error is unreachable and the port machinery is broken. The 2008 report is a GCC 3.4 libstdc++ cwchar clash; configure.ac:271 now sets -std=gnu++17, which GCC 3.4 cannot compile, and git grep finds no live wcstold/wcstoll/cwchar in the tree. M2/distributions/freebsd/ is vestigial from FreeBSD 7.1 (3bd4ddea6e, 2009-01-06) and provably cannot work: Makefile.in:26,62 drive pkg_create and pkg_info, removed in FreeBSD 10.0 in January 2014, and post-install.in/post-deinstall.in are three-line no-ops. No BSD appears anywhere in .github/workflows. A genuine ports effort in June-July 2020 (#1259, #1274, #1275, #1279, #1283, all against /usr/ports/math/macaulay2/) closed without landing a port, and freshports has no macaulay2. Two residues belong on #1091, not here: the 19-year-stale GCC-3.4 HINT at libraries/factory/Makefile.in:73-78, which is the only in-tree text that answers this file, and the --enable-freebsd plumbing at configure.ac:509-516 and m4/files:11-13 |
| 1 | `bugs/dan/1-gc-debugging-mode` | &nbsp; | &nbsp; | drop | both nouns in the sentence are gone, deliberately. M2CPPFLAGS appears nowhere in the tree -- the build variable is M2_CPPFLAGS (configure.ac:266) and the spelling in this file was never in the configured files, only in the file itself. -DDEBUG was removed in 0c90490d32 and 6bed5b9d38 (2016-03/04) and configure.ac:307 records why: 'It is a mistake to add -DDEBUG to CPPFLAGS, because it is nonstandard and may confusing libraries, such as flint'. What --enable-debug passes now is -DGC_DEBUG (:310-311), which gc.h honours, and libraries/gc/Makefile.in:65-80 adds --enable-gc-debug, --enable-gc-assertions, --enable-static and --disable-parallel-mark. So gc debugging compiles. The breakage this file anticipated did happen, as #1027 in 2019, and was fixed by c44922195a plus e149f3a2df. Only residue: an unreferenced DEB_CPPFLAGS = -DDEBUG at libraries/ntl/Makefile.in:14. #1206 is the live remnant of the DEBUG-vs-NDEBUG cleanup and is about mathicgb |
| 1 | `bugs/dan/1-gctests` | &nbsp; | &nbsp; | drop | both targets are gone or unbuildable and the mechanism the file diagnoses does not exist. gc_test went with the whole util/ directory in b68e23a93a (2025-05-22). testgc.c survives at Macaulay2/m2/basictests/ but cannot be built twice over: its Makefile is never generated, having left the configured-files list in f2ba6a4661 (2013-02-10), and LOADLIBES += @GC_LIBS@ at basictests/Makefile.in:57 is an unsubstituted placeholder since 52da3ae6ef (2019-08-17) dropped GC_LIBS from configure -- that line is the single occurrence of the string in the repository. .LIBPATTERNS, whose missing %.dylib the file blames, appears nowhere in the tree at all. What replaced the check: the .m2 files in basictests are compiled into the binary as strings (d/Makefile.in:219, cmake/startup.cmake:80-84) per the 2013 design, and testgc.c being C could not make that transition. The bundled gc is now exercised by upstream bdwgc's own make check, which CI skips via make check -o check-in-libraries. #1091 is the live home for the cleanup and already names gc_test.c |
| 1 | `bugs/dan/1-native-windows-port` | &nbsp; | &nbsp; | drop | compile under cygwin and pass -mno-cygwin to gcc. That flag was deprecated and then removed from cygwin's gcc years ago, and the cygwin distribution it depends on is gone |
| 1 | `bugs/dan/1-site-start.el-file` | &nbsp; | &nbsp; | drop | cygwin postinstall and preremove scripts keeping up to 100 backups of site-start.el; those scripts went with M2/distributions/cygwin |
| 1 | `bugs/dan/1-symbolic-links` | &nbsp; | &nbsp; | drop | avoid symbolic links because firefox cannot follow ones made by a cygwin-compiled program under Windows. No cygwin build remains to make them |
| 1 | `bugs/dan/1-virtual-appliance` | &nbsp; | &nbsp; | drop | distribute M2 for Windows as a VMware virtual appliance, the way Sage did in 2008. Sage stopped, the linked modular.math.washington.edu path is long gone, and M2's Windows story is not this |
| 2 | `bugs/dan/2-cygwin-port-report` | &nbsp; | &nbsp; | drop | a 2006 user report of building M2 under Cygwin against a SAGE spkg, needing CPPFLAGS=-D_setmode=setmode. The cygwin distribution was removed in 2021 (51d82cd738) |
| 3 | `bugs/dan/3-cons-vs-make` | &nbsp; | &nbsp; | drop | a proposal to consider SCons as a build system. The question was settled the other way: M2 has autotools and CMake, and scons appears nowhere in the tree |

## Not yet triaged -- `todo` (542)

| prio | file | kind | autorun | candidate issue |
| ---: | --- | --- | --- | --- |
| 0 | `bugs/mike/0-DegreeLimit-and-toField` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-GF-division.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-SkewCommutative` | note | n/a | [#210](https://github.com/Macaulay2/M2/issues/210) bug in prune |
| 0 | `bugs/mike/0-adam-boocher.m2` | repro | pass-partial | &nbsp; |
| 0 | `bugs/mike/0-basis-with-no-variables` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-bugs-with-de.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-bumped-error-message` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-codim-monomial-ideal.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-crash-denham-bugs.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-curves-g23.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-david-madore-frac.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-discriminant` | note | n/a | [#4449](https://github.com/Macaulay2/M2/issues/4449) discriminant throwing an error when it probably shouldn't |
| 0 | `bugs/mike/0-doc-homogenize.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-doc-quotient.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-elimination-ordering` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-empty-monomial-ordering-blocks` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-fgeiss-memleak.m2` | repro | pass-partial | &nbsp; |
| 0 | `bugs/mike/0-fmoore-pfaffians-res-crash.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-fmoore-quotientBug-jan2013.m2` | repro | pass-partial | &nbsp; |
| 0 | `bugs/mike/0-frac-bug.m2` | repro | pass | &nbsp; |
| 0 | `bugs/mike/0-free-invalid-pointer-in-res` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-gauss-hermite.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-gcd-doc.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-high-memory-use` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-intclosure.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-jrajchgot.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-kleinberg.m2` | repro | timeout | &nbsp; |
| 0 | `bugs/mike/0-lift-realmatrix-ZZ` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-liftable-Matrix` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-memory-problem` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-new-delete` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-primdec.m2` | repro | fail | &nbsp; |
| 0 | `bugs/mike/0-primdec2.m2` | repro | pass | &nbsp; |
| 0 | `bugs/mike/0-quotient-rings.m2` | repro | pass-partial | &nbsp; |
| 0 | `bugs/mike/0-raw-gb-get-matrix` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-rawAlexanderDual` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-rawIdentity-seems-slow` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-rawMonomialIdeal` | note | n/a | [#2569](https://github.com/Macaulay2/M2/issues/2569) locate and code for functions involving options, caching, or @@ are us |
| 0 | `bugs/mike/0-reduction.m2` | repro | pass-partial | [#996](https://github.com/Macaulay2/M2/issues/996) ideal of groebner basis should remember it has a groebner basis |
| 0 | `bugs/mike/0-toricHilbertScheme-test-result-changed-mysteriously` | note | n/a | &nbsp; |
| 0 | `bugs/mike/0-weyl-homog.m2` | repro | pass | &nbsp; |
| 00 | `bugs/mike/00-IrrCharSeries.m2` | repro | fail | &nbsp; |
| 00 | `bugs/mike/00-bug-gbZZ13.m2` | repro | pass-partial | &nbsp; |
| 00 | `bugs/mike/00-bug-radical` | note | n/a | &nbsp; |
| 00 | `bugs/mike/00-charset-wrong.m2` | repro | fail | &nbsp; |
| 00 | `bugs/mike/00-debugging-assert-fail-mpir.m2` | repro | pass | &nbsp; |
| 00 | `bugs/mike/00-eisenbud-bug-extend.m2` | repro | fail | &nbsp; |
| 00 | `bugs/mike/00-factor-crash.m2` | repro | fail | &nbsp; |
| 00 | `bugs/mike/00-gcc48-mountainlion.m2` | repro | fail | &nbsp; |
| 00 | `bugs/mike/00-isPrime-char2.m2` | repro | pass-partial | &nbsp; |
| 00 | `bugs/mike/00-kahle-isPrime.m2` | repro | pass-partial | &nbsp; |
| 00 | `bugs/mike/00-rawCharSeries.m2` | repro | timeout | [#1443](https://github.com/Macaulay2/M2/issues/1443) crash in factory |
| 00 | `bugs/mike/00-res-crash.m2` | repro | timeout | &nbsp; |
| 1 | `bugs/dan/1-gratuitous-use-of-curl` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hal-negative-hf.m2` | repro | fail | &nbsp; |
| 1 | `bugs/dan/1-hashing-doc-examples` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hashtable-efficiency` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-height-balanced-trees` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-help-memory-consumption` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-help-synopsis` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hermite-memory-leak` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hilbertSeries-coherent-sheaf` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-homog-tensor-subst` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hooks` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-html-doc-directory` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-html-package-links` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-html-to-pdf` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-hyperlinks-to-preinstalled-packages` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-icon` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-improvements-for-sage` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-include-files` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-index-strings` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-infinity` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-info` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-info-dir` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-info-doc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-info-files` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-info-link-redirection` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-init-priority.cc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-install-info-dependency` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-installMethod` | note | n/a | [#3076](https://github.com/Macaulay2/M2/issues/3076) Inconsistent "installMethod" error messages |
| 1 | `bugs/dan/1-installPackage` | note | n/a | [#4554](https://github.com/Macaulay2/M2/issues/4554) installPackage does not create or update the info dir index beside the |
| 1 | `bugs/dan/1-installPackage-links` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-interrupt` | note | n/a | [#813](https://github.com/Macaulay2/M2/issues/813) CTRL-c should interrupt multi-line input in a terminal |
| 1 | `bugs/dan/1-interrupt-appearance` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-interrupt-connect` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-invisible-symbol` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-isPrime` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-isPrime-benchmark` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-jacobian` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-join-hash` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-kernel-ringmap` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-kernel-ringmap-ZZ` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-lcm-needed` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-leadTerm-crash.m2` | repro | fail | &nbsp; |
| 1 | `bugs/dan/1-lift-RR-CC` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-lift-modules` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-lifting` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-line-wrapping-with-negative-numbers` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-list-of-ideals-of-R` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-loadPackage` | note | n/a | [#2664](https://github.com/Macaulay2/M2/issues/2664) loadPackage goes inside the package  |
| 1 | `bugs/dan/1-loading-documentation` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-local-doc-design` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-local-variable-shielding` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-logging` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-long-list-parsing` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-make-and-capybara` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-map` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-monoid-degreeLift` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-monorder.m2` | repro | fail | &nbsp; |
| 1 | `bugs/dan/1-monorder2.m2` | repro | pass | &nbsp; |
| 1 | `bugs/dan/1-more-synonyms` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-mpc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-mutable-list-doc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-named-pipes` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-names-of-fraction-rings` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-needless-reloading-of-Macaulay2Doc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-network-security-via-cookies` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-new-lift-and-promote.m2` | repro | fail | &nbsp; |
| 1 | `bugs/dan/1-new-res-options` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-newRing` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-non-orthogonality` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-not-equation` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-on-and-methods` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-open-databases-during-installPackage` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-openListener-IPv6` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-opensuse-build-service` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-options` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-options-of-binary-methods` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-overflow-test` | note | n/a | [#627](https://github.com/Macaulay2/M2/issues/627) overflow test fails and the makefile doesn't know it |
| 1 | `bugs/dan/1-package-configuration` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-package-symbols` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-packages` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-parallel-documentation-and-source` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-parsing-RR` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-part-deprecation-notneeded` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-part-doc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-pivots` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-poincare-bug` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-porting` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-positions-of-strings` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-ppc-version` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-preimage` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-printing-rings` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-printingPrecision` | note | n/a | [#1642](https://github.com/Macaulay2/M2/issues/1642) generateAssertions generates an assertion that fails for eigenvectors |
| 1 | `bugs/dan/1-profile-init` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-prune-setting-eliminated-variables` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-pushForward1` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-python-interface.c` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-python-stuff` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-quotrem-welldef` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-random-bug` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-random-height` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-ranges` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-read` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-read-prompt` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-readline-MacOSX` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-real-output` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-references-citations` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-regularity` | note | n/a | [#3321](https://github.com/Macaulay2/M2/issues/3321) Regularity for modules over quotient rings |
| 1 | `bugs/dan/1-reinteract-graphical-interface-suggestion` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-replace-string-speed` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-ring-maps` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-ringmaps` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-rpm-directories-linger` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-rpm-repository` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-running-examples` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-running-tests-and-examples` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-scalar-times-matrix-coercion` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-scan-apply-idea` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-script-option` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-search-documentation` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-select-method` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-series-expansion.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/dan/1-setRandomSeed` | note | n/a | [#89](https://github.com/Macaulay2/M2/issues/89) test/random.m2 fails |
| 1 | `bugs/dan/1-setting-state` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-setup` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-setup-announcement` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-setup-emacs` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-shadowed-symbols` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-sheaf-Ideal` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-show-watch` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-slow-gcd` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-smithNormalForm` | note | n/a | [#940](https://github.com/Macaulay2/M2/issues/940) smithNormalForm gives incorrect answer (matrix wrong dimensions) |
| 1 | `bugs/dan/1-smithNormalForm-msg` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-smithNormalForm2` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-sort` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-sort-function` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-sort-permutation` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-speed-up-random-matrices` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-spurious-documentation-shadow-warning` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-stack-protector` | note | n/a | [#573](https://github.com/Macaulay2/M2/issues/573) command line option '-std=gnu++11' is valid for C++/ObjC++ but not for |
| 1 | `bugs/dan/1-static-linking` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-step` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-stopping` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-strange-C-warning` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-strange-shadowing` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-string-syntax` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-sub-variables` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-subquotient` | note | n/a | [#3960](https://github.com/Macaulay2/M2/issues/3960) tensoring subquotient modules with ring maps |
| 1 | `bugs/dan/1-symbol-ring` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-symbolic-algebra` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-symmetricAlgebra` | note | n/a | [#4530](https://github.com/Macaulay2/M2/issues/4530) symmetricPower of a matrix ignores relations, disagreeing with the mod |
| 1 | `bugs/dan/1-syntax-error-repeats` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tally-multiples` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tar-file` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tbb` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tensor-galois-fields` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tensor-maps` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tensor-tower-degree` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-test-files` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-testing-book` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tests-to-fix-next` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-tex-simpledoc` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-texmacs-tab-completion` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-thread-safety-of-mutable-hashtables` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-threads` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-toExternalString-QuotietRing` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-toExternalString-poly-bug` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-toField` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-toField-class` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-toString-net-toSymbol` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-top-components` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-trim-gb-reuse` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-two-letter-names` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-ultimate` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-undoing` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-unevaluated-example-output` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-universal-binaries` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-utime-is-obsolete` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-validate-html` | note | n/a | [#2225](https://github.com/Macaulay2/M2/issues/2225) xmlParseEntityDecl: entity xhtml-qname-extra.mod not terminated |
| 1 | `bugs/dan/1-value-dictionary-path` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-variable-names-in-degrees-rings` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-variables` | note | n/a | [#4560](https://github.com/Macaulay2/M2/issues/4560) The debugger skips a calling function that declares no local variables |
| 1 | `bugs/dan/1-version-number` | note | n/a | [#123](https://github.com/Macaulay2/M2/issues/123) configure and /etc/issue |
| 1 | `bugs/dan/1-viewHelp` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-viewHelp-dir` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-virtual-machines` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-weak-pointers` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-web-site` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-weighted-proj` | note | n/a | &nbsp; |
| 1 | `bugs/dan/1-weighted-proj.m2` | repro | pass | &nbsp; |
| 1 | `bugs/dan/1-wide-chars-in-filenames` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-2schreyer-memleak.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-8byte-to-4byte-conversion-warnings` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-GF.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-RR-det.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-Schubert-speed-ups` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-SchurRing-loop` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-ZZ-64ubuntu.m2` | repro | pass | &nbsp; |
| 1 | `bugs/mike/1-add-rawEntries-maybe` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-autoreduction` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-baccala-towers.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-basis-fields.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-big-coeffs-in-fractions` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-blundell-frac-fields.m2` | repro | pass | &nbsp; |
| 1 | `bugs/mike/1-book-notes.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-bothmer-crash.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-brandy-solve-ex.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-cast` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-crash-entradaM27.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-det-RR.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-division--ZZ.m2` | repro | pass | &nbsp; |
| 1 | `bugs/mike/1-ed-carter-extdecomp.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-ext-slow.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-gb-local-bugs.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-gb-matrix-lift-feature-wanted` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-gc-leak.c` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-hal-negative-hf.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-hermiteLLL-leadTerm` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-integralClosureIdeal.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-interrupts` | note | n/a | [#1160](https://github.com/Macaulay2/M2/issues/1160) Ctrl-C interrupts in the read() function interferes with top-level pro |
| 1 | `bugs/mike/1-inverses` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-joswig` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-local-bug.m2` | repro | timeout | &nbsp; |
| 1 | `bugs/mike/1-localrings.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-mingens-subquotient.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-moty-frac.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-mult-trun` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-non-unit-problem-in-zero-ring` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-overflow` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-overflow-ntuple` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-petrovic-M2busError.m2` | repro | timeout | &nbsp; |
| 1 | `bugs/mike/1-prune-ring` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-pushForward1` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-pushforward-functor.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-radical-b.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-radical.m2` | repro | pass | &nbsp; |
| 1 | `bugs/mike/1-random-crash` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-res-change-basis` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-res-memleak.m2` | repro | timeout | &nbsp; |
| 1 | `bugs/mike/1-res-over-local-ring.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-res-slow-andreic.m2` | repro | timeout | &nbsp; |
| 1 | `bugs/mike/1-res-speed-Horrocks-Mumford-2.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-ringmap` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-sam-crash-SchurFunctors.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-schenck-os.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-schreyer-memleak.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-slow-division` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-slow-syz.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-space-measurement` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-support-for-matrices` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-test-files.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-uli-codim-ZZ.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-warnings` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-weibel.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-yang-2nd.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-yang-2nd.txt` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-yang-gb-examples-email.eml` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-yang-gb-examples.m2` | repro | fail | &nbsp; |
| 1 | `bugs/mike/1-yang-gb-examples.txt` | note | n/a | &nbsp; |
| 1 | `bugs/mike/1-yang-slow-subring.m2` | repro | pass-partial | &nbsp; |
| 1 | `bugs/mike/1-yang.sing` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-CC-tostring` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-Holder` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-NTL` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-SYNOPSIS-doc` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-Smith-normal-form` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-ancestor-arg-order` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-cached-maps` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-check-packages-capture` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-colon-parsing` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-compiling-libgc` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-doc-command-line-options` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-doc-misleading-error-message` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-doc-optional-args` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-document-document` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-document-local-indeterminates` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-documentation-links` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-export` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-gb-LLL-option` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-gpu-nvidia` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-height-balanced-trees` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-help-distraction` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-hom-maps` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-indexedvar-ring` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-info-dir` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-inheritance-search-order` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-installPackage` | note | n/a | [#229](https://github.com/Macaulay2/M2/issues/229) read-only files and "installPackage" |
| 2 | `bugs/dan/2-integralclosure-subrings` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-interrupting-system-calls` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-inverse-of-permutation` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-lazy-gb-for-quotient-rings` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-localDictionaries` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-master-doc-index` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-method-doc` | note | n/a | [#4545](https://github.com/Macaulay2/M2/issues/4545) "making new functions with optional arguments" should link to "method" |
| 2 | `bugs/dan/2-methodOptions` | note | n/a | [#2758](https://github.com/Macaulay2/M2/issues/2758) methodOptions, code, locate don't work on MethodFunctionSingle |
| 2 | `bugs/dan/2-mod-n-class` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-multiple-assignment-improvement` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-mysql-databases` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-net-COMMENT-formatting-bug` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-octopus-port` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-p-adic-numbers` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-pseudocode` | note | n/a | [#3408](https://github.com/Macaulay2/M2/issues/3408) Pseudocode and disassemble issues |
| 2 | `bugs/dan/2-res-ZZ` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-ringmap` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-same` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-targettypes-vs-stdint` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-toExternalString.m2` | repro | fail | [#1026](https://github.com/Macaulay2/M2/issues/1026) value and toExternalString... deprecated? |
| 2 | `bugs/dan/2-toc-index-info-doc` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-uninstallPackage` | note | n/a | [#797](https://github.com/Macaulay2/M2/issues/797) uninstallPackage doesn't remove everything |
| 2 | `bugs/dan/2-various-library-and-code-licenses` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-website-trial` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-wraparound-indices-for-matrices` | note | n/a | &nbsp; |
| 2 | `bugs/dan/2-wrapping` | note | n/a | &nbsp; |
| 2 | `bugs/mike/2-090422-Herzog.m2` | repro | fail | &nbsp; |
| 2 | `bugs/mike/2-doc-changes` | note | n/a | &nbsp; |
| 2 | `bugs/mike/2-doc-stopping-conditions` | note | n/a | &nbsp; |
| 2 | `bugs/mike/2-intersect-degreelimit.m2` | repro | pass-partial | &nbsp; |
| 2 | `bugs/mike/2-lift-realmatrix-ZZ` | note | n/a | &nbsp; |
| 2 | `bugs/mike/2-minpres-tests.m2` | repro | pass-partial | &nbsp; |
| 2 | `bugs/mike/2-monomial-overflow.m2` | repro | fail | &nbsp; |
| 2 | `bugs/mike/2-slow-mult` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-ReesAlgebra-package` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-RingElement-as-list` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-errormess-doc.m2` | repro | fail | &nbsp; |
| 3 | `bugs/dan/3-inheritance-on-classes-of-output-parameters` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-left-arrow-tag` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-maps-prune-res-trim` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-nullary-methods` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-optargs-hierarchy.m2` | repro | fail | &nbsp; |
| 3 | `bugs/dan/3-rational-approximation` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-ring-indexing.m2` | repro | pass | &nbsp; |
| 3 | `bugs/dan/3-set==set` | note | n/a | &nbsp; |
| 3 | `bugs/dan/3-sockets-fail` | note | n/a | &nbsp; |
| 3 | `bugs/mike/3-andersbuch-subring.m2` | repro | pass-partial | &nbsp; |
| 3 | `bugs/mike/3-big-syz-aknaton` | note | n/a | &nbsp; |
| 3 | `bugs/mike/3-primary-decomposition.m2` | repro | timeout | &nbsp; |
| 3 | `bugs/mike/3-res-degreelimit.m2` | repro | pass | &nbsp; |
| 4 | `bugs/dan/4-check-package` | note | n/a | [#1080](https://github.com/Macaulay2/M2/issues/1080) CMake build |
| 4 | `bugs/dan/4-documentation-documentation` | note | n/a | &nbsp; |
| 4 | `bugs/dan/4-pretty-plan` | note | n/a | &nbsp; |
| 4 | `bugs/dan/4-res-image.m2` | repro | fail | &nbsp; |
| 4 | `bugs/dan/4-stdio-line-numbers` | note | n/a | &nbsp; |
| 4 | `bugs/dan/4-wrapping` | note | n/a | &nbsp; |
| 5 | `bugs/dan/5-NTL-LLL` | note | n/a | &nbsp; |
| 5 | `bugs/dan/5-factoring-by-lifting` | note | n/a | &nbsp; |
| 5 | `bugs/dan/5-ringmaps-as-promote` | note | n/a | &nbsp; |
| 5 | `bugs/dan/5-string-variable-expansion-feature` | note | n/a | &nbsp; |
| 5 | `bugs/mike/5-ataylor.m2` | repro | pass-partial | &nbsp; |
| 5 | `bugs/mike/5-bermejo.txt` | note | n/a | &nbsp; |
| 5 | `bugs/mike/5-localgb-slow-tmp.m2` | repro | fail | &nbsp; |
| 5 | `bugs/mike/5-localgb-slow.m2` | repro | fail | &nbsp; |
| 5 | `bugs/mike/5-monomials.m2` | repro | pass | [#3459](https://github.com/Macaulay2/M2/issues/3459) matrix of exponents instead of monomials from basis |
| 5 | `bugs/mike/5-mutablemat.m2` | repro | fail | [#2192](https://github.com/Macaulay2/M2/issues/2192) Promotion of mutable matrices |
| 5 | `bugs/mike/5-rawFFLU.m2` | repro | pass | &nbsp; |
| 5 | `bugs/mike/5-res-bench.m2` | repro | timeout | &nbsp; |
| 5 | `bugs/mike/5-res-options.m2` | repro | fail | &nbsp; |
| 5 | `bugs/mike/5-topCoefficients-crash.m2` | repro | fail | &nbsp; |
| 6 | `bugs/dan/6-prune-map` | note | n/a | &nbsp; |
| 6 | `bugs/dan/6-tex-M2-filter` | note | n/a | &nbsp; |
| 6 | `bugs/mike/6-res-bigdegree.m2` | repro | timeout | &nbsp; |
| 7 | `bugs/dan/7-parens-on-left-side-of-assignment` | note | n/a | &nbsp; |
| 7 | `bugs/dan/7-value-of-expressions.m2` | repro | fail | &nbsp; |
| 7 | `bugs/mike/7-test-lapack.m2` | repro | fail | &nbsp; |
| 8 | `bugs/dan/8-leadComponent-vs-pivots` | note | n/a | &nbsp; |
| 9 | `bugs/dan/9-Seysen-vs-LLL` | note | n/a | &nbsp; |
| 9 | `bugs/dan/9-flatness-ZZ.m2` | repro | fail | &nbsp; |
| 9 | `bugs/dan/9-gb-transpose-mystery.m2` | repro | fail | &nbsp; |
| 9 | `bugs/dan/9-on-hold-ZZ-as-ZZpoly` | note | n/a | &nbsp; |
| 9 | `bugs/mike/9-optimization-ideas` | note | n/a | &nbsp; |
| &nbsp; | `bugs/LAcore/LAPACK-crash.output` | note | n/a | &nbsp; |
| &nbsp; | `bugs/LAcore/LAPACK.output` | note | n/a | &nbsp; |
| &nbsp; | `bugs/LAcore/NOLAPACK.output` | note | n/a | &nbsp; |
| &nbsp; | `bugs/LAcore/TO-DO-LAcore.md` | note | n/a | &nbsp; |
| &nbsp; | `bugs/LAcore/bugLAPACK.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/LAcore/testEigenvectors.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/LAcore/testLAPACKvsNOLAPACK.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/LAcore/testSVD.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/LAcore/testSolve.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/README` | note | n/a | [#1089](https://github.com/Macaulay2/M2/issues/1089) Delete Macaulay2/compat |
| &nbsp; | `bugs/anton/DOC/Spaces.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/Dmodules/h-arr.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/EXPERIMENTS/SLP-experiments.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/LINEAR-ALGEBRA/CCsubRR.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/LINEAR-ALGEBRA/primaryDecomposition.m2` | repro | pass | [#156](https://github.com/Macaulay2/M2/issues/156) primaryDecomposition and minimalPresentation |
| &nbsp; | `bugs/anton/MEMORY-LEAKS-old/mem-leak-sparse-system.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS-old/track.gc-bug.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/NAG-leaks.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/eigen-leaks.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/gb-leaks.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/gf-bug.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/hilb-leaks.m2` | repro | timeout | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/mike-NAG.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/monoid-leaks.m2` | repro | timeout | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/test-mem-leaks.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/MEMORY-LEAKS/track-leak.m2` | repro | timeout | &nbsp; |
| &nbsp; | `bugs/anton/NOTES.M2` | note | n/a | &nbsp; |
| &nbsp; | `bugs/anton/NOTES/mahrud.txt` | note | n/a | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/_numericalID_example.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/_numericalIntersection_example.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/mem-leak-1.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/mem-leak-det.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/mem-leak-sparse-system.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/parallelNumericalImplicitization.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/solveSystem-space-quadric.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/NumericalAG/track-predictor.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/PARALLEL/justin.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/PRERELEASE-CHECKS/numerical-packages-checks.m2` | repro | timeout | &nbsp; |
| &nbsp; | `bugs/anton/SLP/SLP-experiments.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/SLP/sparseMonodromySolve.m2` | repro | fail | [#1962](https://github.com/Macaulay2/M2/issues/1962) potentialE requires target solution count |
| &nbsp; | `bugs/anton/pari-to-flint/isprime-tests.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/anton/reallocate-heap-to-GC-heap/ZZ-test.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/anton/reallocate-heap-to-GC-heap/res-f4.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/anton/roots.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/dan/IDEAS` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/M2mem-framepointer-checking-idea` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/Makefile` | note | n/a | [#515](https://github.com/Macaulay2/M2/issues/515) URL change in libraries/mpir/Makefile? |
| &nbsp; | `bugs/dan/Option++Option.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/dan/allVars` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/doc-changes` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/fetching-doc-from-another-package` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/formatting-TABLEs` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/html-validation` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/inheritance-and-immutability` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/monoid-method-caching-and-use` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/outputs-in-doc` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/parsing-a-line` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/positions-in-source` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/self-replicating-program.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/dan/skipping-doc-reading` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/string-keys-in-rings` | note | n/a | &nbsp; |
| &nbsp; | `bugs/dan/wrapping-subquotients` | note | n/a | &nbsp; |
| &nbsp; | `bugs/gfurnish/TODO` | note | n/a | [#1890](https://github.com/Macaulay2/M2/issues/1890) Increasing memory limits & reducing memory usage |
| &nbsp; | `bugs/mike/Makefile` | note | n/a | [#515](https://github.com/Macaulay2/M2/issues/515) URL change in libraries/mpir/Makefile? |
| &nbsp; | `bugs/mike/PROJECTS` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/ReesAlgebra2.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/bench-1-rees.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/bench-assoc34.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/bench-assoc34.sing` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/bench-implicit.m2` | repro | timeout | &nbsp; |
| &nbsp; | `bugs/mike/debugging-singular-code` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/decompose5.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/decompose6.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/det-bug-from-ggsmith.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/devel-0-bernd-comaximalminors.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/devel-1-f4.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/devel-1-fracgb.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/devel-2-toricgb.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/devel-f4-eg1.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/devel-gb-walk.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/example-freitag.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/gb-test-examples-2.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/gb-test-examples.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/GameOfLife.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/GameOfLifeSimple.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/gb-elim-examples.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/gb-one-minute.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/gb-random-18.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/gb-simple-tests.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/master-f4` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/run-gb-elim-examples.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/gbB-refactor-gb/run-gb-one-minute.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/git-468-memory-leak.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/git-issue-1742.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/git-issue-190.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/git-issue-xxx-slow-res.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/git-issue146.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/git-issue242-243-244.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/git-issue316.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/git-issue475-openblas.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/gor-tests.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/goren.m2` | repro | pass-partial | &nbsp; |
| &nbsp; | `bugs/mike/intersect.m2-hold` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/localgb.m2-hold` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/localgb.sing` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/mike-notes` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/monorder.sing` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/new-mathic.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/oneStep3.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/playing-Graphics.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/roots.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/run-gb-test-examples.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/svn-notes` | note | n/a | &nbsp; |
| &nbsp; | `bugs/mike/test-dmat.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/test-flint.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/test-gbZZ.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/test-memleaks-mutablemat.m2` | repro | pass | &nbsp; |
| &nbsp; | `bugs/mike/test-monordering.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/test-pivots.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/wish-betti-numbers.m2` | repro | fail | &nbsp; |
| &nbsp; | `bugs/mike/wish-ragnar.m2` | repro | pass-partial | &nbsp; |

