Fixed. The line is gone.

@DanGrayson reordered it first, in `690d18157b` (2021-01-06, *"put /usr/local before /usr in
configure script"*):

```diff
-LDFLAGS="$LDFLAGS -L/usr/lib -L/usr/local/lib"
+LDFLAGS="$LDFLAGS -L/usr/local/lib -L/usr/lib"
```

and then removed it outright three days later, in `e2b8aaeefc` (2021-01-09, *"don't refer to
/usr/local/include or /usr/include"*). `configure.ac` contains no `-L/usr/lib` and no
`-L/usr/local/lib` today.

One thing worth saying so nobody re-opens this after a grep: `/usr/lib` does still appear in
`configure.ac`, at lines 103–105, but only to read `/usr/lib/os-release` for distribution
detection. That has nothing to do with the linker search path and does not affect a multilib
layout.
