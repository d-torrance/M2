Fixed, and by you. `97e7f58011` adds the two missing `mpz_clear` calls to the early-return branch of
`GBRing::gbvector_combine_lead_terms_ZZ` — the `gbring.cpp:1199` frame in the trace above — and it
shipped in #2501, first released in 1.21. The wider sweep was #2443 (1.20), which cleared or
reallocated mpz objects across `ZZ.cpp`, `ZZp.cpp`, `aring-RRi.hpp`, `aring-gf-givaro.cpp`,
`aring-glue.hpp` and `cra.cpp`, covering both the `RingQQ::fraction` site from your second log and the
Givaro cases from your third.

Rechecked on 1.26.06 with valgrind 3.22. The reproducer in the body now gives

```
definitely lost: 0 bytes in 0 blocks
indirectly lost: 0 bytes in 0 blocks
```

and so does a run leaning on `RingQQ::fraction` — 300 determinants and scalar multiples over
`QQ[a..d]`, plus 200 fractions in a quotient. Both runs report an identical `possibly lost: 13,056
bytes in 24 blocks`, which is startup-constant and not this.

Not re-run: the `ctest -T memcheck` records from March 2021 (`ARingQQGMP.display`,
`FreeAlgebra.quotientArithmetic`, `RingZZmod101.negate`). Their source files were touched by the same
two pull requests, but that is an inference rather than a measurement — so if any of those still show
something, this is worth reopening.
