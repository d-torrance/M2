Closing this: the example is gone, and you removed it yourself.

Confirming first that it was never a complex —

```m2
i4 : C = chainComplex{matrix{{x*dx, y^2+dx}}, matrix{{dx*dy},{y^2*dy^3}}};

i5 : C.dd^2 == 0

o5 = false

i6 : (matrix{{x*dx, y^2+dx}}) * (matrix{{dx*dy},{y^2*dy^3}})

o6 = | y4dy^3+y2dxdy^3+6y3dy^2+xdx^2dy+6y2dy+dxdy |
```

so the composite is plainly nonzero and the answer to "is it supposed to be?" is that as written
it could not be.

`WeylAlgebras/DOC/basics.m2:150-152` are now commented out —

```
      --C = chainComplex{matrix{{x*dx, y^2+dx}},matrix{{dx*dy},{y^2*dy^3}}}
      --FC = Fourier C
      --FC.dd
```

— in [`60532ed71d`](https://github.com/Macaulay2/M2/commit/60532ed71d), *"Minor changes to many
packages to get them working with Complexes rather than OldChainComplexes"*, 2026-01-27, which is
the same migration work that led you to notice this.

**Two things this close does not cover**, so they are not lost by it. Commenting out is disabling
rather than repairing: the `Fourier` documentation no longer demonstrates the operation on a
complex at all, and the three dead lines are still in the file. If a correct example is wanted
there, that is new work rather than this issue.

Reopen if you would rather it stay open for that.
