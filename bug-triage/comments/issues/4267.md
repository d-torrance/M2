Done, in [#4269](https://github.com/Macaulay2/M2/pull/4269) — commit
[`75771ac107`](https://github.com/Macaulay2/M2/commit/75771ac107), *"Raise an error when comparing
lists of different types"*, merged 2026-05-26. It never said `Closes #4267`, which is the only
reason this is still open.

Both cases now behave the way Anton and Mike wanted, with one message rather than the old
asymmetry:

```m2
i1 : {1, 2, 3} == (1, 2, 3)
stdio:1:10:(3):[1]: error: expected lists of the same class

i2 : {1, 2, 3} == [1, 2, 3]
stdio:2:10:(3):[1]: error: expected lists of the same class
```

The check is in `m2/lists.m2:60` and `d/actors3.d:194-208`. Closing.
