A status update on the analysis above, since one of the three failure modes it describes can no
longer fire.

**The documentation example is gone** — `0181eacfee`, *"Fix LocalRings export and stabilize
ThreadedGB docs"* ([PR #4345](https://github.com/Macaulay2/M2/pull/4345), merged 2026-06-09) removed
the `g#(1,2)` example from the `ThreadedGB` node outright, and changed `H#(0,1)` to `H#0` in the
`tgb` node. So the failure reported on 2024-11-20 cannot recur: the documentation no longer depends
on a lineage key that exists only if the scheduling happens to go one way.

**The race is unchanged.** `ThreadedGB.m2:269` still reads `values endGB` at task execution time
rather than at scheduling time, and `endGB#?"trivial"` is still read without a lock at `:115`,
`:136` and `:269`.

**The two test failures are unchanged.** The tests still assert on the non-deterministic key
`H#(0,1)`, at `:646` and `:655` — which are the failures originally reported here.

So the documentation symptom was resolved by avoidance rather than by fixing the underlying race,
and the two test symptoms remain live.
