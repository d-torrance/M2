Closing: this no longer happens. On 1.26.06 the two lines from the report run clean —
`importFrom_Core {"raw"}` returns `{raw}` and `clearAll()` completes silently.

**I could not identify what fixed it**, so this closes on behaviour rather than on a commit, and
should be reopened if it comes back. What I was able to rule out: it is not simply @mahrud's rewrite
of `importFrom` in `e0408656dd` ([PR #3382](https://github.com/Macaulay2/M2/pull/3382), merged
2024-08-05), which replaced the `#?` test with a `try`/`else`. Defining the pre-#3382 version of
`importFrom` by hand and running the same two lines produces only a `warning: 'raw' redefined` now,
not the error above — so whatever changed is downstream of `importFrom` itself.
