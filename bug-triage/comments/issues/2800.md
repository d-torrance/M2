Closing: this was answered and fixed in the thread itself, and nobody came back to shut it.

@kschwede's diagnosis holds — these functions expect homogeneous rings — and the pull request he
promised in the comment above landed the same day. `e08375a327`, in PR #2803, adds two homogeneity
checks to the `rationalMapping` constructor, now at `RationalMaps.m2:107-108`. On 1.26.06 the
script in the report stops at the point where the problem actually is:

```m2
i6 : phi2 = rationalMapping(F, A2, {x, z})
stdio:6:22:(3):[1]: error: rationalMapping: the target should be homogeneous
```

so `phi2` is never built and the confusing failures from `inverseOfMap` and `baseLocusOfMap` are
unreachable.

@AlicaMir — to answer what you asked: not a bug in the mathematics, but the uninformative error you
got was one, and that is what got fixed.
