All three still fail on 1.26.06, from the same places: `isPrime I` and `minimalPrimes I` raise
`no applicable strategy for (minimalPrimes,Ideal)` at `MinimalPrimes.m2:239`, and `codim I` raises
`expected an affine ring (consider Generic=>true to work over QQ)` at `monideal.m2:114`.

Worth making @jchen419's point about the workaround concrete, since the error message recommends it:

```m2
i : codim(I, Generic => true)

o = 4
```

`I` contains 2, so over `QQ` it is the unit ideal — the suggestion answers a question about a
different ideal rather than giving a usable value for this one.

The `codim` half also has a second open row that neither issue links to: #3557, where `rank` over
`ZZ` fails with this same error and there is no way to pass `Generic => true` through it at all. Same
gap, reached from a much more ordinary direction.
