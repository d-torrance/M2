Closing this as not planned, on the strength of the answer already in the thread. The nesting is
still there — `SubalgebraBases/Old-SubalgebraBases/SubalgebraBases` — so the Lintian warning still
fires on every Debian build, but when @timduff35 asked whether it needed fixing the answer was that
it is one of the more pedantic Lintian tags and would not affect the package's standing in Debian.
Five years on, that reads as settled rather than pending.

Nothing here breaks: `Old-SubalgebraBases` is an auxiliary directory and the inner
`SubalgebraBases` holds the archived sagbi sources, so the repeated segment is cosmetic. Worth
reopening if the layout ever confuses package loading, or if a future Lintian profile promotes the
tag to something that blocks an upload.
