Closing: this was done in [PR #4322](https://github.com/Macaulay2/M2/pull/4322), merged 2026-06-04.

@kellerlv's commit `d5c4d0c588` names both halves of the ask — *"Add TEST blocks for the
previously untested export `socleDegrees`"* and *"make the `socleDegrees` documentation example
actually call `socleDegrees`"*. The package now carries

```m2
assert(socleDegrees(coker vars R) == {0})
assert(socleDegrees(R^1) == {})
assert(socleDegrees(R^1/(ideal(x,y,z)*ideal(x,y))) == {1,1})
```

plus a test that a non-field coefficient ring is rejected, and the documentation node has three
`Example` lines that call the function. `check "MCMApproximations"` runs 15 tests, up from 11.
