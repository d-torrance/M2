Closing this as no longer reproducible. Since there were no numbers in the report, the thing to
check is the invariant you described — that the two lines used to run at the same speed and the
second had become much slower. It holds again, and it keeps holding as the example is scaled up.
On 1.26.06-40-gd8e86d689d:

| | `decompose ideal(x^n-1)` | `decompose ideal(x^n-y^n)` | `Strategy => "Legacy"` |
| --- | --- | --- | --- |
| n = 5 | 0.065s | 0.062s | 0.015s |
| n = 11 | 0.123s | | 0.049s |
| n = 17 | 0.262s | 0.272s | 0.196s |

n = 11 is your follow-up case. The default strategy is still 1.3–4× `Legacy`, but at these
absolute times there is no room for the difference you saw.

**I could not identify what fixed it**, and would rather say so than leave the impression that
nothing did. The issue timeline has no cross-referenced pull requests; searching pull requests for
the symptom turns up nothing about performance in the months after the report; and the only
`MinimalPrimes` change in that window,
[`7ae2a9acb0`](https://github.com/Macaulay2/M2/commit/7ae2a9acb0), guards a Hilbert-function hint
against a `degreesRing` mismatch — a correctness fix that would remove a shortcut rather than add
one.

So this closes on behaviour rather than on a commit. If you have an input that still shows it,
please reopen.
