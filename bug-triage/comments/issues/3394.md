Closing: fixed by @mahrud in `7c8a261b1c`, *"moved documentation of intervals"*
([PR #3672](https://github.com/Macaulay2/M2/pull/3672), merged 2025-02-24). All three symptoms are
gone on 1.26.06:

- `methods midpoint` was empty; it now lists nine methods, including `(midpoint, RRi)` and
  `(midpoint, CCi)`.
- `methods RRi` now includes `(left, RRi)`, `(right, RRi)` and `(midpoint, RRi)` among its 106
  entries.
- The rendered `RRi` page links `left`, `right` and `midpoint` under its *"Methods that use a real
  interval"* section.

The side request in the second comment also happened: `doc15.m2` is gone, and the material lives in
`Macaulay2Doc/doc_intervals.m2`.

Not covered by that, and worth its own issue if it is still wanted: the general wish in the original
post that a compiled function missing from `typicalvalues` should raise an error, or at least be
enumerable, rather than silently failing to appear in `methods`.
