Closing as fixed. @tom111's *"I think I have fixed it in my pull request"* above was right, and
the pull request is **#727** — commit `280acceb60`, "Fix issue #724 with binomialIsPrime not
working right", merged 2018-01-14, six days after the report. It is the only commit to
`Binomials.m2` in that window. Nobody closed the issue afterwards.

On 1.26.06-40-gd8e86d689d the reported case is gone, and the neighbouring cases that would
catch an overcorrection are right too:

```m2
i1 : needsPackage "Binomials"; R = QQ[x,y];

i3 : binomialIsPrime ideal(x^2)

o3 = false

i4 : binomialIsPrime ideal(x^2-y^2)

o4 = false

i5 : binomialIsPrime ideal(x-y)

o5 = true

i6 : binomialIsPrime ideal(x*y)

o6 = false
```

`ideal(x^2)` and `ideal(x*y)` are the monomial-containing cases @foxjwill guessed were being
mishandled, and both now answer `false`; `ideal(x-y)` still answers `true`, so the fix did not
simply make the function pessimistic.

Thanks @foxjwill for the report and @tom111 for the fix.
