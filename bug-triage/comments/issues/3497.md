**The segfault is fixed, and nothing here says so.** #3499 (merged 2024-10-25) added the emptiness
check to `rawIdealReorder` at `e/interface/factory.cpp:788`, so on 1.26.06 the original transcript
gives a clean error instead of a crash. The cryptic message from 2024-10-30 is still there, and it
turns out to be wider than this thread records:

```m2
i1 : R = QQ[x];

i2 : I = ideal R;

i3 : irreducibleCharacteristicSeries I
stdio:3:31:(3):[1]: error: expected at least one generator

i4 : debug Core;

i5 : factoryGood ZZ
stdio:5:11:(3):[1]: error: expected argument 1 to be a hash table

i6 : factoryGood QQ
stdio:6:11:(3):[1]: error: expected argument 1 to be a hash table

i7 : factoryGood(GF 4)
stdio:7:11:(3):[1]: error: expected argument 1 to be a hash table

i8 : factoryGood(QQ[x])

o8 = true
```

`factoryGood` is one line, `m2/enginering.m2:343`:

```m2
factoryGood = R -> factoryAlmostGood R and not (options R).Inverses
```

and `options R` is `null` for any ring that is not a polynomial or quotient ring, so it *errors
instead of answering* for every base ring — `ZZ/5` behaves like the three above. That is what
`irreducibleCharacteristicSeries ideal()` reaches through the guard at `m2/factor.m2:181`, since a
bare `ideal()` is over `ZZ`.

The only other caller, `frac EngineRing` at `enginering.m2:350`, escapes by accident rather than by
design: `:345` returns early when the ring is a field, and `:346` dereferences the same `null`
first. So `factoryGood` looks like the single thing to fix here.

Leaving this open for that and for the `({},id_R)` question.
