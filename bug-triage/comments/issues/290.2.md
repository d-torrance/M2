**Correction to my comment above.** I claimed the capability gap was tracked nowhere else
and retitled this issue to be that tracker. Both were wrong. **#292** — same reporter, same
day — already carries it, and carries it better. I have reverted the title and am closing
this as fixed, which is what it should have been.

### What #292 has that this issue does not

The diagnosis, and the decision. @mikestillman, in that thread on 5 June 2015:

> It turns out that local ring computations over ZZ are not implemented: the minimalization
> routine called does not work in this case. **Eventually, we should add this.** Should we
> give an error now (local term orders in polynomial rings over ZZ not yet supported?), or
> is it important enough to do right now?

with @jakobkroeker replying *"I'm fine with an error for now (release 1.8)"* and @DanGrayson
*"Presumably, if any wanted it before now, they would have told us before now."* So the
error that this issue now hits was proposed, agreed and scoped in #292, and #292 also
records *where* the obstacle is — not in the Gröbner basis computation, which Mike notes
"completes the GB internally", but in the minimalization step afterwards.

Everything I wrote above about the fix chain stands. Only the conclusion about where the
remaining question lives was wrong.

### The family, for anyone arriving here later

All four filed by @jakobkroeker on 5 June 2015:

| | | |
| --- | --- | --- |
| #290 | this one — `Weights`, non-idempotent `gb` | closed, fixed by the guard |
| #291 | `RevLex`, non-idempotent `gb` | closed, fixed by the guard |
| #292 | hangs in minimalization | **open** — tracks the missing capability |
| #293 | standard bases for local rings generally, vs Singular's `std()` | open, broader ask over any field |

### How the mistake happened

I searched every issue body and all 16,874 cached comments, but for "local" within a few
words of "gb" or "Gröbner" — and #292 says "local ring computations over ZZ" and "local term
orders in polynomial rings over ZZ", which never put those words together. The
same-author-same-day list I had already generated paired #290 with #291, #291 with #292 and
#292 with #293, and I did not read it before writing the comment above. Both have been
fixed, but the claim was mine and it was unfounded.
