Still reproduces, and it turns out the intended behaviour is already written down — the code
contradicts it visibly, in this function's own documentation.

The test is still skipped: `StatGraphs.m2:1517` opens with
`-- no-check-flag (see discussion at https://github.com/Macaulay2/M2/pull/3628)`, so
`captureTestResult` passes over it on every architecture. Running its body on 1.26.06 gives
`isCyclic G == true`, so the assertion `not isCyclic G` fails. The directed part on its own is
acyclic — `isCyclic D` is `false` for `digraph{{2,1},{3,1},{7,8}}` — so the `true` comes from the
undirected/bidirected handling.

Now the part worth adding. The doc node for `(isCyclic, MixedGraph)` is headlined *"check whether a
mixed graph contains a directed cycle"*, and **its first example is the same graph as the disabled
test**, introduced by the sentence *"In the following example, there are no directed cycles."* The
built example output disagrees:

```
i5 : isCyclic G

o5 = true
```

All four examples in that node return `true`, including the one the prose says has none.

So the disabled test and the documentation prose agree with each other, and the implementation
(`StatGraphs.m2:253-274`, which contracts connected components into an adjacency matrix and calls
`isCyclic` on the resulting digraph) disagrees with both. That makes this a defect against a stated
contract rather than an open question about what the behaviour should be.

Whoever picks it up might also fix the comment at `:252`, which reads "Check whether a MixedGraph is
cyclic, i.e., does not contain any directed cycles".
