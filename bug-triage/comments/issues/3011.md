Closing: gone in [PR #3672](https://github.com/Macaulay2/M2/pull/3672), *"Organizing Macaulay2Doc
#1"*, merged 2025-02-24.

Both halves of the report are fixed. There is no node keyed `"graded modules"` anywhere in
`M2/Macaulay2/` any more, and `_graded_spmodules.html` is absent from the 1.26.06 documentation with
nothing linking to it. The two dangling `SeeAlso` entries are gone too — in `ov_modules.m2` the
`free modules` node now has it commented out, and the one surviving occurrence is a plain string
used as a section heading inside a `Subnodes` list rather than a `TO` link.

@mahrud's *"I think it was an aspirational node"* turned out to be the epitaph.
