Closing as a duplicate of #1518, which is the same missing method and two years older.

Both still fail on 1.26.06, from the same place, with the same message:

```
error: kernel: no method implemented for this type of ring map    -- methods.m2:695
```

so the wording quoted in this issue has drifted to the generic no-method text, but nothing was
implemented. Over `QQ` the same map gives `ideal ()`, so it is specifically the `ZZ` base.

I have moved the part of this issue that #1518 lacks over there: that `graphIdeal f` also fails,
with `expected an affine ring` from `newring.m2:108`, and your point that this ought to be easy since
the graph is not defined differently over the integers. Worth following #1518 rather than this one —
and reopen if you would rather the symmetric-function example live on its own row.
