Both halves of this are in place, so closing.

The branch is gone — `git ls-remote origin refs/heads/gh-pages` comes back empty, so the 200 MB of
weekly-rewritten HTML is no longer in this repository. That was the actual complaint here.

And the engine documentation is online and maintained: the draft linked above,
[`d-torrance/M2-engine-docs`](https://github.com/d-torrance/M2-engine-docs), publishes to
<https://d-torrance.github.io/M2-engine-docs/>, which is live and was last built three days ago.

Being straight about what that is and isn't: it is GitHub Pages under a personal account, not
macaulay2.com as the title asks, and it uses git — which is what @mahrud said he would rather avoid,
preferring Dan's existing server. That preference was not followed, and it is worth reopening or
filing fresh if hosting it on macaulay2.com still matters. But the repository is no longer carrying
the weight, and the docs are reachable, which is what this was for.
