Closing this: it was fixed by the reporter, five years ago, and nobody came back to say so.

The diagnosis in the body was right, and @jieaosong applied it themselves in
[#1989](https://github.com/Macaulay2/M2/pull/1989) —
[`4b2aa89ff4`](https://github.com/Macaulay2/M2/commit/4b2aa89ff4), merged 2021-03-11 — turning
`RationalPoints.m2:175` into

```m2
newstuff := apply(toList (set els)^**n, deepSplice);
```

with `assert(#rationalPoints Grassmannian(1,3,CoefficientRing=>ZZ/2) == 36)` added to the package
test in the same commit, so there is a regression test for it. The example in the body returns
36 points on 1.26.06-40-gd8e86d689d.

The other question raised further down the thread — whether the old package should be retired in
favour of `RationalPoints2` — is #1986 and stays open.
