Fixed in 1.23. Both halves of this landed in `17bc5d4ef5` ("updated documentation of tensor and
flip"), which shipped in #3118 — `tensor-doc.m2` now has

```
Node
  Key
    (tensor, Matrix, Matrix)
    (symbol**, Matrix, Matrix)
  Headline
    tensor product
  Usage
    f ** g
    tensor(f, g)
```

so `tensor(Matrix,Matrix)` is documented, and it shares a node with the operator spelling rather than
sitting beside it. Three of the nine nodes in that file are now keyed on both forms together, which is
the merge this asked for.

Nothing here linked back to this issue, which is why it stayed open.
