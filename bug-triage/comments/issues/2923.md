Closing: @DanGrayson's *"let's remove the corresponding test, on the grounds that it uses too much
memory"* was carried out, and has since been re-tested and confirmed.

@mahrud's `b4305bc171` ([PR #3288](https://github.com/Macaulay2/M2/pull/3288), merged 2024-08-05)
commented out the `(3,5)` assertions in the Example 3.9 test block — the `check(26)` that was
failing here.

It then got a second look. At the Atlanta workshop `412afb1d4a` re-enabled them on the theory that
the old skip was a time-budget matter; `e1eadece57` re-disabled them the next day after the macOS-15
runner produced exactly this issue's `*** out of memory trying to allocate 80 bytes, exiting ***` at
test #26, and left a note in the block recording that the constraint is memory rather than time,
that autotools-ubuntu passes it in about 12 seconds, and that it should be revived only when macOS
runners gain RAM or `genericDeterminantalSymbolicPower` gets cheaper. The `(3,3)` and `(3,4)` cases
remain enabled.
