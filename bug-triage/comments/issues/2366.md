Closing this, since it was a question rather than a defect and it was answered here on the day it was
asked: `topLevelMode = Standard` restores the plain output. Recording that in the close so a search
lands on it.

@pzinn's point remains a fair one and is not dismissed by closing — that the better fix is for
packages to define `texMath` for their own types, rather than for users to turn TeX rendering off.
That is work for package authors, though, and not something this issue can track. The same friction
shows up in #1924, where canned examples render badly in WebApp mode.
