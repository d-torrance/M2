Duplicate of #1802, filed four days earlier: the same gcc-10 internal compiler error during
RTL pass `reload`, on the same Ubuntu 20.10 armhf PPA builds, in a different translation unit.
That issue had already collected a third instance — `eigen.cpp` — as a comment rather than a
new report, and it closed in September 2025 with the general answer, that sometimes the builder
does not have enough memory for the compiler to finish. That applies here unchanged: the ICE
lands in register allocation on one of the heavier template instantiations in the file,
`MutableMat<DMat<ARingCCC>>::createSLEvaluator`, on a 32-bit host.

`eigen.cpp`, which is the one that kept recurring, has its own issue in #2223.
