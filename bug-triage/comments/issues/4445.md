Fixed by @pzinn in [`b7bba66f45`](https://github.com/Macaulay2/M2/commit/b7bba66f45) — whose message
is just *"fix #4445"* — merged as part of [#4456](https://github.com/Macaulay2/M2/pull/4456)
(*"factor.m2 minor fixes"*) on 2026-07-23. One line in `m2/factor.m2`:

```diff
-	RM' := (baseRing R) RM.monoid;
+	RM' := (baseRing R) monoid RM;
```

Both examples from this thread work on 1.26.06-40-gd8e86d689d:

```m2
i3 : factor(x^2-y^2)

o3 = (- y + x)(y + x)

o3 : Expression of class Product

i4 : factor(x^3-y^3)

                2          2
o4 = (- y + x)(y  + x*y + x )

o4 : Expression of class Product
```

Neither the commit nor the PR said `closes #4445`, which is the only reason this stayed open.
Closing.
