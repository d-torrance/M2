Still there on 1.26.06-40-gd8e86d689d — the entry's `toString` is 603662 characters now — and
@d-torrance's zero-denominator `QQ` comes back too.

There is a smaller version that needs no `frac` and produces **no garbage at all**, which may be the
more useful one to fix against:

```m2
i1 : S = QQ[x][y];

i2 : use S;

i3 : a = mutableMatrix{{x}};

i4 : b = mutableMatrix{{y}};

i5 : (ring a_(0,0), ring b_(0,0))

o5 = (QQ[x], S)

o5 : Sequence

i6 : a*b

o6 = | x2 |

o6 : MutableMatrix

i7 : matrix{{x}} * matrix{{y}}

o7 = | xy |

             1      1
o7 : Matrix S  <-- S
```

`x^2` where the immutable product is `x*y`. Line `i5` is the part worth looking at: `a`'s entry sits
in the coefficient ring `QQ[x]` while `b`'s sits in `S`, so nothing promotes the two to a common
ring, and the multiplication then reads one factor's variable as the other's.

That looks like the same missing promotion as the original example, just landing on a monomial
instead of on a nonsense rational, so it might be worth checking that a fix covers both — this one
gives a wrong answer with nothing about the output to suggest anything went wrong.
