Noting a second report of this, now closed into here:
[#4504](https://github.com/Macaulay2/M2/issues/4504) came out of the pre-GitHub `bugs/` tree, where
Dan Grayson had written down a concrete way out of the confusion @eisenbud describes above —

> we should rename `minimalPresentation` to `minimizePresentation` and then use the name
> `minimalPresentation` for the presentation of that.

A verb for the operation, and the freed noun for the object it produces. That is the piece this
thread was missing; the closed issue has the full note.

Still current, for the record:

```m2
i1 : R = QQ[x,y,z]/(x-y);

i2 : ring minimalPresentation ideal(x*z,y^2)

o2 = QQ[y..z]
```

`minimizePresentation` does not exist, so nothing has moved on either side.
