This works now, so closing — but with a check that it is really your example.

I rebuilt the monomial ideal from the PDF and confirmed the complex is the one you had: not just the
count, but all 24 of the facets you printed, matching as a set. It is pure of dimension 7, and
`codim In` and `pdim R/In` are both 7, so it is Cohen–Macaulay.

On version 1.26.06:

```m2
i5 : isShellable D

o5 = true
```

which agrees with the SageMath answer you mentioned, so your expectation that a complex coming from
a lattice should be shellable was right and M2 no longer disagrees. It is not a lucky facet ordering,
either — `shellingOrder(D, Random => true)` found a shelling in 30 out of 30 shuffled orders.

Being honest about one thing: I could not identify which change fixed it. `SimplicialDecomposability`
itself has had no functional change since 2022, so it came from elsewhere. If you still have the
original session and `isShellable` misbehaves on your actual ideal rather than my reconstruction of
it, please reopen and paste the ring and the ideal directly — the PDF was enough to rebuild the
complex but not the polyomino it came from.
