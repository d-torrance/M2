Still reproduces on 1.26.06, unchanged. `prune coker phi` gives exactly the presentation posted
above, and the clearest defect in it is the zero row:

```m2
i : P = presentation prune coker phi

o = {0} | f_1 f_2 0    0       |
    {1} | -3  0   f_2  -f_1f_2 |
    {2} | 0   -3  -f_1 f_1^2   |
    {3} | 0   0   0    0       |

i : select(numrows P, i -> P^{i} == 0)

o = {3}
```

The comparison in this issue needs respelling, since `PruneComplex` was removed in 1.26.05 with its
functionality folded into `Complexes`. The current equivalent of `coker(pruneComplex res M).dd_1` is:

```m2
i : presentation prune coker (minimize freeResolution M).dd_1

o = {0} | f_1 f_2 0    |
    {1} | -3  0   f_2  |
    {2} | 0   -3  -f_1 |
    {3} | 0   0   0    |
```

which is the same 4x3 you got in 2022. So the contrast still stands as originally posted — the
`Complexes` route gets the smaller answer and Core `prune` does not — and your conclusion that the
bug is in the Core unit-pruning code is unaffected.
