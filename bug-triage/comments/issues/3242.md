Closing as a duplicate of #1456, which @d-torrance suspected above — the stack traces are the same
two frames of the same function:

| | frame 3 | frame 4 |
| --- | --- | --- |
| #1456 (2020) | `SLP<ComplexField>::concatenate` `NAG.cpp:382` | `PathTracker::make` `NAG.cpp:1537` |
| this issue (2024) | `SLP<ComplexField>::concatenate` `NAG.cpp:393` | `PathTracker::make` `NAG.cpp:1548` |

Both offsets are shifted by exactly 11 lines over the four years between the reports, and at both
dates those lines are `memcpy` calls inside `concatenate`. The entry points differ —
`pseudoWitnessSet` in `NumericalImplicitization` there, `solveDecomposableSystem` in
`DecomposableSparseSystems` here — but the crash site is one place. (The function is now at
`e/NAG/NAG.cpp:353`, after the engine directory was reorganised.)

#1456 is the better home for it, being four years older and having accumulated the evidence:
sightings on x86-64, s390x, armel and arm64, and @d-torrance's assessment that *"it's very, very
rare"* — he had seen it once in many, many Debian builds.

That also speaks to the question this thread ends on, *"I still don't know why this is crashing only
on my branch"*. On #1456's evidence it is not branch-specific: it is a rare, timing-dependent crash
that has appeared across four architectures and several years of builds, so seeing it in one fork's
Actions and not another's is sampling rather than cause.

Carried over to #1456: this issue contributes a second, cheaper route to the crash site, which is
worth having for anyone trying to build a reliable reproducer.
