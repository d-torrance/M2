Closed as wontfix, with the reasoning kept here since the file reads as an unaddressed request.

The file asks to remove the export list from the bottom of a package's documentation page and put a
link to the table of contents there instead. Both halves are answered:

- **the link already exists** — a package page carries its table-of-contents link, so the substitution
  the file proposes would remove content and add something already present;
- **the export list is considered useful** in its own right: it is the one place that shows, on the
  package's own page, what the package brings into scope.

So nothing is outstanding here. Recorded rather than left silent because the file itself gives no hint
that the link was ever added.
