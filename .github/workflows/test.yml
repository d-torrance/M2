name: test git branches
on:
  push:
    branches:
      - workflow-test

default:
  run:
    working-directory: M2/BUILD/build

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test git stuff
        run: |
          pwd
          git status
          git branches
