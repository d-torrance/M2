Description: Fix nodoc build.
Author: Doug Torrance <dtorrance@piedmont.edu>
Last-Update: 2021-01-25

--- a/M2/Macaulay2/m2/installPackage.m2
+++ b/M2/Macaulay2/m2/installPackage.m2
@@ -783,6 +783,7 @@
     if not hadError then (
 	libDir := pkg#"package prefix" | replace("PKG", pkg#"pkgname", installLayout#"packagelib");
 	iname := libDir|".installed";
+	if not fileExists libDir then makeDirectory libDir;
 	iname << close;
 	verboseLog("file created: ", minimizeFilename iname);
 	);

--- a/M2/Macaulay2/packages/Makefile.in
+++ b/M2/Macaulay2/packages/Makefile.in
@@ -78,7 +78,8 @@
 	@echo "make: Leaving directory \`$(shell pwd)/$1-temporary'"
 	ls -l $1-temporary
 	rm -rf $1-temporary
-	install-info --info-dir=@pre_infodir@ @pre_infodir@/$1.info
+	if [ -e @pre_infodir@/$1.info ]; then \
+		install-info --info-dir=@pre_infodir@ @pre_infodir@/$1.info; fi
 	@ [ "${IgnoreExampleErrors}" = true -o -f $$@ ] || (echo error: file $$@ not made by installPackage >&2; false)
 check-info::@pre_infodir@/$1.info.errors
 @pre_infodir@/$1.info.errors: @pre_infodir@/$1.info
--- a/M2/Macaulay2/packages/FourTiTwo.m2
+++ b/M2/Macaulay2/packages/FourTiTwo.m2
@@ -314,7 +314,7 @@
           toBinomial
      	  (toBinomial, Matrix, Ring)	  
      Headline
-     	  creates a toric ideal from a given set of exponents of its generators; equivalent to "output --binomials" in 4ti2
+     	  creates a toric ideal from a given set of exponents of its generators
      Usage
      	  toBinomial(M,R)
      Inputs
@@ -325,7 +325,8 @@
      	  I: Ideal
      Description
      	  Text
-	       Returns the ideal in the ring {\tt R} generated by the binomials corresponding to rows of {\tt M}
+	       Returns the ideal in the ring {\tt R} generated by the binomials corresponding to rows of {\tt M}.
+	       Equivalent to "output --binomials" in 4ti2.
 	  Example
 	       A = matrix "1,1,1,1; 1,2,3,4"
 	       B = syz A 
@@ -433,7 +434,7 @@
           (toricGraver, Matrix)
      	  (toricGraver, Matrix, Ring)
      Headline
-     	  calculates the Graver basis of the toric ideal; invokes "graver" from 4ti2
+     	  calculates the Graver basis of the toric ideal
      Usage
      	  toricGraver(A) or toricGraver(A,R)
      Inputs
@@ -450,6 +451,7 @@
      	  Text
 	       The Graver basis for any toric ideal $I_A$ contains (properly) the union of all reduced Groebner basis of $I_A$.  
 	       Any element in the Graver basis of the ideal is called a primitive binomial.
+	       This invokes "graver" from 4ti2.
 	  Example
 	       A = matrix "1,1,1,1; 1,2,3,4"
 	       toricGraver(A)
@@ -467,7 +469,7 @@
      	  toricGraverDegrees
           (toricGraverDegrees, Matrix)
      Headline
-     	  displays the degrees of all Graver basis elements for the toric ideal I_A; equivalent to "output --degrees foo.gra" in 4ti2
+     	  displays the degrees of all Graver basis elements for the toric ideal I_A
      Usage
      	  toricGraverDegrees(A) 
      Inputs
@@ -477,6 +479,7 @@
      	  Text
 	       Very often the Graver basis consists of too many binomials, and one is only interested in their degrees. In this case,
 	       instead of looking at the Graver basis of $I_A$, we may just want to look for the degrees of binomials which show up:
+	       Equivalent to "output --degrees foo.gra" in 4ti2.
 	  Example
 	       A = matrix "1,1,1,1; 1,2,3,4"
 	       toricGraver(A) -- the Graver basis
--- a/M2/Macaulay2/packages/Polyhedra/documentation/old_documentation.m2
+++ b/M2/Macaulay2/packages/Polyhedra/documentation/old_documentation.m2
@@ -2824,7 +2824,7 @@
 
 document {
      Key => {sublatticeBasis, (sublatticeBasis,Matrix), (sublatticeBasis,Polyhedron)},
-     Headline => "computes a basis for the sublattice generated by integral vectors or the lattice points of a polytope",
+     Headline => "computes a basis for the sublattice generated by integral vectors or lattice points of a polytope",
      Usage => " B = sublatticeBasis M \nB = sublatticeBasis P",
      Inputs => {
 	  "M" => Matrix => {" over ",TO ZZ," with each column representing a sublattice generator"},
--- a/M2/Macaulay2/packages/SymbolicPowers.m2
+++ b/M2/Macaulay2/packages/SymbolicPowers.m2
@@ -1679,7 +1679,7 @@
 	[symbolicDefect,CIPrimes]
 	[containmentProblem,CIPrimes]
     Headline
-    	an option to compute the symbolic power by taking the intersection of the powers of the primary components
+    	compute the symbolic power by taking the intersection of the powers of the primary components
     Description
     	Text
 	    The default value is false.  When defined to be true, the @TO symbolicPower@ function tests whether the primary components are complete intersections having the same height.  If each component is, then the function takes the intersection of the powers of the components:
--- a/M2/Macaulay2/html-check-links/Makefile.in
+++ b/M2/Macaulay2/html-check-links/Makefile.in
@@ -60,7 +60,7 @@
 ifneq (,$(findstring w,$(word 1,$(MAKEFLAGS))))
 	@echo "make: Entering directory \`$(NEW)'"
 endif
-	cd $(NEW) && @FIND@ . -name \*.html -o -name \*.xhtml | xargs ls -rt | LD_LIBRARY_PATH="$(BUILTLIBPATH)/lib:$$LD_LIBRARY_PATH" xargs "$(DIR)"/html-check-links $(CHKARGS)
+	cd $(NEW) && @FIND@ . -name \*.html -o -name \*.xhtml -exec ls -rt {} + | LD_LIBRARY_PATH="$(BUILTLIBPATH)/lib:$$LD_LIBRARY_PATH" xargs "$(DIR)"/html-check-links $(CHKARGS)
 ifneq (,$(findstring w,$(word 1,$(MAKEFLAGS))))
 	@echo "make: Leaving directory \`$(NEW)'"
 endif
--- a/M2/Macaulay2/tests/normal/doc-links.m2
+++ b/M2/Macaulay2/tests/normal/doc-links.m2
@@ -6,6 +6,7 @@
 	  "<span><a title=\"an example Macaulay2 package\" href=\".*/FirstPackage/html/index.html\">FirstPackage</a> -- an example Macaulay2 package</span>",
 	  html TOH "FirstPackage::FirstPackage"))
 needsPackage "FirstPackage"
+if match("MakeDocumentation\\s* =>\\s*false", getenv "IARGS") then end
 f = FirstPackage#"raw documentation database"
 nodes = {"FirstPackage","firstFunction","firstFunction(ZZ)"}
 nodes / (n -> f#?n)
