Description: Don't link against mathicgb and friends for unit test.
Author: Doug Torrance <dtorrance@piedmont.edu>
Bug: https://github.com/Macaulay2/M2/pull/2160#issuecomment-869186309
Last-Update: 2021-06-27

--- a/M2/Macaulay2/e/unit-tests/Makefile.in
+++ b/M2/Macaulay2/e/unit-tests/Makefile.in
@@ -16,7 +16,7 @@
 CXXFLAGS += -Wno-sign-conversion #-Wno-unused-local-typedefs -coverage
 
 # note: on some machines, gcc can't find -lstdc++
-LOADLIBES += @BUILTLIBS@ @LIBS@ -lmathicgb -lmathic -lmemtailor @FCLIBS@ -pthread
+LOADLIBES += @BUILTLIBS@ @LIBS@ @FCLIBS@ -pthread
 #LOADLIBES += -lgc -lstdc++ -lgmp -lmpfr -lflint -lntl -pthread -framework vecLib # -lgivaro -llinbox
 
 # do not link with gtest -- it's supposed to get compiled in via #include:
